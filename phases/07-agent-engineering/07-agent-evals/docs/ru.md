# Урок 7.5 · Evals агентов

**Фаза 7 — Agent Engineering** · **Результат фазы:** Собрать надёжного агента с памятью, планированием, human-in-the-loop и guardrails.
<!-- exercise -->

**Requires (только для USE IT):** платный API-ключ или аккаунт eval-платформы (LangSmith и др.). BUILD IT работает офлайн, без ключей и без сети.

**Агент собран и подстрахован (7.1–7.4). Но стал ли он лучше после правки?** На глаз этого не понять. Этот урок — про то, как измерять качество агента числом и не ломать его молча.

> **MOTTO.** Агента нельзя улучшать на глаз: нужен тест-сет, метрики и защита от регрессий.

## PROBLEM

Ты поправил промпт или инструмент агента. На том примере, что смотрел, стало лучше — и ты выкатываешь. А на десяти других кейсах стало **хуже**, и ты этого не видишь: проверял-то один.

Это классическая ловушка «улучшения на глаз». Лекарство то же, что для промптов в 2.5, — **eval-набор**: фиксированный тест-сет `{input, expected}`, по которому любую версию агента можно прогнать и сравнить числом. Разница с 2.5 в том, что у агента оценивают не только итог, но и **траекторию** (какие инструменты звал, за сколько шагов). Соберём такой harness.

## CONCEPT

### Интуиция

Eval-набор — это **регрессионный тест для агента**, как юнит-тесты для кода. Ты не «надеешься, что правка не сломала старое» — ты прогоняешь фиксированный набор задач с известными ответами и смотришь, какая доля решена. Поменял агента → прогнал набор → увидел число. Стало ниже базлайна — правка плохая, откатываешь.

Ключевое слово — **фиксированный**: набор не меняется между версиями, поэтому числа сравнимы. Это превращает «вроде получше» в «было 0.7, стало 0.9».

### Как это работает

```
тест-сет [{input, expected}, ...]
        │ agent_fn (конкретная версия агента)
   scorer: success / tool-correctness / число шагов
        │ усреднили по кейсам → success_rate
        ▼
   compare_versions(...) · has_regression(baseline, candidate)
```

- `evaluate_agent(agent_fn, cases, scorer)` — прогнать агента по всем кейсам, посчитать `success_rate` и результат по каждому (`per_case`).
- `compare_versions(versions, cases)` — прогнать несколько версий и отсортировать по `success_rate`.
- `has_regression(baseline, candidate, cases)` — стал ли кандидат хуже базлайна (защита от регрессий перед выкладкой).

Два режима в проде: **offline-evals** ловят регрессии до прода (как юнит-тесты на датасете), **online-evals** меряют дрейф качества на реальном трафике (когда меняются данные, а не код).

## РАЗБОР ПО ШАГАМ

Возьмём тест-сет из трёх арифметических задач и три «версии» агента:

```
cases = [ {"2+2"→"4"}, {"3*3"→"9"}, {"10-7"→"3"} ]
good   = считает правильно      broken = всегда "?"      half = всегда "4"
```

`evaluate_agent` прогоняет каждую версию:

```
good:   "4"✓  "9"✓  "3"✓  → 3/3 = 1.0
broken: "?"✗  "?"✗  "?"✗  → 0/3 = 0.0
half:   "4"✓  "4"✗  "4"✗  → 1/3 = 0.333   (угадал только первый)
```

`compare_versions` сортирует: `good (1.0) > half (0.333) > broken (0.0)` — рейтинг версий числом. А `has_regression(good, broken)` сравнивает `0.0 < 1.0` → `True`: кандидат хуже базлайна, такую правку в прод нельзя. Главное здесь — `half` выглядел бы «рабочим», если проверять только первый кейс (`2+2`); на полном наборе видно, что он решает лишь треть. Так eval ловит то, что глаз пропускает.

## BUILD IT

**Задание: собери offline eval-harness агента** — прогон по тест-сету, сравнение версий, детект регрессий. Только стандартная библиотека, без сети.

> **Перед запуском.** Работай в своей папке курса (`ai-native/7.5-agent-evals/`), а файлы урока клади в подпапку `code/` (как в 0.1). Нужен только **Python 3** (для теста ещё `pytest`).

Создай файл `agent_eval.py`:

- **`task_success(result, expected)`** — `1.0`, если итог совпал с ожидаемым, иначе `0.0`.
- **`evaluate_agent(agent_fn, cases, scorer=task_success)`** — прогнать `agent_fn` по `cases` (`{input, expected}`); вернуть `{"success_rate": round(среднее, 3), "per_case": [...]}` (по каждому кейсу — input/output/score).
- **`compare_versions(versions, cases, scorer=task_success)`** — `versions` это `{name: agent_fn}`; вернуть список `(name, success_rate)`, отсортированный по убыванию.
- **`has_regression(baseline, candidate, cases, scorer=task_success)`** — `True`, если `success_rate` кандидата ниже базлайна.

**Готово, когда** все тесты в `test_agent_eval.py` зелёные — они проверяют: идеальный агент → 1.0; сломанный → 0.0 (и 3 записи в `per_case`); `compare_versions` ставит `good` первым, `broken` последним; `has_regression` ловит `good→broken` и не срабатывает на `good→good`.

```bash
pytest code -q          # красное → реализуй harness → зелёное
python code/agent_eval.py # демо: good=1.0, broken=0.0, регрессия детектится
```

**Подсказка.** `success_rate` — это `sum(scores) / len(cases)` (защити от пустого набора). `compare_versions` — `sorted(..., key=lambda x: x[1], reverse=True)`. `has_regression` — просто сравнение двух `success_rate`.

Внизу, в [«Исходниках урока»](#lesson-files), — три способа пройти упражнение (собрать самому · подсмотреть эталон · делегировать ИИ) и тесты-ТЗ.

## USE IT

Online-evals и регрессии в проде (мульти-провайдер):

- **LangSmith Evaluation** — offline (на датасетах, как unit-тесты) + online (скоринг прод-трафика); оценка агентных траекторий и RAG-метрик; интеграция с pytest/CI.
- **RAGAS** — метрики для RAG/agentic без ручной разметки.
- **Anthropic — empirical evaluations** — как строить надёжные eval-наборы и грейдинг (в т.ч. LLM-as-judge из 2.5).
- Встрой eval в CI: порог на `success_rate`, падение ниже базлайна — блок мерджа (ровно `has_regression`).

## SHIP IT

**Артефакт:** `agent-eval` (skill) → [`outputs/agent-eval/SKILL.md`](../outputs/agent-eval/SKILL.md)

Скилл оценки агента: подкладываешь тест-сет и версии — получаешь рейтинг и флаг регрессии. Зарегистрирован в [`.claude/skills/agent-eval`](../../../.claude/skills/agent-eval/SKILL.md). Связь с 2.5 (eval промптов), 7.4 (поломки), 10.4 (online-трейсинг). Дальше в фазе — realtime/голосовые агенты (7.6).

## ЧАСТЫЕ ОШИБКИ

- **Улучшать агента на глаз по одному примеру.** Правка может поднять один кейс и уронить десять. Только прогон по фиксированному набору показывает реальный эффект.
- **Менять тест-сет между версиями.** Тогда числа несравнимы. Набор фиксируют; растёт он отдельно (новые кейсы), но сравнение версий — на одном и том же.
- **Мерить только итог, игнорируя траекторию.** Агент мог дать верный ответ, но 20 лишними шагами или не тем инструментом. Оценивай и success, и путь (шаги, tool-correctness).
- **Нет автоматической защиты от регрессий.** Без `has_regression` в CI плохая правка просто вмёрджится. Порог на success_rate — блок мерджа.
- **Путать offline и online evals.** Offline ловит регрессии кода до прода; online ловит дрейф качества на реальных данных. Нужны оба — это разные риски.

## ПРОВЕРЬ СЕБЯ

Ответь на вопросы — проверка сразу, с пояснением.

{{quiz}}

## Материалы

- [LangSmith — Evaluation](https://docs.langchain.com/langsmith/evaluation) — offline/online evals агентов.
- [Ragas — метрики](https://docs.ragas.io/en/stable/concepts/metrics/available_metrics/) — метрики для RAG и agentic.
- [Anthropic — Create strong empirical evaluations](https://platform.claude.com/docs/en/docs/build-with-claude/define-success) — критерии успеха и построение eval-наборов.
- [τ-bench (Sierra, 2024)](https://arxiv.org/abs/2406.12045) — eval агента в реалистичном Tool-Agent-User диалоге; метрика pass^k (надёжность на повторах).
- [GAIA (2023)](https://arxiv.org/abs/2311.12983) — бенчмарк общего ассистента: многошаговые реальные задачи (человек 92% vs GPT-4 15%).
- [SWE-bench (2023)](https://arxiv.org/abs/2310.06770) — eval коддинг-агентов на реальных GitHub issue (+ вариант Verified).
- [AgentBench (ICLR'24)](https://arxiv.org/abs/2308.03688) — 8 интерактивных сред, типовые причины провалов агентов.
- [Langfuse](https://github.com/langfuse/langfuse) — open-source трейсинг + evals + датасеты (online и offline, ловля регрессий).
- [AWS — Evaluating AI agents for production (Strands Evals)](https://aws.amazon.com/blogs/machine-learning/evaluating-ai-agents-for-production-a-practical-guide-to-strands-evals/) — CI/CD quality-gate, различие pass@k vs pass^k.

---
**Часы:** ~4 · **DoD:** `pytest code -q` зелёный, демо запускается, ru.md заполнен. ✅ **Урок готов**
