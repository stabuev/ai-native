# Артефакт: матрица маршрутизации моделей

Какой класс задач на какую модель. Развитие матрицы из урока 0.1; классификатор — [`code/complexity_router.py`](../code/complexity_router.py).

## Матрица

| Класс задачи | Признаки | Модель | Почему |
|---|---|---|---|
| Массовое/простое | классификация, перевод, извлечение, короткое | Gemini Flash | дёшево на потоке |
| Рутина | черновики, короткие ответы | Haiku 4.5 | быстро и дёшево |
| Обычное (дефолт) | саммари, обычные ответы, RAG | Sonnet 4.6 | баланс цена/качество |
| Сложное | код, анализ, рассуждение, длинный контекст | Opus 4.8 | топ только где нужен |
| Дёшево + reasoning / приватность | нужен self-host или минимальная цена | DeepSeek V4 | open-weight (MIT), reasoning, можно держать у себя (Ф11) |

## Правила маршрутизации

- **Дефолт — Sonnet**, вниз (Haiku/Flash) на массовом/простом, вверх (Opus) на сложном.
- **Роутер сам должен быть дешёвым** — эвристика (`classify`) или дешёвая модель-классификатор.
- **Сомневаешься между тирами** → каскад с quality-gate (урок 9.3), а не «сразу топ».
- **Учитывай латентность** — Haiku для интерактива (быстрый TTFT).
- Матрицу пересматривай по факту качества (eval, урок 2.5) и расходов (FinOps, урок 9.4).

## Как использовать

```python
from complexity_router import route
model = route(user_request)     # 'gemini-flash' | 'sonnet-4.6' | 'opus-4.8'
```

Ожидаемая экономия маршрутизации — 40–70%; со стэком кэш+batch+каскад — до 70–90%.
