# Урок 5.2 · Code Interpreter

**Фаза 5 — Данные и аналитика** · **Результат фазы:** анализировать данные с ИИ, превращать вопросы на естественном языке в SQL-запросы (NL→SQL) и делать автоотчёты с валидацией результатов.

**Результат урока:** после урока студент сможет исполнить утверждённый `AnalysisPlan` из 5.1 в доступном Code Interpreter, отделить безопасность среды от корректности анализа и принять либо отклонить результат по входным данным, сгенерированному коду, журналу исполнения и проверкам.

**Опоры:** в 5.1 ты зафиксировал источник, смысл строки, период, фильтры, метрику и ожидаемые проверки. · **Дальше:** в 5.3 вместо файла появится база данных, а вместе с ней — SQL, права доступа и семантический слой.

**Requires:** для офлайн-практики не нужны Python, API-ключ и сеть. Для USE IT нужен доступ к любому AI-инструменту, который умеет принимать файл и исполнять код; доступность зависит от продукта, тарифа и настроек организации. Если такого инструмента нет, используй готовые журналы исполнения из урока.

> **Главная мысль.** Изоляция защищает систему от кода. Проверка анализа защищает решение от неверного кода. Это две разные задачи.

## Проблема: код успешно исполнился, но ответ всё равно неверен

В 5.1 был утверждён план:

```text
Вопрос: какие регионы принесли больше оплаченной выручки в Q2?
Источник: sales.csv.
Grain: одна строка = один заказ.
Фильтры: quarter = Q2, status = paid.
Метрика: paid_revenue = SUM(amount), RUB.
Разрез: region.
Ожидаемо: прочитано 6 строк, использовано 4, общий итог 200.
```

Теперь модель пишет Python:

```python
import pandas as pd

df = pd.read_csv("sales.csv")
selected = df.query("quarter == 'Q2'")
result = selected.groupby("region")["amount"].sum()
```

Код синтаксически корректен. Изолированная среда запускает его без ошибки и возвращает:

```text
Москва = 150
Питер = 100
Казань = 20
Итого = 270
```

Среда выполнила свою работу: код не получил лишнего доступа к компьютеру и завершился успешно. Но модель забыла фильтр `status == "paid"`, поэтому в Питер попал возврат `70`. **Успешное исполнение не означает правильный анализ.**

Отсюда два независимых вопроса:

1. **Можно ли безопасно исполнить этот код?**
2. **Соответствует ли код утверждённому плану и данным?**

Code Interpreter помогает с первым вопросом. Ответственность за второй остаётся у человека.

## Центральная модель

### Что такое Code Interpreter

**Code Interpreter** — это не отдельная модель и не способность LLM безошибочно считать «в уме». Это рабочий контур:

```text
утверждённый план
        ↓
модель предлагает код
        ↓
отдельная среда исполняет код
        ↓
возвращает результат, ошибку и созданные файлы
        ↓
человек проверяет код и доказательства
```

В разных продуктах эта возможность может называться data analysis, Python tool или code execution. Название меняется, а роли остаются:

- **Модель** переводит задачу в код и при необходимости предлагает исправление.
- **Среда исполнения** запускает код с заданными возможностями и ограничениями.
- **Человек** подтверждает смысл расчёта и принимает результат.

### Sandbox и container обычными словами

**Sandbox**, или песочница, — среда, которая отделяет недоверенный код от основной системы. Код получает не весь компьютер, а только разрешённую рабочую область.

**Container**, или контейнер, — один из способов предоставить такую отдельную область: собственные процессы, файлы, библиотеки и лимиты. Некоторые платформы используют контейнеры, другие описывают среду иначе. Для пользователя важен не бренд технологии, а проверяемая граница доступа.

Функция Python `exec(text)` исполняет переданный текст как код в текущем процессе. Ограничение доступных ей функций не создаёт отдельную защитную границу: код всё ещё может зависнуть, исчерпать память или воспользоваться переданным ему объектом. Поэтому в этом уроке мы **не запускаем произвольный код модели локально** и не собираем псевдо-sandbox.

### Capability: что среде разрешено

**Capability**, или возможность среды, — конкретное действие, которое она разрешает коду. Перед рабочим запуском нужно выяснить:

| Граница | Что проверить |
|---|---|
| Входные файлы | Какие файлы доступны и как подтверждается их версия? |
| Файловая область | Видит ли код только рабочую папку или другие файлы тоже? |
| Сеть | Может ли код обращаться к внешним сайтам и API? |
| Библиотеки | Какие пакеты уже установлены и можно ли добавлять новые? |
| Ресурсы | Есть ли ограничения времени, памяти, вычислительной мощности и размера файлов? |
| Состояние | Сохраняются ли переменные и созданные файлы между запусками? |
| Хранение | Как долго провайдер хранит входы, код и результаты? |
| Выходы | Можно ли увидеть код, ошибки, таблицы и созданные файлы? |

У этих вопросов нет универсальных ответов. Возможности различаются между продуктами и меняются со временем. Записывай только то, что подтверждено интерфейсом, настройками организации или актуальной документацией. Неизвестное помечай как `unknown`, а не угадывай.

### Execution trace: доказательства одного запуска

**Execution trace**, или журнал исполнения, — связанная запись того, что было запущено и что произошло:

```text
план и входы
+ сгенерированный код
+ среда и известные ограничения
+ статус, ошибки и исправления
+ результаты и созданные файлы
+ проверки и решение человека
```

Один финальный ответ модели не является полным журналом. Если первая версия кода упала, а вторая сработала, полезно сохранить обе: ошибка показывает, что менялось и почему финальный результат отличается.

## Три уровня проверки

### 1. Безопасность исполнения

Проверь, что код запускается не в основном процессе и получает только допустимые возможности. Это снижает риск доступа к локальным файлам, секретам, сети и ресурсам.

### 2. Корректность анализа

Сопоставь `AnalysisPlan` с кодом строка за строкой:

| Поле плана | Что искать в коде и результате |
|---|---|
| Источник | Открыт именно согласованный файл |
| Grain и row ID | Строки не задвоены и считаемая единица не изменилась |
| Период | Применён нужный период |
| Фильтры | Все условия включения и исключения присутствуют |
| Метрика | Использованы правильная колонка, агрегат и единица |
| Разрез | Группировка выполнена по нужной колонке |
| Проверки | Видны read / used / excluded, полный итог и сверка |

Показанный код делает расчёт проверяемым, но не проверяет себя автоматически. Код может быть аккуратным, воспроизводимым и неверным по смыслу.

### 3. Допустимость данных

Даже хороший sandbox не даёт разрешения загружать в него любые данные. До загрузки проверь правила организации, чувствительность колонок, одобренный продукт, настройки хранения и необходимость обезличивания. Неизвестная политика хранения — причина остановиться и уточнить, а не предположить удаление «после чата».

## Разбор двух журналов

### Запуск A: технически успешный, аналитически неверный

```text
input: sales.csv, 6 строк
code filter: quarter == Q2
execution status: success
rows read / used / excluded: 6 / 5 / 1
overall: 270 RUB
```

Проверка:

- среда исполнила код без ошибки;
- период Q2 применён;
- фильтр `status=paid` отсутствует;
- использовано 5 строк вместо ожидаемых 4;
- итог `270` не совпадает с ожидаемой сверкой `200`.

**Решение:** `reject`. Безопасный запуск не компенсирует неверный фильтр.

### Запуск B: технически и аналитически согласованный

```python
import pandas as pd

df = pd.read_csv("sales.csv")
selected = df.query("quarter == 'Q2' and status == 'paid'")
groups = selected.groupby("region")["amount"].sum().sort_values(ascending=False)
overall = selected["amount"].sum()
```

```text
execution status: success
rows read / used / excluded: 6 / 4 / 2
groups: Москва 150, Питер 30, Казань 20
overall: 200 RUB
reconciliation: 150 + 30 + 20 = 200
```

**Решение:** `accept` для учебного вопроса, если `sales.csv` является согласованным источником и загрузка данных разрешена.

Сравнение показывает центральный механизм:

| Свойство | Запуск A | Запуск B |
|---|---:|---:|
| Код исполнился | Да | Да |
| Среда изолирована | По контракту среды | По контракту среды |
| Все фильтры плана применены | Нет | Да |
| Итог сошёлся | Нет | Да |
| Решение | Reject | Accept |

## ОФЛАЙН-ПРАКТИКА: принять или отклонить запуск

Работай в созданной в фазе 0 папке `ai-native-work/course-work/phase-5/code-interpreter/`. Клонировать курс не нужно. Скопируй со страницы шаблон карточки исполнения и сначала самостоятельно оцени сценарий.

### Сценарий

План требует источник `sales.csv`, фильтры Q2 + paid и итог `200`. Журнал сообщает:

```text
input file: sales-final-v2.csv
rows read / used / excluded: 6 / 4 / 2
code filters: quarter == Q2 and status == paid
overall: 200 RUB
execution status: success
environment: отдельная среда; сеть запрещена; срок хранения unknown
```

Ответь письменно:

1. Какое решение принимаешь: `accept`, `reject` или `needs clarification`?
2. Какие доказательства уже достаточны?
3. Какой факт отсутствует?
4. Какое минимальное действие разблокирует решение?

Сильный ответ — `needs clarification`: код и числа согласованы, но имя входного файла отличается от утверждённого источника, а связь версий не подтверждена. Нужно подтвердить происхождение `sales-final-v2.csv` или повторить запуск на согласованном файле. Неизвестный срок хранения отдельно блокирует загрузку рабочих чувствительных данных, но не мешает учебному синтетическому примеру.

## USE IT

Используй синтетический `sales.csv` из 5.1. Не загружай рабочие данные только ради прохождения урока.

### Шаг 1. Передай утверждённый план

```text
Исполни этот AnalysisPlan над загруженным sales.csv.
Не меняй определение метрики и фильтры без явного согласования.
Покажи сгенерированный код, строки read / used / excluded,
полный результат по группам, общий итог и контрольную сверку.
Если код упадёт и ты его исправишь, сохрани обе версии и объясни изменение.
```

### Шаг 2. Зафиксируй среду

Запиши продукт, режим и дату. По интерфейсу, настройкам организации или официальной документации установи файловую область, доступ к сети, состояние между запусками, лимиты и срок хранения. Если факт не найден, запиши `unknown`.

Не проси модель угадать политику собственного провайдера. Ответ модели о среде не заменяет документацию и настройки аккаунта.

### Шаг 3. Проверь код против плана

Найди в коде источник, оба фильтра, `SUM(amount)`, группировку по `region` и общий итог. Затем сверь `6 / 4 / 2`, группы `150 / 30 / 20` и итог `200`.

### Шаг 4. Сохрани решение

Заполни карточку исполнения. Если что-то не подтверждено, выбери `needs clarification` и назови одно следующее действие. Не переписывай неопределённость как уверенный вывод.

Если у тебя нет Code Interpreter, заполни карточку по запуску A или офлайн-сценарию. Результат урока — качественное решение по доказательствам, а не доступ к конкретному платному продукту.

## SHIP IT

**Артефакт:** карточка проверки исполнения → [`outputs/execution-review-card.md`](../outputs/execution-review-card.md)

Карточка должна связать утверждённый план, конкретный вход, код, среду, журнал, проверки и решение `accept / reject / needs clarification`. Она готова, если другой человек может понять, что исполнялось, почему результат принят или отклонён и какой неизвестный факт ещё требует ответа.

## Реалистичные ошибки

- **Считать зелёный статус доказательством правильности.** `success` означает, что код завершился, но не подтверждает фильтры, метрику и источник.
- **Назвать урезанный `exec` песочницей.** Ограничение функций внутри основного процесса не создаёт изоляцию по процессам, файлам и ресурсам.
- **Проверить код, но не вход.** Правильный расчёт над другой версией файла отвечает на другой набор данных.
- **Проверить только финальную версию.** Скрытая ошибка и автоматическое исправление могут изменить метод; сохрани обе версии.
- **Предположить отсутствие сети или мгновенное удаление данных.** Возможности и хранение зависят от продукта и конфигурации; неизвестное остаётся неизвестным.
- **Загрузить рабочие данные до проверки политики.** Sandbox ограничивает код, но не отменяет правила доступа, хранения и передачи данных.
- **Просить только число.** Для приёмки нужны код, read / used / excluded, полный итог и сверка.
- **Считать показанный код достаточным.** Его ещё нужно сопоставить с утверждённым `AnalysisPlan`.

## Что сознательно осталось за границей

- Построение собственного sandbox и способы обхода изоляции.
- Контейнеры, системные вызовы и низкоуровневые механизмы безопасности.
- Сравнение провайдеров и нестабильные лимиты конкретных тарифов.
- Регрессия, статистические тесты, z-score, IQR и другие новые аналитические методы.
- Работа с производственными и чувствительными данными.
- SQL, права базы данных и семантический слой: это следующий урок.
- Подробная безопасность исполнения и доступов: это 6.5.

## Переход в 5.3

В 5.2 исполнителем была отдельная среда с файлами:

```text
AnalysisPlan → Python → файловая среда → execution trace
```

В 5.3 исполнителем станет база данных:

```text
вопрос → SQL → соединение с БД → результат
```

Появятся новые границы: какие таблицы видны, с какими правами выполняется запрос и кто определяет смысл «выручки». Карточка исполнения сохранит те же вопросы про вход, код, результат и принятие, а семантический слой зафиксирует метрики для всех запросов.

## ПРОВЕРЬ СЕБЯ

Вопросы проверяют различие между изоляцией и корректностью, работу с неизвестными ограничениями и решение по журналу исполнения.

{{quiz}}

## Дополнительное чтение

Это необязательные материалы: для завершения урока читать их не нужно. Выбери одну ветку по своей задаче, а не проходи весь список подряд.

### Если используешь готовый инструмент

- [OpenAI — Data analysis with ChatGPT](https://help.openai.com/en/articles/8437071-code-interpreter) — открой разделы **Reviewing analysis details**, **Privacy and uploaded files** и **Constraints**, чтобы сверить код, допущения, работу с файлами и ограничения среды ChatGPT.
- [OpenAI — политика хранения чатов и файлов](https://help.openai.com/ru-ru/articles/8983778-chat-and-file-retention-policies-in-chatgpt) — используй для полей «срок хранения» и «удаление» в карточке исполнения; обрати внимание, что правила зависят от типа чата, файла и рабочего пространства.
- [Anthropic — Code execution tool](https://platform.claude.com/docs/en/agents-and-tools/tool-use/code-execution-tool) — если работаешь через Claude API, сравни разделы **How code execution works**, **Networking and security**, **Resource limits**, **Container reuse** и **Data retention** с контрактом среды из урока.

### Если строишь интеграцию

- [OpenAI — Code Interpreter API](https://developers.openai.com/api/docs/guides/tools-code-interpreter) — открой разделы о containers и files, чтобы понять, как приложение создаёт или переиспользует среду и передаёт ей входные данные.
- [E2B](https://github.com/e2b-dev/E2B) — инженерная ветка после урока: разделы **What is E2B?** и **Run your first Sandbox** показывают отдельный облачный исполнитель и его программный интерфейс; устанавливать или запускать E2B для 5.2 не требуется.

### Если углубляешь проверяемость и безопасность

- [The Turing Way — Guide for Reproducible Research](https://book.the-turing-way.org/reproducible-research/reproducible-research/) — расширяет карточку исполнения до общей модели воспроизводимости: вместе сохраняются данные, инструменты, код и результаты.
- [Embrace The Red — Lack of Isolation between Code Interpreter sessions of GPTs](https://embracethered.com/blog/posts/2024/lack-of-isolation-gpts-code-interpreter/) — исторический разбор того, почему нельзя предполагать границы среды без проверки; описанная уязвимость была исправлена в мае 2024 года, поэтому материал нужен как кейс рассуждения, а не как описание текущего состояния продукта.

---
**Часы:** ~4 · **DoD:** студент отличает модель от среды исполнения; по журналу обнаруживает пропущенный фильтр и неподтверждённый источник; карточка связывает план, вход, код, ограничения, результат и решение; недоверенный код не запускается локально. ✅ **Урок готов**
