# Урок 5.1 · ИИ как аналитик

**Фаза 5 — Данные и аналитика** · **Результат фазы:** Анализировать данные с ИИ, делать NL→SQL и автоотчёты с валидацией результатов.
<!-- exercise -->

**Requires (только для USE IT):** платный API-ключ или аккаунт ChatGPT/Claude с загрузкой файлов. BUILD IT работает офлайн, без ключей и без сети.

**Это вход в Фазу 5 — данные.** Дальше будут Code Interpreter, NL→SQL, BI-агенты и автоотчёты. Но в основе всего лежит одна простая идея, которую мы разберём здесь до винтика: ИИ-аналитик не «понимает» таблицу магически — он строит **план** и исполняет его кодом.

> **MOTTO.** Аналитика — это план: что сгруппировать, что посчитать, как агрегировать; ИИ строит план, код его исполняет.

## PROBLEM

«Посмотри данные и скажи, что интересного» — звучит как магия. Кажется, будто модель смотрит на таблицу и «видит» инсайт. Это не так — и пока механика выглядит магией, ты не можешь ни проверить ответ, ни понять, почему он иногда неверный.

На деле ИИ-аналитик делает буквально следующее: превращает твой вопрос в **план** (по чему сгруппировать, какую колонку считать, каким агрегатом) и **исполняет его кодом** (pandas / Code Interpreter), а не «угадывает» цифры из головы. Соберём этот механизм на голой стандартной библиотеке — и магии не останется.

## CONCEPT

### Интуиция

Любой аналитический вопрос раскладывается на три кубика:

- **разрез (group_by)** — по чему режем: по региону, по продукту, по дню;
- **метрика (value)** — что именно меряем: сумму чего, среднее чего;
- **агрегат (agg)** — как сворачиваем группу в одно число: `sum`, `mean`, `count`.

«Выручка по регионам» = разрез `region` + метрика `amount` + агрегат `sum`. «Средний чек по продукту» = разрез `product` + метрика `amount` + агрегат `mean`. Вопрос на человеческом языке — это и есть план из трёх кубиков; работа ИИ — собрать план, работа кода — его исполнить.

### Как это работает

Механизм называется **split-apply-combine** — три шага:

```
вопрос → план(group_by, value, agg) → исполнение → результат
                                          │
   split    разложить строки по группам (по group_by)
   apply    в каждой группе применить агрегат к value (sum/mean/count)
   combine  собрать {группа: число} и при желании взять топ-N
```

- `load_csv(text)` — прочитать таблицу в список словарей (строки).
- `aggregate(rows, group_by, value, agg)` — сгруппировать строки и свернуть каждую группу в число.
- `top_n(d, n)` — отсортировать результат по убыванию и взять верх.

Ровно это делает pandas (`df.groupby(...).sum()`), и ровно это собирает под капотом ChatGPT/Claude, когда ты грузишь CSV и спрашиваешь «топ-5 регионов по выручке». Мы делаем то же руками, чтобы видеть каждый шаг.

## РАЗБОР ПО ШАГАМ

Возьмём таблицу продаж и вопрос «выручка по регионам, топ-2» (план: `group_by=region`, `value=amount`, `agg=sum`):

```
region  amount
Москва  100
Москва   50
Питер    30
Питер    70
Казань   20
```

**split** — раскладываем строки по `region`:

```
Москва → [100, 50]
Питер  → [30, 70]
Казань → [20]
```

**apply** — в каждой группе берём `sum` колонки `amount`:

```
Москва → 100 + 50 = 150
Питер  → 30 + 70  = 100
Казань → 20
```

**combine** — собираем `{Москва: 150, Питер: 100, Казань: 20}` и берём `top_n(2)` → `[(Москва, 150), (Питер, 100)]`.

Теперь **поменяем один кубик** — `agg=sum` на `agg=mean` — на тех же данных и том же `split`:

```
apply (mean):  Москва → (100+50)/2 = 75    Питер → (30+70)/2 = 50    Казань → 20/1 = 20
combine:       {Москва: 75, Питер: 50, Казань: 20}
```

Данные и группировка те же, а ответ другой: «выручка» (sum 150/100/20) превратилась в «средний чек» (mean 75/50/20), а `agg=count` дал бы число продаж (2/2/1). Вот почему план важнее данных: один и тот же `split` отвечает на разные вопросы — решает выбор агрегата.

## BUILD IT

**Задание: собери мини-аналитику** — `load_csv` + `aggregate` + `top_n`, по принципу split-apply-combine. Только стандартная библиотека (`csv`, `io`, `statistics`), без сети.

> **Перед запуском.** Работай в своей папке курса (`ai-native/5.1-ai-analyst/`), а файлы урока клади в подпапку `code/` (как в 0.1). Нужен только **Python 3** (для теста ещё `pytest`).

Создай файл `analyze.py` с тремя функциями:

- **`load_csv(text)`** — распарсить CSV-текст в список словарей (строка таблицы = словарь `{колонка: значение}`). Подсказка: `csv.DictReader(io.StringIO(text.strip()))`.
- **`aggregate(rows, group_by, value, agg="sum")`** — сгруппировать `rows` по колонке `group_by`; в каждой группе свернуть колонку `value`: `sum` или `mean` числовых значений, либо `count` строк. Верни словарь `{группа: число}`. Нечисловые/пустые значения в `value` при sum/mean пропускай.
- **`top_n(d, n=3)`** — вернуть список из `n` пар `(ключ, значение)` по убыванию значения.

**Готово, когда** все тесты в `test_analyze.py` зелёные — они проверяют: парсинг строк; `sum` по региону (Москва = 150, Питер = 100); `count` и `mean`; `top_n` сортирует по убыванию (первый — Москва, 150).

```bash
pytest code -q        # красное → реализуй три функции → зелёное
python code/analyze.py # демо: выручка по регионам, топ-2, средний чек по продукту
```

**Подсказка.** Группировка: `groups.setdefault(r.get(group_by), []).append(r)`. Числа защити хелпером `_num(v)` через `try/except float(...)`. `top_n` — `sorted(d.items(), key=lambda kv: kv[1], reverse=True)[:n]`.

Внизу, в [«Исходниках урока»](#lesson-files), — три способа пройти упражнение (собрать самому · подсмотреть эталон · делегировать ИИ) и тесты-ТЗ.

## USE IT

Тот же план исполняет pandas — и ровно это делает ИИ-аналитик при загрузке файла (мульти-провайдер):

```python
import pandas as pd
df = pd.read_csv("sales.csv")
df.groupby("region")["amount"].sum().sort_values(ascending=False)   # = aggregate + top_n
```

- **ChatGPT** — загрузи CSV/Excel и спроси «выручка по регионам, топ-5»: он напишет и выполнит pandas-код в Code Interpreter и построит график.
- **Claude** — загрузка файла в чат или Project + тот же запрос; через API — code execution tool.

Принцип один: естественный вопрос → план (group_by / value / agg) → исполнение кодом. Ты по-прежнему отвечаешь за то, чтобы план соответствовал вопросу.

⚠️ ИИ не знает происхождения данных и твоих допущений (что считать «выручкой», включать ли возвраты). Результат всегда **проверяй** — этому посвящён урок 5.5.

## SHIP IT

**Артефакт:** Шаблон аналитического запроса → [`outputs/analysis-request-template.md`](../outputs/analysis-request-template.md)

Структура запроса к ИИ-аналитику (данные · вопрос · метрика · разрез · формат · проверка) — чтобы ответы были воспроизводимы и проверяемы, а не «как поняла модель». Дальше: исполнение кода в песочнице (5.2), NL→SQL (5.3), валидация и автоотчёты (5.5).

## ЧАСТЫЕ ОШИБКИ

- **Считать, что ИИ «видит» инсайт в таблице.** Он строит план и исполняет код. Нет плана — нет воспроизводимости; проси его показать код/логику, а не только число.
- **Не задать определения метрики.** «Выручка» с возвратами или без? «Активные» за какой период? Без явных допущений модель подставит свои — и цифра поедет.
- **Путать агрегаты.** `sum` ≠ `mean` ≠ `count`. «Сколько продали» — это `sum` суммы или `count` сделок? Уточни кубик `agg`, иначе ответишь не на тот вопрос.
- **Доверять числу без проверки источника.** ИИ не знает, откуда данные и насколько чисты. Сверяй с источником и здравым смыслом (5.5).
- **Грузить гигантский файл целиком в чат.** Окно конечно (4.4); для больших данных — выборка, агрегаты заранее или SQL (5.3), а не сырой дамп.

## ПРОВЕРЬ СЕБЯ

Ответь на вопросы — проверка сразу, с пояснением.

{{quiz}}

## Материалы

- [pandas — Getting started](https://pandas.pydata.org/docs/getting_started/index.html) — стандартный инструмент анализа данных (`groupby`).
- [OpenAI — Data analysis / Code Interpreter](https://help.openai.com/en/articles/8437071-code-interpreter) — анализ загруженных файлов кодом.
- [Anthropic — Code execution tool](https://platform.claude.com/docs/en/agents-and-tools/tool-use/code-execution-tool) — анализ данных кодом через API.
- [Wes McKinney — Python for Data Analysis (3e, бесплатно онлайн)](https://wesmckinney.com/book/) — «библия» pandas от автора библиотеки.
- [PandasAI](https://github.com/sinaptik-ai/pandas-ai) — естественный язык поверх dataframe: «pandas под управлением промпта» (v3.0.0, 2025).
- [Anthropic — Use Claude for Excel](https://support.claude.com/en/articles/12650343-use-claude-for-excel) — свежая интеграция: читать, править и создавать таблицы прямо в Excel (CSV/Excel в Claude).
- [TDS — My First Exploratory Data Analysis with ChatGPT](https://towardsdatascience.com/my-first-exploratory-data-analysis-with-chatgpt-7f100005efdc/) — практический разбор EDA через чат (Medium).
- [DABstep (Adyen × Hugging Face, 2025)](https://arxiv.org/abs/2506.23719) — бенчмарк агентов-аналитиков на 450+ реальных задачах: лучшие модели решают лишь ~15% сложных — числовой аргумент, почему результат ИИ-аналитика надо проверять (5.5).

---
**Часы:** ~4 · **DoD:** `pytest code -q` зелёный, демо запускается, ru.md заполнен. ✅ **Урок готов**
