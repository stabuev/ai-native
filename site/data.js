/* СГЕНЕРИРОВАНО build.js — не редактировать вручную. */
const META = {
  "title": "AI Native: From Scratch to Agentic Systems",
  "phases": 13,
  "lessons": 59,
  "done": 59,
  "hours": 228,
  "built": "2026-06-21"
};
const PHASES = [{"id":0,"name":"Setup & Tooling — Настройка и инструменты","lessons":[{"id":"0.1","title":"Карта инструментов ИИ 2026","status":"done","motto":"Не «одна модель на всё», а правильная модель под конкретную задачу.","hours":2,"dir":"phases/00-setup-and-tooling/00-ai-tools-map","html":"<h1>Урок 0.1 · Карта инструментов ИИ 2026</h1><p><strong>Фаза 0 — Setup &amp; Tooling</strong> · <strong>Результат фазы:</strong> Настроить окружение, ключи и агента; осознанно выбрать модель под задачу.</p><blockquote><strong>MOTTO.</strong> Не «одна модель на всё», а правильная модель под конкретную задачу.</blockquote><h2>PROBLEM</h2><p>Моделей и инструментов в 2026 — десятки, и легко свалиться в две крайности: гонять всё через самую дорогую модель «на всякий случай» (счёт за API улетает в космос) или через самую дешёвую (качество проседает на сложном). Без карты «что под что» выбор превращается в гадание, а различия между Claude, GPT, Gemini, Copilot и Perplexity сливаются в кашу.</p><h2>CONCEPT</h2><p>Инструменты делятся на два слоя:</p><ul><li><strong>Модели через API</strong> — то, что вызываешь кодом (Claude, GPT, Gemini). Платишь за токены, выбираешь под задачу.</li><li><strong>Готовые продукты</strong> — обёртки вокруг моделей под сценарий: Copilot (код в IDE/M365), Perplexity (поиск с источниками), чат-интерфейсы.</li></ul><p>Ключевая ось выбора модели — <strong>сложность × объём × цена × задержка</strong>:</p><pre><code>       дорого/умно\n            ▲\n   Opus 4.8 │ сложное рассуждение, код, анализ\n Sonnet 4.6 │ рабочая лошадка\n   GPT-5.x  │ универсальные задачи\n  Haiku 4.5 │ рутина, черновики, извлечение\nGemini Flash│ массовое, классификация\nDeepSeek V4 │ дёшево + reasoning + open-weight (self-host)\n            └──────────────► дёшево/быстро/массово</code></pre><p>Правило: бери <strong>самую дешёвую модель, которая справляется</strong>, и эскалируй вверх только когда качество не дотягивает (мост к Фазе 9 — маршрутизация и FinOps).</p><p>Отдельная ось — <strong>открытость весов</strong>: закрытые API (Claude/GPT/Gemini) vs <strong>open-weight</strong> (DeepSeek под MIT, Llama, Qwen, Mistral), которые можно запустить у себя. Open-weight решает приватность/суверенитет данных (мост к Фазе 11), но self-host выгоден не по цене, а ради контроля.</p><h2>BUILD IT</h2><p>Реестр моделей и рекомендатор «задача → модель» с нуля, без зависимостей: [<code>code/model_map.py</code>](../code/model_map.py).</p><ul><li><code>REGISTRY</code> — модели с ценой входа/выхода, тиром и сильными сторонами;</li><li><code>recommend(task)</code> — по профилю задачи (<code>complexity</code>, <code>high_volume</code>, <code>needs_code</code>, <code>budget_sensitive</code>) возвращает модель и обоснование;</li><li><code>estimate_cost(model, n_in, n_out)</code> — прикидка стоимости вызова.</li></ul><pre><code>python code/model_map.py\npytest code -q</code></pre><p>Демо показывает суть: простое+массовое → Gemini Flash, обычное → Sonnet, сложное/код → Opus.</p><h2>USE IT</h2><p>Прогони <strong>один и тот же запрос</strong> через разные инструменты и сравни ответ, скорость и уместность:</p><ul><li><strong>Claude</strong> (Opus 4.8 / Mythos) — сложное рассуждение, длинный контекст, код;</li><li><strong>GPT-5.x</strong> — универсальный baseline;</li><li><strong>Gemini</strong> — массовые задачи, мультимодальность, дешёвый Flash;</li><li><strong>Copilot</strong> — код прямо в IDE и в M365;</li><li><strong>Perplexity</strong> — вопросы, где нужен поиск с источниками.</li></ul><p>Вызов модели кодом одинаков по форме у всех провайдеров (детали — в уроке 0.2):</p><pre><code># Anthropic\nfrom anthropic import Anthropic\nAnthropic().messages.create(model=\"claude-opus-4-8\", max_tokens=300,\n    messages=[{\"role\": \"user\", \"content\": \"Сравни 2 подхода к кэшированию\"}])\n\n# OpenAI\nfrom openai import OpenAI\nOpenAI().responses.create(model=\"gpt-5.x\", input=\"Сравни 2 подхода к кэшированию\")\n\n# Google\nfrom google import genai\ngenai.Client().models.generate_content(model=\"gemini-2.x-flash\",\n    contents=\"Сравни 2 подхода к кэшированию\")</code></pre><h2>SHIP IT</h2><p><strong>Артефакт:</strong> Личная матрица «задача → модель» → [<code>outputs/task-to-model-matrix.md</code>](../outputs/task-to-model-matrix.md)</p><p>Заполняешь под свои реальные задачи (какую модель берёшь и почему), а <code>recommend()</code> из Build It даёт стартовую рекомендацию. Матрица возвращается в Фазе 9, когда будем строить автоматический роутер.</p><h2>Материалы</h2><ul><li><a href=\"https://platform.claude.com/docs/en/about-claude/models/overview\" target=\"_blank\" rel=\"noopener\">Claude models overview</a> — официальная карта моделей Anthropic и их сильные стороны.</li><li><a href=\"https://openai.com/api/pricing/\" target=\"_blank\" rel=\"noopener\">OpenAI API pricing</a> — актуальные цены моделей OpenAI за токены.</li><li><a href=\"https://ai.google.dev/gemini-api/docs\" target=\"_blank\" rel=\"noopener\">Gemini API — models</a> — модели Google и когда брать Flash.</li><li><a href=\"https://api-docs.deepseek.com/quick_start/pricing\" target=\"_blank\" rel=\"noopener\">DeepSeek — Models &amp; Pricing</a> — дешёвый open-weight вариант (V4-Flash/Pro).</li></ul><hr><p><strong>Часы:</strong> ~2 · <strong>DoD:</strong> <code>pytest code -q</code> зелёный, демо запускается, ru.md заполнен. ✅ <strong>Урок готов</strong></p>"},{"id":"0.2","title":"Аккаунты, тарифы, API-ключи, окружение","status":"done","motto":"Ключи — в `.env`, секреты — не в коде, окружение — воспроизводимо.","hours":2,"dir":"phases/00-setup-and-tooling/00-accounts-keys-env","html":"<h1>Урок 0.2 · Аккаунты, тарифы, API-ключи, окружение</h1><p><strong>Фаза 0 — Setup &amp; Tooling</strong> · <strong>Результат фазы:</strong> Настроить окружение, ключи и агента; осознанно выбрать модель под задачу.</p><blockquote><strong>MOTTO.</strong> Ключи — в <code>.env</code>, секреты — не в коде, окружение — воспроизводимо.</blockquote><h2>PROBLEM</h2><p>Первый запрос к API спотыкается не о модель, а о быт: где взять ключ, куда его положить, почему <code>ANTHROPIC_API_KEY</code> «не виден», и как не закоммитить секрет в git. Захардкоженный ключ в коде утекает в репозиторий и в историю; «работает у меня» рушится на другой машине без зафиксированного окружения.</p><h2>CONCEPT</h2><p>Здоровое окружение состоит из трёх вещей:</p><pre><code>venv (изоляция)  +  .env (секреты)  +  .env.example (контракт)\n        │                  │                    │\n   свои пакеты      ключи вне кода        что нужно завести\n   не ломают         и вне git           (без значений)\n   систему         (.gitignore)</code></pre><ul><li><strong>venv</strong> — изолированный Python, чтобы зависимости урока не конфликтовали с системными.</li><li><strong>.env</strong> — файл <code>KEY=VALUE</code> с секретами; он **в <code>.gitignore</code>**, в репозиторий не попадает.</li><li><strong>.env.example</strong> — тот же список ключей без значений; коммитится как контракт «что нужно завести».</li></ul><h2>BUILD IT</h2><p>Парсер <code>.env</code> и валидатор ключей с нуля, без зависимостей и <strong>без сети</strong>: [<code>code/envcheck.py</code>](../code/envcheck.py).</p><ul><li><code>parse_env(text)</code> — разбирает <code>.env</code> (комментарии, <code>export</code>, кавычки, inline-комментарии);</li><li><code>validate(env, required)</code> — проверяет, что нужные ключи заданы, не плейсхолдеры и похожи на ключ по префиксу.</li></ul><pre><code>python -m venv .venv &amp;&amp; source .venv/bin/activate\npip install -r requirements.txt\npython code/envcheck.py\npytest code -q</code></pre><p>Валидатор офлайн ловит типовые ошибки (нет ключа, остался <code>your-key-here</code>, неверный префикс) до того, как ты потратишь время на отлов 401 от API.</p><h2>USE IT</h2><p>Заведи аккаунты и ключи в консолях провайдеров, затем сделай первый реальный запрос:</p><ul><li><strong>Anthropic Console</strong> → <code>ANTHROPIC_API_KEY</code> (<code>sk-ant-…</code>)</li><li><strong>OpenAI Platform</strong> → <code>OPENAI_API_KEY</code> (<code>sk-…</code>)</li><li><strong>Google AI Studio</strong> → <code>GOOGLE_API_KEY</code></li></ul><pre><code>import os\n# ключи берутся из окружения, а не из кода\n# Anthropic\nfrom anthropic import Anthropic\nprint(Anthropic().messages.create(model=\"claude-haiku-4-5\", max_tokens=50,\n    messages=[{\"role\": \"user\", \"content\": \"Привет одним словом\"}]).content[0].text)\n\n# OpenAI\nfrom openai import OpenAI\nprint(OpenAI().responses.create(model=\"gpt-5.x\", input=\"Привет одним словом\").output_text)\n\n# Google\nfrom google import genai\nprint(genai.Client().models.generate_content(\n    model=\"gemini-2.x-flash\", contents=\"Привет одним словом\").text)</code></pre><p>Бери для проверки <strong>самую дешёвую</strong> модель (Haiku / Flash) — первый запрос не должен стоить дорого.</p><h2>SHIP IT</h2><p><strong>Артефакт:</strong> Готовое окружение + проверочный скрипт → [<code>outputs/env-starter/</code>](../outputs/env-starter/)</p><p>Drop-in набор для любого проекта курса: <code>.env.example</code> (контракт ключей), <code>check_env.py</code> (самодостаточный валидатор без зависимостей) и <code>.gitignore</code>-строка для <code>.env</code>. Копируешь в новый проект — и окружение проверяемо с первой минуты.</p><h2>Материалы</h2><ul><li><a href=\"https://docs.anthropic.com/en/docs/quickstart\" target=\"_blank\" rel=\"noopener\">Anthropic — Quickstart</a> — ключ и первый запрос к Claude.</li><li><a href=\"https://platform.openai.com/docs/quickstart\" target=\"_blank\" rel=\"noopener\">OpenAI — Developer quickstart</a> — ключ и первый запрос к OpenAI.</li><li><a href=\"https://ai.google.dev/gemini-api/docs/api-key\" target=\"_blank\" rel=\"noopener\">Gemini — API keys</a> — получение и хранение ключа Google.</li><li><a href=\"https://pypi.org/project/python-dotenv/\" target=\"_blank\" rel=\"noopener\">python-dotenv</a> — загрузка <code>.env</code> в окружение (12-factor).</li></ul><hr><p><strong>Часы:</strong> ~2 · <strong>DoD:</strong> <code>pytest code -q</code> зелёный, демо запускается, ru.md заполнен. ✅ <strong>Урок готов</strong></p>"},{"id":"0.3","title":"Агент и SkillKit","status":"done","motto":"Агент + скиллы превращают модель из «чата» в рабочий инструмент с памятью навыков.","hours":2,"dir":"phases/00-setup-and-tooling/00-agent-and-skillkit","html":"<h1>Урок 0.3 · Агент и SkillKit</h1><p><strong>Фаза 0 — Setup &amp; Tooling</strong> · <strong>Результат фазы:</strong> Настроить окружение, ключи и агента; осознанно выбрать модель под задачу.</p><blockquote><strong>MOTTO.</strong> Агент + скиллы превращают модель из «чата» в рабочий инструмент с памятью навыков.</blockquote><h2>PROBLEM</h2><p>Голая модель в чате забывает контекст и не умеет твоих приёмов. Агент (Claude Code / Cursor / Codex) живёт в твоём репозитории, видит файлы и запускает команды — но без <strong>скиллов</strong> каждый раз объясняешь одно и то же заново. И ещё: курс на 200 часов нельзя проходить вслепую — нужно понять, что пролистать, а на чём сфокусироваться.</p><h2>CONCEPT</h2><p><strong>Скилл</strong> — это переиспользуемый навык в виде папки с <code>SKILL.md</code> (frontmatter <code>name</code>/<code>description</code> + инструкция). Агент видит описание и подхватывает скилл, когда задача под него подходит.</p><pre><code>агент (Claude Code / Cursor / Codex)\n   └── .claude/skills/&lt;name&gt;/SKILL.md   ← навык: когда применять + как\n          /find-your-level      → входной квиз → персональный маршрут\n          /check-understanding  → квиз по фазе → что повторить</code></pre><p>В этом уроке мы и собираем два курсовых скилла как настоящие артефакты, и заодно понимаем механику скиллов изнутри (детально — Фаза 4).</p><h2>BUILD IT</h2><p>Два офлайн-движка без зависимостей:</p><ul><li>[<code>code/find_your_level.py</code>](../code/find_your_level.py) — входной квиз: 10 вопросов с весами → <code>score()</code> → <code>level()</code> (Новичок/Практик/Инженер) → <code>route()</code> строит план по 13 фазам с действием (focus/skim/skip) и оценкой часов.</li><li>[<code>code/check_understanding.py</code>](../code/check_understanding.py) — квиз по фазе: <code>run_quiz(phase, answers)</code> считает результат, даёт вердикт и список уроков на повтор (банк для Фазы 1 как образец).</li></ul><pre><code>python code/find_your_level.py\npython code/check_understanding.py\npytest code -q</code></pre><p>Новичок получает полные ~200 ч, инженер — ~178 ч (пропускает setup, листает базу). Это и есть «персональный маршрут».</p><h2>USE IT</h2><ol><li>Поставь агента: <strong>Claude Code</strong> (<code>npm i -g @anthropic-ai/claude-code</code>), Cursor или Codex.</li><li>Положи курсовые скиллы в <code>.claude/skills/</code> (они уже в репозитории) — агент подхватит их по описанию.</li><li>Запусти онбординг прямо в агенте:</li></ol><pre><code>/find-your-level            → ответь на 10 вопросов → получишь маршрут с часами\n/check-understanding 1       → после Фазы 1 → узнаешь, что повторить</code></pre><p>Скиллы кросс-платформенны: тот же <code>SKILL.md</code> понимают Claude Code, Cursor и Codex.</p><blockquote><strong>Врезка (2026): категории кодовых агентов.</strong> Claude Code — терминал и глубокий контекст; Cursor — IDE и инлайн-редактирование; Codex — облачный фоновый воркер; OpenClaw / Hermes — локальные персональные платформы (подробно в уроке 4.5). У каждого свой файл-инструкция проекта: <code>CLAUDE.md</code> (Claude Code), <code>AGENTS.md</code> (Codex), <code>.cursorrules</code> (Cursor) — но формат скиллов <code>SKILL.md</code> у них общий.</blockquote><h2>SHIP IT</h2><p><strong>Артефакт:</strong> Персональный маршрут с оценкой часов → [<code>outputs/personal-route.md</code>](../outputs/personal-route.md)</p><p>Плюс два рабочих скилла: [<code>.claude/skills/find-your-level</code>](../../../.claude/skills/find-your-level/SKILL.md) и [<code>.claude/skills/check-understanding</code>](../../../.claude/skills/check-understanding/SKILL.md). Маршрут — твой личный план прохождения курса; <code>/check-understanding</code> возвращается после ключевых фаз (1, 4, 8).</p><h2>Материалы</h2><ul><li><a href=\"https://code.claude.com/docs/en/overview\" target=\"_blank\" rel=\"noopener\">Claude Code — overview</a> — агент в терминале, IDE и десктопе.</li><li><a href=\"https://docs.claude.com/en/docs/agents-and-tools/agent-skills/overview\" target=\"_blank\" rel=\"noopener\">Agent Skills — overview</a> — что такое <code>SKILL.md</code> и как он подгружается.</li><li><a href=\"https://github.com/anthropics/skills\" target=\"_blank\" rel=\"noopener\">anthropics/skills</a> — публичные скиллы и <code>template-skill</code> для старта.</li></ul><hr><p><strong>Часы:</strong> ~2 · <strong>DoD:</strong> <code>pytest code -q</code> зелёный, демо запускается, ru.md заполнен. ✅ <strong>Урок готов</strong></p>"},{"id":"0.4","title":"Соглашения курса","status":"done","motto":"Единый формат урока — чтобы код, разбор и артефакт всегда лежали на своих местах.","hours":2,"dir":"phases/00-setup-and-tooling/00-course-conventions","html":"<h1>Урок 0.4 · Соглашения курса</h1><p><strong>Фаза 0 — Setup &amp; Tooling</strong> · <strong>Результат фазы:</strong> Настроить окружение, ключи и агента; осознанно выбрать модель под задачу.</p><blockquote><strong>MOTTO.</strong> Единый формат урока — чтобы код, разбор и артефакт всегда лежали на своих местах.</blockquote><h2>PROBLEM</h2><p>Когда у каждого урока своя раскладка, репозиторий превращается в свалку: непонятно, где код, где разбор, где готовый артефакт, а проверить «урок готов или нет» можно только глазами. На 52 урока это смертельно. Нужен <strong>общий контракт</strong> структуры и автоматическая проверка Definition of Done.</p><h2>CONCEPT</h2><p>Каждый урок — это тройка папок и цикл из 6 шагов:</p><pre><code>phases/&lt;NN&gt;-&lt;phase&gt;/&lt;NN&gt;-&lt;lesson&gt;/\n├── code/      Build It (с нуля) + Use It + тест test_*.py\n├── docs/ru.md разбор по 6 шагам: MOTTO·PROBLEM·CONCEPT·BUILD IT·USE IT·SHIP IT\n└── outputs/   артефакт: prompt · skill · agent · MCP</code></pre><p><strong>Definition of Done</strong> урока: <code>docs/ru.md</code> со всеми секциями, рабочий код с проходящим <code>test_*.py</code>, непустой <code>outputs/</code>. Это можно и нужно проверять программой.</p><h2>BUILD IT</h2><p>Scaffolder + валидатор урока с нуля, без зависимостей: [<code>code/conventions.py</code>](../code/conventions.py).</p><ul><li><code>scaffold_lesson(root, phase_dir, lesson_dir, id, title)</code> — создаёт дерево <code>code/docs/outputs</code> и стаб <code>docs/ru.md</code> со всеми 6 шагами;</li><li><code>validate_lesson(path)</code> — возвращает список нарушений DoD (нет секции, нет теста, пустой <code>outputs/</code>).</li></ul><pre><code>python code/conventions.py\npytest code -q</code></pre><p>Демо создаёт демо-урок во временной папке и тут же проверяет: свежий стаб «не готов» (нет кода и артефакта) — ровно как и должно быть до наполнения.</p><h2>USE IT</h2><p>Те же соглашения уже автоматизированы скиллом курса — <code>/lesson-author &lt;id&gt;</code> (см. [<code>.claude/skills/lesson-author</code>](../../../.claude/skills/lesson-author/SKILL.md)):</p><pre><code>/lesson-author 2.3     # находит урок в README, заводит папки, пишет стаб ru.md,\n                       # ведёт по Build It / Use It / Ship It и прогоняет pytest</code></pre><p>Цикл сдачи урока: <code>scaffold</code> → наполнить → <code>validate_lesson</code> зелёный → отметить <code>[x]</code> в <code>PROGRESS.md</code> → коммит <strong>после всей фазы</strong>.</p><h2>SHIP IT</h2><p><strong>Артефакт:</strong> Заготовка репозитория курса → [<code>outputs/course-kit/</code>](../outputs/course-kit/)</p><p>Мини-кит для старта своего курса/репозитория в этом формате: <code>conventions.py</code> (scaffolder + валидатор) и <code>CONVENTIONS.md</code> (контракт структуры и DoD). Тот же валидатор можно повесить в CI, чтобы «незаконченные» уроки не проходили проверку.</p><h2>Материалы</h2><ul><li><a href=\"https://docs.pytest.org/en/stable/getting-started.html\" target=\"_blank\" rel=\"noopener\">pytest — Get started</a> — как писать и запускать тесты урока.</li><li><a href=\"https://docs.claude.com/en/docs/agents-and-tools/agent-skills/overview\" target=\"_blank\" rel=\"noopener\">Agent Skills — overview</a> — механика скиллов, на которой стоит <code>/lesson-author</code>.</li><li><a href=\"https://github.com/stabuev/ai-engineering-from-scratch\" target=\"_blank\" rel=\"noopener\">ai-engineering-from-scratch</a> — образец каркаса «фазы → уроки → артефакты».</li></ul><hr><p><strong>Часы:</strong> ~2 · <strong>DoD:</strong> <code>pytest code -q</code> зелёный, демо запускается, ru.md заполнен. ✅ <strong>Урок готов</strong></p>"}],"hours":8,"outcome":"Настроить окружение, ключи и агента; осознанно выбрать модель под задачу."},{"id":1,"name":"Как работают LLM","lessons":[{"id":"1.1","title":"Токены и токенизация","status":"done","motto":"Модель видит не слова и не символы, а токены — и платишь ты тоже за токены.","hours":3,"dir":"phases/01-how-llms-work/01-tokens-and-tokenization","html":"<h1>Урок 1.1 · Токены и токенизация</h1><p><strong>Фаза 1 — Как работают LLM</strong> · <strong>Результат фазы:</strong> Объяснить токены, контекст и inference; собрать токен-бюджетер; отличать надёжный ответ от галлюцинации.</p><blockquote><strong>MOTTO.</strong> Модель видит не слова и не символы, а токены — и платишь ты тоже за токены.</blockquote><h2>PROBLEM</h2><p>«Сколько это будет стоить и влезет ли в контекст?» — на оба вопроса нельзя ответить, считая слова или символы. Модель работает с токенами, а их число не равно ни тому, ни другому: <code>tokenization</code> может быть 1 токеном, а <code>ทดสอบ</code> — десятком. Пока не понимаешь, откуда берутся токены, бюджет контекста и счёт за API остаются магией.</p><h2>CONCEPT</h2><p>Токенизация разбивает текст на куски по выученной статистике, а не по словарю языка. Самый наглядный механизм — <strong>BPE (byte-pair encoding)</strong>: начинаем с отдельных символов и итеративно сливаем самую частую соседнюю пару в один токен.</p><pre><code>\"low lower lowest\"\n l o w &lt;/w&gt;  l o w e r &lt;/w&gt;  l o w e s t &lt;/w&gt;\n        │ частая пара (l,o) → слияние\n lo w &lt;/w&gt;   lo w e r &lt;/w&gt;   lo w e s t &lt;/w&gt;\n        │ (lo,w) → слияние\n low &lt;/w&gt;    low e r &lt;/w&gt;    low e s t &lt;/w&gt;   ...</code></pre><p>Частые куски («low», «er») получают свой токен, редкие распадаются на мелкие части. Поэтому частотный текст «дешевле» по токенам, чем редкий.</p><h2>BUILD IT</h2><p>Реализован мини-BPE с нуля, без зависимостей: [<code>code/tokenizer.py</code>](../code/tokenizer.py).</p><ul><li><code>train_bpe(corpus, num_merges)</code> — учит merge-правила по частоте пар;</li><li><code>encode(text, merges)</code> / <code>decode(tokens)</code> — кодирование и обратный разбор.</li></ul><p>Запуск демо и тестов:</p><pre><code>python code/tokenizer.py\npytest code -q</code></pre><p>Демо показывает ключевую интуицию: чем больше выучено merge-правил, тем меньше токенов на тот же текст, а <code>decode(encode(x)) == x</code>.</p><h2>USE IT</h2><p>То же самое — через продакшн-токенизатор. &lt;!-- <strong>Requires:</strong> опциональная библиотека, без сети --&gt;</p><pre><code>pip install tiktoken</code></pre><pre><code>import tiktoken\nenc = tiktoken.get_encoding(\"o200k_base\")\ntext = \"tokenization is not word counting\"\nprint(len(enc.encode(text)), \"токенов\")     # реальный счётчик</code></pre><p>Сравни: наш учебный токенизатор и продакшн дают разное число токенов, но принцип один — слияние частых пар. Для оценки бюджета всегда считай реальным токенайзером модели.</p><h2>SHIP IT</h2><p><strong>Артефакт:</strong> <code>prompt-token-budgeter</code> → [<code>outputs/prompt-token-budgeter.md</code>](../outputs/prompt-token-budgeter.md)</p><p>Переиспользуемый «бюджетер»: оценивает число токенов и стоимость запроса по карте цен 2026 (вход/выход) до отправки в API. Используется во всех последующих фазах, особенно в Фазе 9 (FinOps).</p><h2>Материалы</h2><ul><li><a href=\"https://github.com/openai/tiktoken\" target=\"_blank\" rel=\"noopener\">openai/tiktoken</a> — продакшн-BPE-токенайзер (есть учебный submodule с визуализацией).</li><li><a href=\"https://github.com/karpathy/minbpe\" target=\"_blank\" rel=\"noopener\">karpathy/minbpe</a> — минимальная чистая реализация BPE для понимания механизма.</li><li><a href=\"https://arxiv.org/abs/1508.07909\" target=\"_blank\" rel=\"noopener\">Sennrich et al., 2016 — Subword Units (BPE)</a> — первоисточник BPE в NLP.</li></ul><hr><p><strong>Часы:</strong> ~3 · <strong>DoD:</strong> <code>pytest code -q</code> зелёный, демо запускается, ru.md заполнен. ✅ <strong>Урок готов</strong> (эталон уровня «теория»).</p>"},{"id":"1.2","title":"Контекстное окно и inference","status":"done","motto":"Inference — это не «один ответ», а цикл: предскажи токен, допиши, повтори.","hours":3,"dir":"phases/01-how-llms-work/01-context-window-and-inference","html":"<h1>Урок 1.2 · Контекстное окно и inference</h1><p><strong>Фаза 1 — Как работают LLM</strong> · <strong>Результат фазы:</strong> Объяснить токены, контекст и inference; собрать токен-бюджетер; отличать надёжный ответ от галлюцинации.</p><blockquote><strong>MOTTO.</strong> Inference — это не «один ответ», а цикл: предскажи токен, допиши, повтори.</blockquote><h2>PROBLEM</h2><p>Кажется, что модель «читает вопрос и пишет ответ» целиком. На самом деле она генерирует <strong>по одному токену за шаг</strong>, каждый раз заново глядя на весь контекст. Не понимая этого, нельзя объяснить, почему ответ приходит постепенно (стриминг), почему «забывается» начало длинного диалога и что вообще значит «контекст 200K токенов».</p><h2>CONCEPT</h2><p>Генерация — авторегрессивный цикл вокруг <strong>контекстного окна</strong> (сколько последних токенов модель видит):</p><pre><code>prompt ─► [ контекстное окно ] ─► P(next token) ─► argmax/sample ─► токен\n              ▲                                                       │\n              └──────────────── допишем токен в контекст ◄───────────┘\n                         (что вышло за окно — «исчезает»)</code></pre><p>Два следствия: <strong>(1)</strong> ответ строится пошагово — поэтому возможен стриминг; <strong>(2)</strong> окно конечно — выходит за него история, и модель её больше не «видит» (отсюда — бюджет контекста).</p><p><strong>Reasoning-модели</strong> (Claude extended thinking, DeepSeek V4 thinking-mode, o-серия) добавляют к этому циклу скрытый этап «размышления»: перед ответом генерируются *thinking-токены*, за которые ты тоже платишь. Это даёт качество на сложных задачах, но увеличивает и стоимость, и задержку — учитывай в бюджете (Фаза 9).</p><h2>BUILD IT</h2><p>Учебный inference-loop с контекстным окном, без зависимостей: [<code>code/inference_loop.py</code>](../code/inference_loop.py).</p><ul><li><code>train_ngram(corpus, order)</code> — игрушечная модель: n-граммы с backoff;</li><li><code>next_token_probs(model, context)</code> — распределение следующего токена по самому длинному совпавшему суффиксу;</li><li><code>generate(model, prompt, max_tokens, window)</code> — жадная авторегрессия; <code>window</code> ограничивает видимый контекст.</li></ul><pre><code>python code/inference_loop.py\npytest code -q</code></pre><p>Ключевая демонстрация: одинаковый prompt при разном <code>window</code> даёт <strong>разный</strong> результат — это и есть эффект контекстного окна.</p><h2>USE IT</h2><p>Тот же цикл — у реальной модели, но виден через <strong>стриминг</strong> (токены приходят по мере генерации). Мульти-провайдер:</p><pre><code># Anthropic\nfrom anthropic import Anthropic\nwith Anthropic().messages.stream(model=\"claude-opus-4-8\", max_tokens=200,\n    messages=[{\"role\": \"user\", \"content\": \"Объясни inference в 3 предложениях\"}]) as s:\n    for text in s.text_stream:\n        print(text, end=\"\")\n\n# OpenAI\nfrom openai import OpenAI\nfor ev in OpenAI().responses.create(model=\"gpt-5.x\",\n        input=\"Объясни inference в 3 предложениях\", stream=True):\n    if ev.type == \"response.output_text.delta\":\n        print(ev.delta, end=\"\")\n\n# Google\nfrom google import genai\nfor chunk in genai.Client().models.generate_content_stream(\n        model=\"gemini-2.x-flash\", contents=\"Объясни inference в 3 предложениях\"):\n    print(chunk.text, end=\"\")</code></pre><p>Каждый «delta» — это один (или несколько) только что сгенерированных токенов: тот самый цикл из Build It, но в проде.</p><h2>SHIP IT</h2><p><strong>Артефакт:</strong> Памятка по бюджету контекста → [<code>outputs/context-budget-cheatsheet.md</code>](../outputs/context-budget-cheatsheet.md)</p><p>Короткая шпаргалка: из чего складывается контекст (system + история + RAG + ответ), что обрезать первым, как не упереться в окно. Связка с <code>prompt-token-budgeter</code> из 1.1 и с Фазой 4 (контекст-инжиниринг).</p><h2>Материалы</h2><ul><li><a href=\"https://docs.anthropic.com/en/api/messages-streaming\" target=\"_blank\" rel=\"noopener\">Anthropic — Streaming Messages</a> — как устроен стриминг (SSE) у Claude.</li><li><a href=\"https://platform.openai.com/docs/guides/streaming-responses\" target=\"_blank\" rel=\"noopener\">OpenAI — Streaming responses</a> — стриминг в Responses API.</li><li><a href=\"https://ai.google.dev/gemini-api/docs/quickstart\" target=\"_blank\" rel=\"noopener\">Gemini — Quickstart</a> — первый вызов и стриминг у Google.</li></ul><hr><p><strong>Часы:</strong> ~3 · <strong>DoD:</strong> <code>pytest code -q</code> зелёный, демо запускается, ru.md заполнен. ✅ <strong>Урок готов</strong></p>"},{"id":"1.3","title":"Сэмплирование, температура, детерминизм","status":"done","motto":"Модель выдаёт распределение, а не ответ; температура и top-p решают, как из него выбрать.","hours":3,"dir":"phases/01-how-llms-work/01-sampling-temperature","html":"<h1>Урок 1.3 · Сэмплирование, температура, детерминизм</h1><p><strong>Фаза 1 — Как работают LLM</strong> · <strong>Результат фазы:</strong> Объяснить токены, контекст и inference; собрать токен-бюджетер; отличать надёжный ответ от галлюцинации.</p><blockquote><strong>MOTTO.</strong> Модель выдаёт распределение, а не ответ; температура и top-p решают, как из него выбрать.</blockquote><h2>PROBLEM</h2><p>«Почему один и тот же промпт даёт то одинаковые, то разные ответы?» и «как сделать вывод воспроизводимым?» — без понимания сэмплинга это магия. На каждом шаге модель отдаёт <strong>распределение вероятностей</strong> по токенам; то, насколько ответ детерминирован или креативен, определяют параметры выбора: <code>temperature</code>, <code>top_k</code>, <code>top_p</code>.</p><h2>CONCEPT</h2><pre><code>logits ──softmax(T)──► вероятности ──фильтр top-k / top-p──► выбор токена\n            │                              │\n   T→0: пик на argmax (детерминизм)   top-k: оставить k лучших\n   T↑ : распределение «плоское»       top-p: минимум токенов с суммой ≥ p (nucleus)</code></pre><ul><li><strong>temperature</strong> — «сглаживает» (↑) или «заостряет» (↓) распределение. <code>T=0</code> → всегда argmax → детерминизм.</li><li><strong>top-k</strong> — оставить k самых вероятных кандидатов, остальное отбросить.</li><li><strong>top-p (nucleus)</strong> — оставить минимальный набор, чья суммарная вероятность ≥ p (размер набора плавает).</li></ul><h2>BUILD IT</h2><p>Реализация softmax(T), top-k, top-p и сэмплера с нуля: [<code>code/sampling.py</code>](../code/sampling.py).</p><ul><li><code>softmax(logits, temperature)</code> — распределение; <code>T&lt;=0</code> → one-hot на argmax;</li><li><code>top_k_filter</code> / <code>top_p_filter</code> — отсечение хвоста + перенормировка;</li><li><code>sample(logits, temperature, top_k, top_p, rng)</code> — выбор индекса; с <code>rng=Random(seed)</code> воспроизводим.</li></ul><pre><code>python code/sampling.py\npytest code -q</code></pre><p>Демо показывает: с ростом <code>T</code> растёт энтропия распределения; <code>T=0</code> и <code>top_k=1</code> дают строго argmax; один и тот же seed → один и тот же сэмпл (детерминизм).</p><h2>USE IT</h2><p>Покрути параметры у реальных моделей и сравни разброс ответов (мульти-провайдер):</p><pre><code># OpenAI — temperature 0..2\nOpenAI().responses.create(model=\"gpt-5.x\", input=\"Придумай слоган\", temperature=0.2)\n# Google — temperature/top_p в generation_config\ngenai.Client().models.generate_content(model=\"gemini-2.x-flash\", contents=\"Придумай слоган\",\n    config={\"temperature\": 0.9, \"top_p\": 0.95})</code></pre><p>⚠️ <strong>Важно (2026):</strong> у новейших Claude (<strong>Opus 4.7+ / 4.8</strong>) параметры <code>temperature</code>/<code>top_p</code>/<code>top_k</code> <strong>не поддерживаются</strong> — их передача вернёт 400, поведение задаётся промптом. Для экспериментов с сэмплингом бери модели, где они открыты (OpenAI: 0–2; Gemini; более ранние Claude — причём <code>temperature</code> и <code>top_p</code> меняют <strong>по одному</strong>, не вместе).</p><h2>SHIP IT</h2><p><strong>Артефакт:</strong> Гайд по параметрам генерации → [<code>outputs/generation-params-guide.md</code>](../outputs/generation-params-guide.md)</p><p>Шпаргалка «какой параметр под какую задачу»: когда <code>T=0</code> (извлечение, классификация, воспроизводимость), когда выше (черновики, брейншторм), как сочетать с top-p и где они вообще доступны.</p><h2>Материалы</h2><ul><li><a href=\"https://arxiv.org/abs/1904.09751\" target=\"_blank\" rel=\"noopener\">Holtzman et al., 2019 — Nucleus Sampling (top-p)</a> — первоисточник top-p и почему argmax деградирует.</li><li><a href=\"https://docs.anthropic.com/en/api/messages\" target=\"_blank\" rel=\"noopener\">Anthropic — Messages API</a> — параметры запроса и ограничения по моделям.</li><li><a href=\"https://help.openai.com/en/articles/6654000-best-practices-for-prompt-engineering-with-the-openai-api\" target=\"_blank\" rel=\"noopener\">OpenAI — Prompt engineering best practices</a> — про temperature и выбор параметров.</li></ul><hr><p><strong>Часы:</strong> ~3 · <strong>DoD:</strong> <code>pytest code -q</code> зелёный, демо запускается, ru.md заполнен. ✅ <strong>Урок готов</strong></p>"},{"id":"1.4","title":"Галлюцинации и границы доверия","status":"done","motto":"Модель всегда говорит уверенно — доверие нужно проверять, а не предполагать.","hours":3,"dir":"phases/01-how-llms-work/01-hallucinations-and-trust","html":"<h1>Урок 1.4 · Галлюцинации и границы доверия</h1><p><strong>Фаза 1 — Как работают LLM</strong> · <strong>Результат фазы:</strong> Объяснить токены, контекст и inference; собрать токен-бюджетер; отличать надёжный ответ от галлюцинации.</p><blockquote><strong>MOTTO.</strong> Модель всегда говорит уверенно — доверие нужно проверять, а не предполагать.</blockquote><h2>PROBLEM</h2><p>LLM по умолчанию старается быть полезной и <strong>отвечает даже там, где не знает</strong>: придумывает факты, принимает ложные предпосылки, «угадывает» неизвестное. Звучит это так же уверенно, как и правда. Без понимания, где проходит граница доверия, легко принять галлюцинацию за факт — и встроить её в работу.</p><h2>CONCEPT</h2><p>Галлюцинация — не «сбой», а следствие механизма: модель предсказывает правдоподобный следующий токен, а не истину. Типовые провокаторы:</p><pre><code>неотвечаемое (выдуманная сущность, будущее)  → ждём «не знаю»\nложная предпосылка («почему X из золота?»)   → ждём «оспорить предпосылку»\nнормальный вопрос                            → ждём ответ по делу</code></pre><p>Защита — не «верить/не верить», а <strong>проверяемое поведение</strong>: дать право сказать «не знаю», требовать цитат/источников, сверять ответы между прогонами и моделями.</p><h2>BUILD IT</h2><p>Тест-набор проб + эвристический оценщик доверия, без зависимостей: [<code>code/hallucination_probes.py</code>](../code/hallucination_probes.py).</p><ul><li><code>PROBES</code> — пробы трёх типов: <code>unanswerable</code>, <code>false_premise</code>, <code>answerable</code>;</li><li><code>evaluate_response(probe, response)</code> — по маркерам проверяет, корректно ли поведение (воздержался / оспорил / ответил);</li><li><code>trust_score(results)</code> — доля корректных реакций.</li></ul><pre><code>python code/hallucination_probes.py\npytest code -q</code></pre><p>Оценщик намеренно простой (ищет маркеры, а не «понимает» текст) — он задаёт <strong>что именно проверять</strong>. Это нижняя граница, а не замена человеку.</p><h2>USE IT</h2><p>Прогони один и тот же набор <code>PROBES</code> через разные модели и версии и сравни <code>trust_score</code> (мульти-провайдер):</p><pre><code>def ask_claude(p):   # Anthropic\n    return Anthropic().messages.create(model=\"claude-opus-4-8\", max_tokens=300,\n        messages=[{\"role\": \"user\", \"content\": p[\"prompt\"]}]).content[0].text\ndef ask_openai(p):   # OpenAI\n    return OpenAI().responses.create(model=\"gpt-5.x\", input=p[\"prompt\"]).output_text\ndef ask_gemini(p):   # Google\n    return genai.Client().models.generate_content(model=\"gemini-2.x-flash\", contents=p[\"prompt\"]).text\n\nfor ask in (ask_claude, ask_openai, ask_gemini):\n    results = [evaluate_response(p, ask(p)) for p in PROBES]\n    print(ask.__name__, \"trust_score:\", trust_score(results))</code></pre><p>Так видно: модели по-разному держат границу доверия; «допиши: можешь сказать ‘не знаю’» в промпте заметно поднимает score.</p><h2>SHIP IT</h2><p><strong>Артефакт:</strong> Чек-лист доверия к ответу → [<code>outputs/trust-checklist.md</code>](../outputs/trust-checklist.md)</p><p>Практический чек-лист: когда требовать цитат, как провоцировать «не знаю», что перепроверять у человека. Возвращается в Фазе 11 (фактчекинг) и в любых рабочих пайплайнах.</p><h2>Материалы</h2><ul><li><a href=\"https://docs.claude.com/en/docs/test-and-evaluate/strengthen-guardrails/reduce-hallucinations\" target=\"_blank\" rel=\"noopener\">Anthropic — Reduce hallucinations</a> — приёмы: право на «не знаю», цитаты, низкая температура.</li><li><a href=\"https://github.com/anthropics/courses/blob/master/prompt_engineering_interactive_tutorial/Anthropic%201P/08_Avoiding_Hallucinations.ipynb\" target=\"_blank\" rel=\"noopener\">anthropics/courses — Avoiding Hallucinations</a> — интерактивный разбор на примерах.</li><li><a href=\"https://arxiv.org/abs/2202.03629\" target=\"_blank\" rel=\"noopener\">Ji et al., 2022 — Survey of Hallucination in NLG</a> — обзор: типы галлюцинаций (intrinsic/extrinsic) и причины.</li></ul><hr><p><strong>Часы:</strong> ~3 · <strong>DoD:</strong> <code>pytest code -q</code> зелёный, демо запускается, ru.md заполнен. ✅ <strong>Урок готов</strong></p>"}],"hours":12,"outcome":"Объяснить токены, контекст и inference; собрать токен-бюджетер; отличать надёжный ответ от галлюцинации."},{"id":2,"name":"Промпт-инжиниринг","lessons":[{"id":"2.1","title":"Анатомия промпта","status":"done","motto":"Промпт — это интерфейс к интеллекту; у хорошего интерфейса есть структура.","hours":3,"dir":"phases/02-prompt-engineering/02-prompt-anatomy","html":"<h1>Урок 2.1 · Анатомия промпта</h1><p><strong>Фаза 2 — Промпт-инжиниринг</strong> · <strong>Результат фазы:</strong> Писать воспроизводимые промпты и измерять их качество через eval-harness.</p><blockquote><strong>MOTTO.</strong> Промпт — это интерфейс к интеллекту; у хорошего интерфейса есть структура.</blockquote><h2>PROBLEM</h2><p>«Напиши покороче» работает случайно: сегодня ок, завтра мимо. Без структуры промпт невоспроизводим — нельзя понять, что именно повлияло на ответ, и нельзя переиспользовать находку. Нужна <strong>анатомия</strong>: разложить промпт на части, которыми можно управлять.</p><h2>CONCEPT</h2><p>У промпта пять управляемых блоков:</p><pre><code>[ Роль ]      кто отвечает (эксперт, редактор, парсер)\n[ Контекст ]  что нужно знать для задачи\n[ Задача ]    что именно сделать  ← единственный обязательный блок\n[ Формат ]    как должен выглядеть ответ\n[ Примеры ]   few-shot образцы вход→выход (урок 2.2)</code></pre><p>Чем явнее блоки, тем стабильнее ответ и тем легче его измерять (мост к 2.5). Структуру удобно держать как шаблон с подстановками.</p><h2>BUILD IT</h2><p>Сборщик промпта из блоков + линтер на полноту: [<code>code/prompt_template.py</code>](../code/prompt_template.py).</p><ul><li><code>build_prompt(task, role=…, context=…, output_format=…, examples=…)</code> — собирает промпт; обязателен только <code>task</code>;</li><li><code>lint_prompt(text)</code> — какие анатомические блоки отсутствуют.</li></ul><pre><code>python code/prompt_template.py\npytest code -q</code></pre><p>Линтер делает структуру проверяемой: «голый» промпт сигналит о недостающих ролях/формате — это и есть путь к воспроизводимости.</p><h2>USE IT</h2><p>Один и тот же структурированный промпт — на трёх платформах; сравни, как меняется ответ:</p><pre><code>prompt = build_prompt(task=\"Суммаризируй отзыв в 3 пунктах\",\n                      role=\"Ты редактор поддержки\", output_format=\"Маркированный список\")\n# Anthropic\nAnthropic().messages.create(model=\"claude-sonnet-4-6\", max_tokens=300,\n    system=\"Ты редактор поддержки\", messages=[{\"role\": \"user\", \"content\": prompt}])\n# OpenAI\nOpenAI().responses.create(model=\"gpt-5.x\", instructions=\"Ты редактор поддержки\", input=prompt)\n# Google\ngenai.Client().models.generate_content(model=\"gemini-2.x-flash\", contents=prompt)</code></pre><p>Заметь: «роль» у провайдеров живёт в разных местах (system / instructions / в тексте) — анатомия одна, синтаксис разный.</p><h2>SHIP IT</h2><p><strong>Артефакт:</strong> Базовый шаблон промпта → [<code>outputs/prompt-template.md</code>](../outputs/prompt-template.md)</p><p>Переиспользуемый каркас «роль · контекст · задача · формат · примеры» с подстановками. Дальше: примеры → урок 2.2, измерение версий → урок 2.5.</p><h2>Материалы</h2><ul><li><a href=\"https://docs.anthropic.com/en/docs/build-with-claude/prompt-engineering/overview\" target=\"_blank\" rel=\"noopener\">Anthropic — Prompt engineering overview</a> — с чего начать и какие техники есть.</li><li><a href=\"https://docs.anthropic.com/en/docs/build-with-claude/prompt-engineering/claude-4-best-practices\" target=\"_blank\" rel=\"noopener\">Anthropic — Prompting best practices</a> — живой справочник приёмов.</li><li><a href=\"https://github.com/anthropics/prompt-eng-interactive-tutorial\" target=\"_blank\" rel=\"noopener\">anthropics/prompt-eng-interactive-tutorial</a> — интерактивный курс по промптингу.</li></ul><hr><p><strong>Часы:</strong> ~3 · <strong>DoD:</strong> <code>pytest code -q</code> зелёный, демо запускается, ru.md заполнен. ✅ <strong>Урок готов</strong></p>"},{"id":"2.2","title":"Few-shot и chain-of-thought","status":"done","motto":"Покажи примеры — и модель поймёт формат; попроси рассуждать — и она решит сложное.","hours":3,"dir":"phases/02-prompt-engineering/02-few-shot-and-cot","html":"<h1>Урок 2.2 · Few-shot и chain-of-thought</h1><p><strong>Фаза 2 — Промпт-инжиниринг</strong> · <strong>Результат фазы:</strong> Писать воспроизводимые промпты и измерять их качество через eval-harness.</p><blockquote><strong>MOTTO.</strong> Покажи примеры — и модель поймёт формат; попроси рассуждать — и она решит сложное.</blockquote><h2>PROBLEM</h2><p>На задачах с нестандартным форматом или многошаговым рассуждением «просто спросить» даёт сбои: модель угадывает разметку, путает классы, прыгает к неверному ответу. Две дешёвые техники резко поднимают качество без дообучения — <strong>few-shot</strong> (примеры в промпте) и <strong>chain-of-thought</strong> (пошаговое рассуждение).</p><h2>CONCEPT</h2><pre><code>zero-shot : задача → ответ                         (модель угадывает формат)\nfew-shot  : пример,пример,пример → задача → ответ   (учится по образцам)\nCoT       : задача → «думай по шагам» → шаги → ответ (разворачивает рассуждение)</code></pre><ul><li><strong>Few-shot</strong> задаёт формат и семантику метки примерами — особенно помогает классификации и разметке.</li><li><strong>CoT</strong> даёт модели «развернуть» промежуточные шаги — помогает арифметике и логике.</li></ul><h2>BUILD IT</h2><p>Сборщики few-shot/CoT + демонстрация ценности примеров: [<code>code/few_shot.py</code>](../code/few_shot.py).</p><ul><li><code>format_few_shot(instruction, examples, query)</code> и <code>format_cot(instruction, query)</code> — построение промптов;</li><li><code>knn_predict</code> (few-shot аналог) против <code>zero_shot_predict</code> (без примеров) на игрушечном датасете → измеримая разница в <code>accuracy</code>.</li></ul><pre><code>python code/few_shot.py\npytest code -q</code></pre><p>Демо численно показывает то, ради чего урок: few-shot даёт точность <strong>1.0</strong> против <strong>0.5</strong> у zero-shot на тех же данных.</p><h2>USE IT</h2><p>Сравни точность с/без примеров на реальной модели (мульти-провайдер): прогони набор кейсов в трёх режимах — zero-shot, few-shot, CoT — и сравни долю верных.</p><pre><code>zero = \"Классифицируй тональность: 'ужасный сервис'\"\nfew  = format_few_shot(\"Классифицируй тональность.\", EXAMPLES, \"ужасный сервис\")\ncot  = format_cot(\"Классифицируй тональность.\", \"ужасный сервис\")\nfor prompt in (zero, few, cot):\n    Anthropic().messages.create(model=\"claude-haiku-4-5\", max_tokens=100,\n        messages=[{\"role\": \"user\", \"content\": prompt}])\n    OpenAI().responses.create(model=\"gpt-5.x\", input=prompt)\n    genai.Client().models.generate_content(model=\"gemini-2.x-flash\", contents=prompt)</code></pre><p>Измерять разницу будем системно в уроке 2.5 (eval-harness).</p><h2>SHIP IT</h2><p><strong>Артефакт:</strong> Набор few-shot заготовок → [<code>outputs/few-shot-templates.md</code>](../outputs/few-shot-templates.md)</p><p>Готовые шаблоны few-shot и CoT под типовые задачи (классификация, извлечение, рассуждение) — подставляешь свои примеры.</p><h2>Материалы</h2><ul><li><a href=\"https://arxiv.org/abs/2005.14165\" target=\"_blank\" rel=\"noopener\">Brown et al., 2020 — Language Models are Few-Shot Learners (GPT-3)</a> — откуда взялся few-shot.</li><li><a href=\"https://arxiv.org/abs/2201.11903\" target=\"_blank\" rel=\"noopener\">Wei et al., 2022 — Chain-of-Thought Prompting</a> — первоисточник CoT.</li><li><a href=\"https://github.com/anthropics/prompt-eng-interactive-tutorial\" target=\"_blank\" rel=\"noopener\">anthropics/prompt-eng-interactive-tutorial</a> — практические главы про примеры и пошаговое мышление.</li></ul><hr><p><strong>Часы:</strong> ~3 · <strong>DoD:</strong> <code>pytest code -q</code> зелёный, демо запускается, ru.md заполнен. ✅ <strong>Урок готов</strong></p>"},{"id":"2.3","title":"Декомпозиция и итеративное уточнение","status":"done","motto":"Сложную задачу не «промптят» одним мегапромптом, а разбивают на цепочку шагов.","hours":3,"dir":"phases/02-prompt-engineering/02-decomposition-and-iteration","html":"<h1>Урок 2.3 · Декомпозиция и итеративное уточнение</h1><p><strong>Фаза 2 — Промпт-инжиниринг</strong> · <strong>Результат фазы:</strong> Писать воспроизводимые промпты и измерять их качество через eval-harness.</p><blockquote><strong>MOTTO.</strong> Сложную задачу не «промптят» одним мегапромптом, а разбивают на цепочку шагов.</blockquote><h2>PROBLEM</h2><p>Один огромный промпт «сделай всё сразу» ненадёжен: модель роняет шаги, путает порядок, а отладить «почему плохо» невозможно — всё слиплось. Сложные процессы (исследование → черновик → правка → формат) требуют <strong>декомпозиции</strong>: цепочки промптов, где выход одного шага — вход следующего.</p><h2>CONCEPT</h2><pre><code>вход → [шаг 1] → [шаг 2] → [шаг 3] → итог\n          │ каждый шаг = отдельный промпт с одной целью\n          ▼ выход шага виден и проверяем (трейс)\n   типовые цепочки: Extract → Transform → Analyze → Format\n                    Generate → Review → Improve  (self-correction)</code></pre><p>Плюсы: <strong>точность</strong> (каждый шаг — в фокусе), <strong>ясность</strong> (простые инструкции), <strong>трассируемость</strong> (видно, где сломалось). Это прямой мост к агентам (Фаза 7).</p><h2>BUILD IT</h2><p>Раннер цепочки промптов с трейсом: [<code>code/prompt_chain.py</code>](../code/prompt_chain.py).</p><ul><li><code>ChainStep(name, fn)</code> — шаг цепочки;</li><li><code>run_chain(steps, initial, trace)</code> — прогоняет значение через шаги, возвращает <code>(итог, история)</code>.</li></ul><pre><code>python code/prompt_chain.py\npytest code -q</code></pre><p>Шаги здесь — детерминированные функции (офлайн); цепочка <code>extract → sum → format</code> решает то, что один шаг не может. Виден трейс каждого перехода — это и есть отлаживаемость.</p><h2>USE IT</h2><p>В проде каждый <code>ChainStep.fn</code> — отдельный вызов модели; выход подаётся в следующий промпт:</p><pre><code>def step_llm(instruction):\n    def fn(value):\n        prompt = f\"{instruction}\\n\\nВход:\\n{value}\"\n        return Anthropic().messages.create(model=\"claude-sonnet-4-6\", max_tokens=500,\n            messages=[{\"role\": \"user\", \"content\": prompt}]).content[0].text\n        # OpenAI: OpenAI().responses.create(model=\"gpt-5.x\", input=prompt).output_text\n        # Google: genai.Client().models.generate_content(model=\"gemini-2.x-flash\", contents=prompt).text\n    return fn\n\nchain = [ChainStep(\"outline\", step_llm(\"Составь план\")),\n         ChainStep(\"draft\",   step_llm(\"Напиши черновик по плану\")),\n         ChainStep(\"edit\",    step_llm(\"Отредактируй: короче и яснее\"))]</code></pre><p>Независимые шаги можно гонять параллельно; последовательно — только когда есть зависимость.</p><h2>SHIP IT</h2><p><strong>Артефакт:</strong> Промпт-цепочка под рабочий процесс → [<code>outputs/prompt-chain.md</code>](../outputs/prompt-chain.md)</p><p>Шаблон цепочки (этапы, что подаётся между шагами, где точки проверки) под твой реальный процесс. Дальше: агентный цикл reason→act→observe — урок 7.1.</p><h2>Материалы</h2><ul><li><a href=\"https://docs.anthropic.com/en/docs/build-with-claude/prompt-engineering/chain-prompts\" target=\"_blank\" rel=\"noopener\">Anthropic — Chain complex prompts</a> — когда и как дробить задачу на цепочку.</li><li><a href=\"https://arxiv.org/abs/2201.11903\" target=\"_blank\" rel=\"noopener\">Wei et al., 2022 — Chain-of-Thought</a> — пошаговое рассуждение как основа декомпозиции.</li><li><a href=\"https://docs.anthropic.com/en/docs/build-with-claude/prompt-engineering/claude-4-best-practices\" target=\"_blank\" rel=\"noopener\">Anthropic — Prompting best practices</a> — паттерн self-correction (generate → review → improve).</li></ul><hr><p><strong>Часы:</strong> ~3 · <strong>DoD:</strong> <code>pytest code -q</code> зелёный, демо запускается, ru.md заполнен. ✅ <strong>Урок готов</strong></p>"},{"id":"2.4","title":"Системные промпты и структурированный вывод","status":"done","motto":"Если ответ парсит программа — он должен быть схемой, а не прозой.","hours":3,"dir":"phases/02-prompt-engineering/02-system-prompts-structured-output","html":"<h1>Урок 2.4 · Системные промпты и структурированный вывод</h1><p><strong>Фаза 2 — Промпт-инжиниринг</strong> · <strong>Результат фазы:</strong> Писать воспроизводимые промпты и измерять их качество через eval-harness.</p><blockquote><strong>MOTTO.</strong> Если ответ парсит программа — он должен быть схемой, а не прозой.</blockquote><h2>PROBLEM</h2><p>Когда вывод модели идёт в код (БД, API, UI), свободный текст — катастрофа: то лишние пояснения, то поле пропало, то число пришло строкой. «Верни JSON» в промпте не гарантирует валидную структуру. Нужны <strong>системный промпт</strong>, задающий формат, и <strong>валидатор</strong>, который проверяет соответствие схеме на нашей стороне.</p><h2>CONCEPT</h2><pre><code>system prompt (роль + строгий формат)\n        │\n        ▼\nответ модели ──extract_json──► объект ──validate_schema──► ✓ / список ошибок\n   (часто в ```json … ```)        (типы, обязательные поля)</code></pre><p>Два уровня надёжности: <strong>(1)</strong> провайдерский structured output / function calling заставляет модель держать схему; <strong>(2)</strong> свой валидатор — страховка, что пришедшее реально ей соответствует (доверяй, но проверяй).</p><h2>BUILD IT</h2><p>Извлечение JSON + валидатор мини-схемы: [<code>code/structured_output.py</code>](../code/structured_output.py).</p><ul><li><code>extract_json(text)</code> — снять <code></code><code>json-ограждение и распарсить;</code></li><li><code>validate_schema(obj, schema)</code> — проверить типы и обязательные поля (boolean не считается integer);</li><li><code>SYSTEM_PROMPT</code> + <code>SCHEMA</code> — пример под извлечение тикета.</li></ul><pre><code>python code/structured_output.py\npytest code -q</code></pre><p>Валидатор ловит то, что молча ломает прод: пропущенное обязательное поле, <code>priority: true</code> вместо числа, не-JSON в ответе.</p><h2>USE IT</h2><p>Проси у провайдера гарантированную структуру (мульти-провайдер):</p><pre><code>schema = {\"type\": \"object\", \"properties\": {\n    \"title\": {\"type\": \"string\"}, \"priority\": {\"type\": \"integer\"}},\n    \"required\": [\"title\", \"priority\"], \"additionalProperties\": False}\n\n# OpenAI — Structured Outputs (response_format / text.format = json_schema)\nOpenAI().responses.create(model=\"gpt-5.x\", input=\"...\",\n    text={\"format\": {\"type\": \"json_schema\", \"name\": \"ticket\", \"schema\": schema}})\n# Google — responseSchema в config\ngenai.Client().models.generate_content(model=\"gemini-2.x-flash\", contents=\"...\",\n    config={\"response_mime_type\": \"application/json\", \"response_schema\": schema})\n# Anthropic — через tool use (function calling): описываешь input_schema инструмента</code></pre><p>Даже с гарантией провайдера прогоняй ответ через свой <code>validate_schema</code> — это дешёвая страховка.</p><h2>SHIP IT</h2><p><strong>Артефакт:</strong> Системный промпт + JSON-схема → [<code>outputs/system-prompt-and-schema.md</code>](../outputs/system-prompt-and-schema.md)</p><p>Готовая пара «system prompt + схема + валидатор» под извлечение структуры из текста. Возвращается в Фазе 5 (NL→SQL, отчёты) и Фазе 6 (инструменты MCP).</p><h2>Материалы</h2><ul><li><a href=\"https://platform.openai.com/docs/guides/structured-outputs\" target=\"_blank\" rel=\"noopener\">OpenAI — Structured model outputs</a> — гарантированное соответствие JSON-схеме.</li><li><a href=\"https://openai.com/index/introducing-structured-outputs-in-the-api/\" target=\"_blank\" rel=\"noopener\">OpenAI — Introducing Structured Outputs</a> — зачем и чем отличается от JSON mode.</li><li><a href=\"https://json-schema.org/\" target=\"_blank\" rel=\"noopener\">JSON Schema</a> — стандарт описания схем (типы, required, additionalProperties).</li></ul><hr><p><strong>Часы:</strong> ~3 · <strong>DoD:</strong> <code>pytest code -q</code> зелёный, демо запускается, ru.md заполнен. ✅ <strong>Урок готов</strong></p>"},{"id":"2.5","title":"Оценка качества промптов","status":"done","motto":"«Кажется, лучше» — не метрика. Промпт улучшают, измеряя на наборе кейсов.","hours":3,"dir":"phases/02-prompt-engineering/02-prompt-evaluation","html":"<h1>Урок 2.5 · Оценка качества промптов</h1><p><strong>Фаза 2 — Промпт-инжиниринг</strong> · <strong>Результат фазы:</strong> Писать воспроизводимые промпты и измерять их качество через eval-harness.</p><blockquote><strong>MOTTO.</strong> «Кажется, лучше» — не метрика. Промпт улучшают, измеряя на наборе кейсов.</blockquote><h2>PROBLEM</h2><p>Промпты правят «на глаз»: поменял формулировку, ответ на одном примере стал красивее — закоммитил. А на десяти других стало хуже, и ты этого не видишь. Без <strong>eval-harness</strong> нельзя ни сравнить две версии промпта честно, ни понять, что новая модель/параметр реально лучше. Это венец фазы: промпт-инжиниринг становится наукой, а не гаданием.</p><h2>CONCEPT</h2><pre><code>набор кейсов {input, expected}\n        │\n   prompt_fn (версия промпта / модель)\n        │\n     метрика(pred, expected) → score за кейс\n        │\n   усреднили → score версии → compare(версии) → рейтинг</code></pre><p>Метрика выбирается под задачу: <code>exact_match</code> (классификация, извлечение), <code>contains</code> (есть ли нужное), LLM-as-judge (качество текста — позже). Главное — фиксированный набор кейсов и автоматическая оценка.</p><h2>BUILD IT</h2><p>Mini eval-harness с метриками и сравнением версий: [<code>code/eval_harness.py</code>](../code/eval_harness.py).</p><ul><li><code>exact_match</code>, <code>contains</code> — метрики <code>(pred, expected) → [0..1]</code>;</li><li><code>evaluate(prompt_fn, cases, metric)</code> — прогон по кейсам → <code>{score, per_case}</code>;</li><li><code>compare(versions, cases, metric)</code> — рейтинг версий по убыванию качества.</li></ul><pre><code>python code/eval_harness.py\npytest code -q</code></pre><p>Демо честно ранжирует «версии промпта»: рабочая → 1.0, угадайка → 0.0; <code>compare</code> ставит лучшую первой. Это и есть инструмент выбора.</p><h2>USE IT</h2><p>В проде <code>prompt_fn</code> — вызов модели; harness прогоняет твой набор кейсов и сравнивает версии/модели (мульти-провайдер):</p><pre><code>def v_claude(x): return Anthropic().messages.create(model=\"claude-haiku-4-5\",\n    max_tokens=50, messages=[{\"role\": \"user\", \"content\": PROMPT.format(x=x)}]).content[0].text\ndef v_openai(x): return OpenAI().responses.create(model=\"gpt-5.x\", input=PROMPT.format(x=x)).output_text\ndef v_gemini(x): return genai.Client().models.generate_content(\n    model=\"gemini-2.x-flash\", contents=PROMPT.format(x=x)).text\n\nprint(compare({\"claude\": v_claude, \"openai\": v_openai, \"gemini\": v_gemini}, CASES, metric=contains))</code></pre><p>Так выбор модели и версии промпта становится измеримым (мост к Фазе 9 — цена против качества).</p><h2>SHIP IT</h2><p><strong>Артефакт:</strong> <code>prompt-eval</code> (skill) → [<code>outputs/prompt-eval/</code>](../outputs/prompt-eval/SKILL.md)</p><p>Переиспользуемый скилл оценки промптов: подкладываешь кейсы и версии — получаешь рейтинг. Зарегистрирован в [<code>.claude/skills/prompt-eval</code>](../../../.claude/skills/prompt-eval/SKILL.md). Это рабочая лошадка для всех последующих фаз, где надо доказать, что «стало лучше».</p><h2>Материалы</h2><ul><li><a href=\"https://docs.anthropic.com/en/docs/build-with-claude/develop-tests\" target=\"_blank\" rel=\"noopener\">Anthropic — Create strong empirical evaluations</a> — как строить eval-наборы и грейдинг.</li><li><a href=\"https://docs.anthropic.com/en/docs/build-with-claude/define-success\" target=\"_blank\" rel=\"noopener\">Anthropic — Define your success criteria</a> — что именно измерять.</li><li><a href=\"https://github.com/anthropics/anthropic-cookbook/blob/main/misc/building_evals.ipynb\" target=\"_blank\" rel=\"noopener\">anthropics/anthropic-cookbook — building_evals</a> — практический ноутбук по evals.</li></ul><hr><p><strong>Часы:</strong> ~3 · <strong>DoD:</strong> <code>pytest code -q</code> зелёный, демо запускается, ru.md заполнен. ✅ <strong>Урок готов</strong></p>"}],"hours":16,"outcome":"Писать воспроизводимые промпты и измерять их качество через eval-harness."},{"id":3,"name":"Текст и документы","lessons":[{"id":"3.1","title":"Резюмирование, редактура, тон","status":"done","motto":"Текст обрабатывают конвейером: сжать → почистить → задать тон.","hours":4,"dir":"phases/03-text-and-documents/03-summarization-editing-tone","html":"<h1>Урок 3.1 · Резюмирование, редактура, тон</h1><p><strong>Фаза 3 — Текст и документы</strong> · <strong>Результат фазы:</strong> Строить пайплайны резюмирования и редактуры; собрать базовый RAG над своим корпусом.</p><blockquote><strong>MOTTO.</strong> Текст обрабатывают конвейером: сжать → почистить → задать тон.</blockquote><h2>PROBLEM</h2><p>Большая часть работы — это текст: длинные письма, отчёты, переписка. «Сделай покороче и поприличнее» одним махом ненадёжно: то смысл потеряется, то тон не тот. Нужен <strong>конвейер</strong> с раздельными стадиями, которые можно настраивать и проверять по отдельности.</p><h2>CONCEPT</h2><pre><code>сырой текст → [черновик/резюме] → [редактура] → [тон] → готово\n                 что важно            убрать лишнее   под аудиторию</code></pre><p>Разделение стадий = контроль: каждую можно менять и оценивать независимо (мост к eval, урок 2.5). Это тот же приём декомпозиции из 2.3, но для текста.</p><h2>BUILD IT</h2><p>Текстовый пайплайн «черновик → редактура → тон», без зависимостей: [<code>code/editing_pipeline.py</code>](../code/editing_pipeline.py).</p><ul><li><code>summarize_extractive(text, n)</code> — extractive-резюме по частоте значимых слов;</li><li><code>tighten(text)</code> — убрать слова-филлеры и лишние пробелы;</li><li><code>set_tone(text, tone)</code> — заменить маркеры тона (formal/casual);</li><li><code>run_pipeline(text, stages)</code> — собрать стадии в конвейер.</li></ul><pre><code>python code/editing_pipeline.py\npytest code -q</code></pre><p>Стадии офлайн и детерминированы; в проде каждую заменяет вызов модели, но контракт «текст → текст» тот же.</p><h2>USE IT</h2><p>Те же стадии — промптами к модели на реальных документах/переписке (мульти-провайдер):</p><pre><code>def stage(instruction, text, model=\"claude-sonnet-4-6\"):\n    p = f\"{instruction}\\n\\nТекст:\\n{text}\"\n    return Anthropic().messages.create(model=model, max_tokens=600,\n        messages=[{\"role\": \"user\", \"content\": p}]).content[0].text\n    # OpenAI:  OpenAI().responses.create(model=\"gpt-5.x\", input=p).output_text\n    # Google:  genai.Client().models.generate_content(model=\"gemini-2.x-flash\", contents=p).text\n\ndraft = stage(\"Сожми в 3 пункта\", document)\nedited = stage(\"Сделай короче и яснее, убери воду\", draft)\nfinal = stage(\"Перепиши в деловом тоне\", edited)</code></pre><h2>SHIP IT</h2><p><strong>Артефакт:</strong> Редакторский skill → [<code>outputs/editor/</code>](../outputs/editor/SKILL.md)</p><p>Скилл <code>editor</code> (черновик → редактура → тон) с настраиваемыми стадиями; зарегистрирован в [<code>.claude/skills/editor</code>](../../../.claude/skills/editor/SKILL.md). Дальше масштабируем на длинные документы — урок 3.2.</p><h2>Материалы</h2><ul><li><a href=\"https://docs.anthropic.com/en/docs/build-with-claude/prompt-engineering/claude-4-best-practices\" target=\"_blank\" rel=\"noopener\">Anthropic — Prompting best practices</a> — приёмы для качественных текстовых задач.</li><li><a href=\"https://docs.anthropic.com/en/docs/build-with-claude/prompt-engineering/overview\" target=\"_blank\" rel=\"noopener\">Anthropic — Prompt engineering overview</a> — обзор техник.</li><li><a href=\"https://github.com/anthropics/prompt-eng-interactive-tutorial\" target=\"_blank\" rel=\"noopener\">anthropics/prompt-eng-interactive-tutorial</a> — практика на примерах.</li></ul><hr><p><strong>Часы:</strong> ~4 · <strong>DoD:</strong> <code>pytest code -q</code> зелёный, демо запускается, ru.md заполнен. ✅ <strong>Урок готов</strong></p>"},{"id":"3.2","title":"Длинный контекст и работа с файлами","status":"done","motto":"Документ не влезает в окно — режь на части, сжимай каждую, своди вместе.","hours":4,"dir":"phases/03-text-and-documents/03-long-context-and-files","html":"<h1>Урок 3.2 · Длинный контекст и работа с файлами</h1><p><strong>Фаза 3 — Текст и документы</strong> · <strong>Результат фазы:</strong> Строить пайплайны резюмирования и редактуры; собрать базовый RAG над своим корпусом.</p><blockquote><strong>MOTTO.</strong> Документ не влезает в окно — режь на части, сжимай каждую, своди вместе.</blockquote><h2>PROBLEM</h2><p>Отчёт на 200 страниц не влезает в контекстное окно (урок 1.2), а если и влезает (1M токенов) — это дорого и «середина теряется». Нужна стратегия: разбить документ на куски, обработать каждый и собрать общий результат — <strong>chunking + map-reduce</strong>.</p><h2>CONCEPT</h2><pre><code>длинный документ\n   │ chunking (перекрывающиеся куски)\n   ▼\n[chunk] [chunk] [chunk] ...\n   │ map: сжать каждый кусок\n   ▼\n[summary] [summary] [summary]\n   │ reduce: свести в один результат\n   ▼\nитоговое резюме</code></pre><p>Перекрытие (<code>overlap</code>) сохраняет связность на стыках. Map-reduce масштабируется на документы любой длины и кладётся в основу RAG (урок 3.3).</p><h2>BUILD IT</h2><p>Chunking с перекрытием + map-reduce, без зависимостей: [<code>code/chunking.py</code>](../code/chunking.py).</p><ul><li><code>chunk_text(text, size, overlap)</code> — резать на куски по словам внахлёст (валидирует <code>overlap &lt; size</code>);</li><li><code>map_reduce(chunks, map_fn, reduce_fn)</code> — обработать каждый кусок и свести результаты.</li></ul><pre><code>python code/chunking.py\npytest code -q</code></pre><p>Тесты проверяют главное: ни одно слово не теряется, соседние чанки реально перекрываются, map-reduce корректно сводит.</p><h2>USE IT</h2><p>В проде <code>map_fn</code> — суммаризация куска моделью, <code>reduce_fn</code> — сведение саммари; либо грузишь файл целиком в длинное окно:</p><pre><code>chunks = chunk_text(big_doc, size=800, overlap=100)\nsummarize = lambda c: Anthropic().messages.create(model=\"claude-haiku-4-5\", max_tokens=200,\n    messages=[{\"role\": \"user\", \"content\": f\"Сожми кусок:\\n{c}\"}]).content[0].text\nfinal = map_reduce(chunks, summarize,\n    lambda parts: stage_reduce(\"\\n\".join(parts)))   # свести саммари в одно</code></pre><p>Альтернатива для умеренных объёмов — длинный контекст напрямую: загрузка PDF/таблиц и окно до 1M токенов (Claude/Gemini), с подсказками из <a href=\"https://docs.anthropic.com/en/docs/build-with-claude/prompt-engineering/long-context-tips\" target=\"_blank\" rel=\"noopener\">long context tips</a>: данные — вверх, вопрос — в конец.</p><h2>SHIP IT</h2><p><strong>Артефакт:</strong> Пайплайн обработки длинных документов → [<code>outputs/long-doc-pipeline.md</code>](../outputs/long-doc-pipeline.md)</p><p>Рецепт «когда chunking+map-reduce, когда длинное окно» + параметры (size/overlap) под типы документов. Связка с RAG — урок 3.3, с бюджетом — урок 1.1.</p><h2>Материалы</h2><ul><li><a href=\"https://docs.anthropic.com/en/docs/build-with-claude/prompt-engineering/long-context-tips\" target=\"_blank\" rel=\"noopener\">Anthropic — Long context prompting tips</a> — как работать с большим окном.</li><li><a href=\"https://docs.langchain.com/oss/python/integrations/splitters\" target=\"_blank\" rel=\"noopener\">LangChain — Text splitters</a> — стратегии chunking (recursive, по токенам, по структуре).</li><li><a href=\"https://www.anthropic.com/research/prompting-long-context\" target=\"_blank\" rel=\"noopener\">Anthropic — Prompting long context (research)</a> — почему позиция данных важна.</li></ul><hr><p><strong>Часы:</strong> ~4 · <strong>DoD:</strong> <code>pytest code -q</code> зелёный, демо запускается, ru.md заполнен. ✅ <strong>Урок готов</strong></p>"},{"id":"3.3","title":"Основы RAG: retrieval","status":"done","motto":"RAG — это «найди нужное, потом ответь»: retrieval решает, что модель вообще увидит.","hours":4,"dir":"phases/03-text-and-documents/03-rag-basics-retrieval","html":"<h1>Урок 3.3 · Основы RAG: retrieval</h1><p><strong>Фаза 3 — Текст и документы</strong> · <strong>Результат фазы:</strong> Строить пайплайны резюмирования и редактуры; собрать базовый RAG над своим корпусом.</p><blockquote><strong>MOTTO.</strong> RAG — это «найди нужное, потом ответь»: retrieval решает, что модель вообще увидит.</blockquote><h2>PROBLEM</h2><p>Модель не знает твоих документов и выдумывает (урок 1.4), а весь корпус в окно не влезает (3.2). Решение — <strong>RAG</strong>: на запрос находим релевантные куски и подкладываем их в промпт. Сердце RAG — <strong>retrieval</strong>: как из тысяч кусков достать те самые. Сначала строим ретривер сами, чтобы понять механизм.</p><h2>CONCEPT</h2><pre><code>корпус → эмбеддинги (вектор на документ)\nзапрос → эмбеддинг запроса\n            │ косинусная близость\n            ▼\n     топ-k ближайших кусков → в промпт модели</code></pre><p>«Эмбеддинг» — это представление текста числами так, что похожее по смыслу близко в пространстве. В Build It роль эмбеддинга играет <strong>TF-IDF</strong>; в проде — нейроэмбеддинги, но принцип «вектор + близость» тот же.</p><h2>BUILD IT</h2><p>TF-IDF ретривер с нуля: [<code>code/retriever.py</code>](../code/retriever.py).</p><ul><li><code>build_index(docs)</code> — посчитать idf и tf-idf векторы документов;</li><li><code>search(query, index, k)</code> — топ-k по косинусной близости.</li></ul><pre><code>python code/retriever.py\npytest code -q</code></pre><p>Тесты показывают суть retrieval: на «язык программирования» первым идёт док про Python, на «домашние животные» — про кошек/собак, а нерелевантный запрос даёт score 0.</p><h2>USE IT</h2><p>Тот же retrieval — через нейроэмбеддинги и готовое хранилище (мульти-провайдер):</p><pre><code># Эмбеддинги: OpenAI text-embedding-3-* / Google text-embedding / Voyage (для Anthropic)\nemb = OpenAI().embeddings.create(model=\"text-embedding-3-small\", input=docs)\n# Хранилище: Chroma берёт индексацию и поиск на себя\nimport chromadb\ncol = chromadb.Client().create_collection(\"docs\")\ncol.add(ids=ids, documents=docs)            # Chroma сам считает эмбеддинги\nhits = col.query(query_texts=[\"мой вопрос\"], n_results=3)</code></pre><p>Принцип не изменился — поменялись только «эмбеддинг» (нейросеть вместо TF-IDF) и «индекс» (БД вместо словаря).</p><h2>SHIP IT</h2><p><strong>Артефакт:</strong> Мини-RAG скрипт → [<code>outputs/mini_rag.py</code>](../outputs/mini_rag.py)</p><p>Готовый <code>retrieve → augment → (generate)</code>: находит топ-k кусков и собирает промпт с контекстом. Полноценное хранилище — урок 3.4; первоисточник идеи — <a href=\"https://arxiv.org/abs/2005.11401\" target=\"_blank\" rel=\"noopener\">RAG, Lewis et al. 2020</a>.</p><h2>Материалы</h2><ul><li><a href=\"https://arxiv.org/abs/2005.11401\" target=\"_blank\" rel=\"noopener\">Lewis et al., 2020 — Retrieval-Augmented Generation</a> — первоисточник RAG.</li><li><a href=\"https://platform.openai.com/docs/guides/embeddings\" target=\"_blank\" rel=\"noopener\">OpenAI — Embeddings guide</a> — нейроэмбеддинги и косинусная близость.</li><li><a href=\"https://docs.trychroma.com/docs/overview/introduction\" target=\"_blank\" rel=\"noopener\">Chroma — Introduction</a> — готовое хранилище для retrieval.</li></ul><hr><p><strong>Часы:</strong> ~4 · <strong>DoD:</strong> <code>pytest code -q</code> зелёный, демо запускается, ru.md заполнен. ✅ <strong>Урок готов</strong></p>"},{"id":"3.4","title":"Векторные хранилища","status":"done","motto":"Векторное хранилище — это память RAG: добавил документы → ищешь по смыслу.","hours":4,"dir":"phases/03-text-and-documents/03-vector-stores","html":"<h1>Урок 3.4 · Векторные хранилища</h1><p><strong>Фаза 3 — Текст и документы</strong> · <strong>Результат фазы:</strong> Строить пайплайны резюмирования и редактуры; собрать базовый RAG над своим корпусом.</p><blockquote><strong>MOTTO.</strong> Векторное хранилище — это память RAG: добавил документы → ищешь по смыслу.</blockquote><h2>PROBLEM</h2><p>В уроке 3.3 ретривер пересчитывал всё на каждый запрос и жил в памяти процесса. Для реального корпуса нужно <strong>хранилище</strong>: добавлять документы инкрементально, искать по близости и сохранять на диск, чтобы не индексировать заново. Это превращает retrieval в переиспользуемый ассистент по документам.</p><h2>CONCEPT</h2><pre><code>VectorStore:\n  add(id, text)   → text → эмбеддинг → хранить (id, text, vec)\n  query(text, k)  → эмбеддинг запроса → косинус → топ-k\n  save/load       → персистентность на диск</code></pre><p>Тот же интерфейс у Chroma/FAISS/pgvector — разница в масштабе и алгоритме поиска (точный перебор против ANN). Сначала собираем маленькое хранилище сами.</p><h2>BUILD IT</h2><p>Мини векторное хранилище с персистентностью: [<code>code/vector_store.py</code>](../code/vector_store.py).</p><ul><li><code>embed(text)</code> — <strong>детерминированный</strong> хеширующий эмбеддинг (встроенный <code>hash()</code> рандомизирован — нельзя);</li><li><code>VectorStore.add / query / save / load</code> — добавление, поиск по косинусу, сохранение/загрузка.</li></ul><pre><code>python code/vector_store.py\npytest code -q</code></pre><p>Тест save/load доказывает персистентность: после загрузки с диска поиск даёт тот же результат — хранилище переживает перезапуск.</p><h2>USE IT</h2><p>В проде подключаешь готовое хранилище и нейроэмбеддинги (мульти-провайдер):</p><pre><code># Chroma (локально/сервер) — берёт эмбеддинги и индекс на себя\nimport chromadb\ncol = chromadb.PersistentClient(path=\"./db\").get_or_create_collection(\"kb\")\ncol.add(ids=ids, documents=docs)\ncol.query(query_texts=[\"вопрос\"], n_results=5)\n\n# FAISS — быстрый ANN-поиск по своим векторам (OpenAI/Google embeddings)\n# pgvector — векторный поиск прямо в PostgreSQL</code></pre><p>Тот же <code>add/query</code>, но индекс масштабируется на миллионы векторов (HNSW/IVF), а эмбеддинги считает нейросеть.</p><h2>SHIP IT</h2><p><strong>Артефакт:</strong> doc-assistant skill → [<code>outputs/doc-assistant/</code>](../outputs/doc-assistant/SKILL.md)</p><p>Скилл <code>doc-assistant</code>: складываешь корпус в хранилище и спрашиваешь его на естественном языке (retrieve → augment → ответ). Зарегистрирован в [<code>.claude/skills/doc-assistant</code>](../../../.claude/skills/doc-assistant/SKILL.md) — это итог Фазы 3 над собственным корпусом.</p><h2>Материалы</h2><ul><li><a href=\"https://docs.trychroma.com/docs/overview/introduction\" target=\"_blank\" rel=\"noopener\">Chroma — Introduction</a> — открытое векторное хранилище для RAG.</li><li><a href=\"https://github.com/facebookresearch/faiss\" target=\"_blank\" rel=\"noopener\">facebookresearch/faiss</a> — быстрый ANN-поиск по плотным векторам.</li><li><a href=\"https://platform.openai.com/docs/guides/embeddings\" target=\"_blank\" rel=\"noopener\">OpenAI — Embeddings guide</a> — какие эмбеддинги класть в хранилище.</li></ul><hr><p><strong>Часы:</strong> ~4 · <strong>DoD:</strong> <code>pytest code -q</code> зелёный, демо запускается, ru.md заполнен. ✅ <strong>Урок готов</strong></p>"},{"id":"3.5","title":"Production RAG","status":"done","motto":"Прод-RAG — это не «один ретривер», а гибрид сигналов, reranking и измеримое качество.","hours":4,"dir":"phases/03-text-and-documents/03-production-rag","html":"<h1>Урок 3.5 · Production RAG</h1><p><strong>Фаза 3 — Текст и документы</strong> · <strong>Результат фазы:</strong> Строить пайплайны резюмирования и редактуры; собрать базовый RAG над своим корпусом.</p><blockquote><strong>MOTTO.</strong> Прод-RAG — это не «один ретривер», а гибрид сигналов, reranking и измеримое качество.</blockquote><h2>PROBLEM</h2><p>Базовый vector-RAG (3.3–3.4) в проде проседает: чистая семантика промахивается на точных терминах, кодах, именах; чистый keyword — на синонимах и перефразировках. А ещё непонятно, стало ли лучше. Прод-RAG объединяет <strong>vector + keyword</strong>, <strong>переранжирует</strong> результаты и <strong>измеряет</strong> качество ретрива и ответа.</p><h2>CONCEPT</h2><pre><code>запрос ─┬─ vector ranking (семантика, TF-IDF/эмбеддинги)\n        └─ keyword ranking (точные слова)\n                │ Reciprocal Rank Fusion (слияние рангов)\n                ▼\n        rerank → top-k → ответ\n                │\n        eval: faithfulness · context precision/recall (RAGAS)</code></pre><p>Два рычага качества: <strong>гибрид</strong> (берём лучшее из двух ретриверов) и <strong>оценка</strong> (меряем заземлённость и релевантность, а не «на глаз»).</p><h2>BUILD IT</h2><p>Гибридный ретривер (vector+keyword) + RRF-reranking, без зависимостей: [<code>code/hybrid_retriever.py</code>](../code/hybrid_retriever.py).</p><ul><li><code>vector_ranking</code> / <code>keyword_ranking</code> — два независимых сигнала;</li><li><code>reciprocal_rank_fusion(rankings)</code> — слияние рангов (RRF);</li><li><code>hybrid_search(query, index, top_k)</code> — итоговый гибрид.</li></ul><pre><code>python code/hybrid_retriever.py\npytest code -q</code></pre><p>Тест показывает суть: keyword ловит точный термин <code>numpy</code>, который чистый vector мог бы ранжировать ниже, а RRF сводит оба сигнала.</p><h2>USE IT</h2><p>В проде — managed retrieval, графовый RAG и автоматическая оценка (мульти-провайдер):</p><ul><li><strong>GraphRAG</strong> (Microsoft) — строит граф сущностей/связей и community-саммари: сильнее baseline-RAG на «глобальных» вопросах по большому корпусу.</li><li><strong>Managed retrieval</strong> — гибридный поиск из коробки (Chroma/pgvector/векторные БД), нейроэмбеддинги вместо TF-IDF.</li><li><strong>RAGAS</strong> — оценка пайплайна без ручной разметки: faithfulness, answer relevancy, context precision/recall — отдельно по ретриверу и генератору.</li></ul><h2>SHIP IT</h2><p><strong>Артефакт:</strong> production-rag pipeline + eval-отчёт → [<code>outputs/production-rag-pipeline.md</code>](../outputs/production-rag-pipeline.md)</p><p>Рецепт прод-RAG (chunking → гибрид → rerank → ответ → eval) + шаблон отчёта по метрикам RAGAS. Связь с 3.2 (chunking), 3.3–3.4 (retrieval/store), 2.5/7.5 (eval).</p><h2>Материалы</h2><ul><li><a href=\"https://microsoft.github.io/graphrag/\" target=\"_blank\" rel=\"noopener\">Microsoft GraphRAG</a> — графовый RAG для сложных запросов.</li><li><a href=\"https://docs.ragas.io/en/stable/concepts/metrics/available_metrics/\" target=\"_blank\" rel=\"noopener\">Ragas — метрики</a> — faithfulness, context precision/recall.</li><li><a href=\"https://arxiv.org/abs/2309.15217\" target=\"_blank\" rel=\"noopener\">Ragas (arXiv 2309.15217)</a> — first-source автоматической оценки RAG.</li></ul><hr><p><strong>Часы:</strong> ~4 · <strong>DoD:</strong> <code>pytest code -q</code> зелёный, демо запускается, ru.md заполнен. ✅ <strong>Урок готов</strong></p>"}],"hours":18,"outcome":"Строить пайплайны резюмирования и редактуры; собрать базовый RAG над своим корпусом."},{"id":4,"name":"Скиллы, память и проекты (слой персистентности)","lessons":[{"id":"4.1","title":"Skills и SKILL.md","status":"done","motto":"Скилл — это навык в виде папки: агент сам подгружает нужный по описанию.","hours":4,"dir":"phases/04-skills-memory-projects/04-skills-and-skill-md","html":"<h1>Урок 4.1 · Skills и SKILL.md</h1><p><strong>Фаза 4 — Скиллы, память и проекты</strong> · <strong>Результат фазы:</strong> Упаковывать способности в SKILL.md, хранить контекст в памяти и собирать рабочие проекты.</p><blockquote><strong>MOTTO.</strong> Скилл — это навык в виде папки: агент сам подгружает нужный по описанию.</blockquote><h2>PROBLEM</h2><p>Хорошие приёмы (как редактировать, как считать, как оформлять) приходится объяснять агенту заново каждый раз. <strong>Скилл</strong> упаковывает навык в переиспользуемую папку с <code>SKILL.md</code>: агент видит описание и сам решает, когда его применить. Без понимания формата и матчинга скиллы кажутся магией.</p><h2>CONCEPT</h2><pre><code>.claude/skills/&lt;name&gt;/SKILL.md\n   ---\n   name: &lt;как зовут&gt;\n   description: &lt;когда применять + триггеры&gt;   ← по этому агент выбирает скилл\n   ---\n   &lt;инструкция: тело, грузится только если скилл выбран&gt;  (progressive disclosure)</code></pre><p>Ключ — <strong>description с триггерами</strong>: именно по нему агент матчит запрос на скилл, не загружая тело всех скиллов в контекст (экономия — урок 4.4).</p><h2>BUILD IT</h2><p>Парсер SKILL.md + валидатор + матчер, без зависимостей: [<code>code/skill_loader.py</code>](../code/skill_loader.py).</p><ul><li><code>parse_skill(text)</code> — разобрать frontmatter (<code>name</code>/<code>description</code>) и тело (без PyYAML);</li><li><code>validate_skill(parsed)</code> — проверить обязательные поля и длину description;</li><li><code>match_skill(query, skills)</code> — выбрать скилл по пересечению слов запроса и description.</li></ul><pre><code>python code/skill_loader.py\npytest code -q</code></pre><p>Матчер намеренно простой (совпадение слов) — он показывает суть выбора; настоящий агент матчит <strong>семантически</strong>, поэтому в description важны разные формулировки-триггеры.</p><h2>USE IT</h2><p>Положи скилл в <code>.claude/skills/&lt;name&gt;/SKILL.md</code> — Claude Code / Cursor / Codex подхватят его по описанию. Проверка наличия:</p><pre><code>ls .claude/skills/*/SKILL.md          # проектные скиллы\nls ~/.claude/skills/*/SKILL.md        # личные скиллы</code></pre><p>Тот же <code>SKILL.md</code> кросс-платформенный; в курсе так сделаны <code>editor</code>, <code>doc-assistant</code>, <code>prompt-eval</code>, онбординг-скиллы.</p><h2>SHIP IT</h2><p><strong>Артефакт:</strong> Кросс-платформенный skill → [<code>outputs/skill-template/</code>](../outputs/skill-template/SKILL.md)</p><p>Готовый шаблон <code>SKILL.md</code> (frontmatter + структура тела + чек-лист хорошего description) — копируешь, заполняешь, кладёшь в <code>.claude/skills/</code>. Дальше скиллы соединяются с памятью (4.2) и проектами (4.3).</p><h2>Материалы</h2><ul><li><a href=\"https://docs.claude.com/en/docs/agents-and-tools/agent-skills/overview\" target=\"_blank\" rel=\"noopener\">Anthropic — Agent Skills overview</a> — что такое скиллы и как грузятся.</li><li><a href=\"https://github.com/anthropics/skills\" target=\"_blank\" rel=\"noopener\">anthropics/skills</a> — публичные скиллы и <code>template-skill</code>.</li><li><a href=\"https://code.claude.com/docs/en/overview\" target=\"_blank\" rel=\"noopener\">Claude Code — overview</a> — где живут скиллы (<code>.claude/skills</code>).</li></ul><hr><p><strong>Часы:</strong> ~4 · <strong>DoD:</strong> <code>pytest code -q</code> зелёный, демо запускается, ru.md заполнен. ✅ <strong>Урок готов</strong></p>"},{"id":"4.2","title":"Память: короткая и длинная","status":"done","motto":"Без памяти агент каждый раз начинает с чистого листа; память — это контекст, переживающий сессию.","hours":4,"dir":"phases/04-skills-memory-projects/04-memory-short-and-long","html":"<h1>Урок 4.2 · Память: короткая и длинная</h1><p><strong>Фаза 4 — Скиллы, память и проекты</strong> · <strong>Результат фазы:</strong> Упаковывать способности в SKILL.md, хранить контекст в памяти и собирать рабочие проекты.</p><blockquote><strong>MOTTO.</strong> Без памяти агент каждый раз начинает с чистого листа; память — это контекст, переживающий сессию.</blockquote><h2>PROBLEM</h2><p>Модель забывает всё между запросами: предпочтения, факты о пользователе, решения прошлых задач. Каждый диалог — заново. Нужна <strong>память</strong>: короткая (в пределах сессии) и длинная (на диске, между сессиями) с поиском по релевантности и учётом свежести.</p><h2>CONCEPT</h2><pre><code>add(text, ts)  → запись с меткой времени\n                     │\nsearch(query)  → релевантность (совпадение) + свежесть (затухание во времени)\n                     │\n              топ-k записей → в контекст следующего запроса\nsave / load    → переживает перезапуск (длинная память)</code></pre><p>Темпоральность важна: при равной релевантности свежее обычно важнее. Это ровно тот принцип, по которому работает и моя межсессионная память.</p><h2>BUILD IT</h2><p>Файловое хранилище памяти с поиском и темпоральностью: [<code>code/memory_store.py</code>](../code/memory_store.py).</p><ul><li><code>add(text, tags, ts)</code> — запись с меткой времени;</li><li><code>search(query, k, now)</code> — релевантность + свежесть (полураспад <code>half_life</code>);</li><li><code>recent(n)</code> — последние записи; <code>save/load</code> — персистентность.</li></ul><pre><code>python code/memory_store.py\npytest code -q</code></pre><p>Тест доказывает темпоральность: две записи про «кофе», но свежая выходит первой; а после save/load память переживает перезапуск.</p><h2>USE IT</h2><p>Платформенная память делает то же самое под капотом (мульти-провайдер):</p><ul><li><strong>ChatGPT Memory</strong> — «заметки» отдельно от истории чатов: помнит факты о тебе между диалогами, управляется в Settings → Personalization.</li><li><strong>Claude</strong> — постоянный контекст через Projects (knowledge) и память сессий.</li><li><strong>Gemini</strong> — персонализация и Gems с заданным контекстом.</li></ul><p>Принцип один: хранить вне промпта и подмешивать релевантное; разница — в том, что и как долго хранится и как этим управлять (приватность — Фаза 11).</p><h2>SHIP IT</h2><p><strong>Артефакт:</strong> Модуль памяти для агента → [<code>outputs/memory_module.py</code>](../outputs/memory_module.py)</p><p>Переиспользуемый <code>MemoryStore</code> (add/search/recent/save/load) — подключается к агенту в Фазе 7 (память и планирование). Соединяется с контекст-инжинирингом (4.4): что из памяти класть в окно.</p><h2>Материалы</h2><ul><li><a href=\"https://help.openai.com/en/articles/8590148-memory-faq\" target=\"_blank\" rel=\"noopener\">OpenAI — Memory FAQ</a> — как устроена память ChatGPT и управление ей.</li><li><a href=\"https://openai.com/index/memory-and-new-controls-for-chatgpt/\" target=\"_blank\" rel=\"noopener\">OpenAI — Memory and new controls (blog)</a> — анонс и принципы.</li><li><a href=\"https://www.anthropic.com/engineering/effective-context-engineering-for-ai-agents\" target=\"_blank\" rel=\"noopener\">Anthropic — Effective context engineering</a> — память как часть управляемого контекста.</li></ul><hr><p><strong>Часы:</strong> ~4 · <strong>DoD:</strong> <code>pytest code -q</code> зелёный, демо запускается, ru.md заполнен. ✅ <strong>Урок готов</strong></p>"},{"id":"4.3","title":"Проекты и рабочие пространства","status":"done","motto":"Проект = постоянная инструкция + база знаний: ассистент перестаёт начинать «с нуля».","hours":4,"dir":"phases/04-skills-memory-projects/04-projects-and-workspaces","html":"<h1>Урок 4.3 · Проекты и рабочие пространства</h1><p><strong>Фаза 4 — Скиллы, память и проекты</strong> · <strong>Результат фазы:</strong> Упаковывать способности в SKILL.md, хранить контекст в памяти и собирать рабочие проекты.</p><blockquote><strong>MOTTO.</strong> Проект = постоянная инструкция + база знаний: ассистент перестаёт начинать «с нуля».</blockquote><h2>PROBLEM</h2><p>Универсальный чат не знает твоего контекста: каждый раз объясняешь роль, правила и подкладываешь документы. <strong>Проект</strong> (workspace) фиксирует system prompt и базу знаний один раз — и ассистент отвечает «как член твоей команды» во всех диалогах. Соберём такой проект сами.</p><h2>CONCEPT</h2><pre><code>Project\n ├─ system_prompt   постоянная роль и правила\n ├─ knowledge[]     база знаний проекта\n └─ build_context(query):\n        system_prompt + retrieve(query, k) + запрос  → промпт модели</code></pre><p>Это объединение фазы: <strong>скилл</strong> (способность) + <strong>память</strong> (что помним) + <strong>RAG</strong> (что нашли) внутри одного рабочего пространства.</p><h2>BUILD IT</h2><p>Проект «system prompt + база знаний» с retrieval: [<code>code/project.py</code>](../code/project.py).</p><ul><li><code>Project(name, system_prompt, knowledge)</code> — рабочее пространство;</li><li><code>add_knowledge(doc)</code> — пополнить базу;</li><li><code>retrieve(query, k)</code> / <code>build_context(query, k)</code> — найти релевантное и собрать промпт.</li></ul><pre><code>python code/project.py\npytest code -q</code></pre><p>Тест показывает суть: в контекст попадает system prompt и <strong>только релевантные</strong> куски знаний (нерелевантное отсекается — экономия окна, урок 4.4).</p><h2>USE IT</h2><p>Те же «проекты» есть во всех платформах (мульти-провайдер):</p><ul><li><strong>Claude Projects</strong> — workspace с инструкциями и knowledge base (RAG включается автоматически).</li><li><strong>Custom GPTs</strong> (OpenAI) — name + instructions + knowledge + capabilities, собирается в билдере.</li><li><strong>Gemini Gems</strong> — кастомный ассистент с инструкциями и файлами знаний.</li></ul><p>Везде одно: постоянная инструкция + загруженные документы → ассистент под конкретную задачу без повторных объяснений.</p><h2>SHIP IT</h2><p><strong>Артефакт:</strong> Настроенный ассистент с базой знаний → [<code>outputs/assistant-project.md</code>](../outputs/assistant-project.md)</p><p>Рецепт сборки проекта: что класть в system prompt, как структурировать knowledge, как проверять качество ответов. Дальше — управление контекстом проекта (4.4) и превращение в агента (Фаза 7).</p><h2>Материалы</h2><ul><li><a href=\"https://www.anthropic.com/news/projects\" target=\"_blank\" rel=\"noopener\">Claude — Projects</a> — workspace с инструкциями и базой знаний.</li><li><a href=\"https://help.openai.com/en/articles/8554397-creating-and-editing-gpts\" target=\"_blank\" rel=\"noopener\">OpenAI — Creating and editing GPTs</a> — сборка Custom GPT.</li><li><a href=\"https://blog.google/products/gemini/google-gems-tips/\" target=\"_blank\" rel=\"noopener\">Google — Gems tips</a> — кастомные ассистенты Gemini.</li></ul><hr><p><strong>Часы:</strong> ~4 · <strong>DoD:</strong> <code>pytest code -q</code> зелёный, демо запускается, ru.md заполнен. ✅ <strong>Урок готов</strong></p>"},{"id":"4.4","title":"Контекст-инжиниринг","status":"done","motto":"Контекст — конечный бюджет; инженерия в том, ЧТО и в каком порядке в него класть.","hours":4,"dir":"phases/04-skills-memory-projects/04-context-engineering","html":"<h1>Урок 4.4 · Контекст-инжиниринг</h1><p><strong>Фаза 4 — Скиллы, память и проекты</strong> · <strong>Результат фазы:</strong> Упаковывать способности в SKILL.md, хранить контекст в памяти и собирать рабочие проекты.</p><blockquote><strong>MOTTO.</strong> Контекст — конечный бюджет; инженерия в том, ЧТО и в каком порядке в него класть.</blockquote><h2>PROBLEM</h2><p>Скиллы, память, RAG, история диалога — всё хочет места в контекстном окне, а оно конечно (урок 1.2) и платное (урок 1.1). Запихнуть всё нельзя, да и вредно: лишнее снижает внимание модели («context rot»). Нужна <strong>политика сборки и компакции</strong> контекста: по приоритету собрать, лишнее — отбросить.</p><h2>CONCEPT</h2><pre><code>слои контекста (по приоритету):\n  system (4) &gt; RAG (3) &gt; память (2) &gt; история (1)\n        │ assemble(layers, budget)\n        ▼\n  берём по убыванию приоритета, пока влезает в бюджет\n  не влезло → компакция: отбрасываем/режем низкоприоритетное</code></pre><p>Это «context engineering»: не «какие слова», а «какая конфигурация контекста даёт нужное поведение» при ограниченном окне.</p><h2>BUILD IT</h2><p>Сборка контекста под бюджет + компакция, без зависимостей: [<code>code/context_engine.py</code>](../code/context_engine.py).</p><ul><li><code>estimate_tokens(text)</code> — оценка размера (≈ слова; точнее — токенайзером из 1.1);</li><li><code>assemble(layers, budget)</code> — собрать по приоритету, что не влезло — в <code>dropped</code>;</li><li><code>compact_history(history, max_items)</code> — оставить последние сообщения.</li></ul><pre><code>python code/context_engine.py\npytest code -q</code></pre><p>Тест показывает политику в действии: при тесном бюджете остаются <code>system</code> и <code>RAG</code>, а <code>history</code> отбрасывается; всё влезает — берём всё.</p><h2>USE IT</h2><p>Контекст-инжиниринг связывает RAG и стоимость (мульти-провайдер):</p><ul><li><strong>RAG вместо «всего корпуса»</strong> — кладём только топ-k релевантных кусков (Фаза 3), а не весь документ.</li><li><strong>Память — выборочно</strong> — подмешиваем релевантные записи (4.2), а не всю историю.</li><li><strong>Стоимость</strong> — каждый слой считаем <code>prompt-token-budgeter</code> (1.1); меньше токенов → дешевле и быстрее (Фаза 9).</li><li>Инструменты тоже занимают контекст: держи 3–5 ключевых всегда, остальное — по запросу (см. Anthropic context engineering).</li></ul><h2>SHIP IT</h2><p><strong>Артефакт:</strong> Политика управления контекстом → [<code>outputs/context-policy.md</code>](../outputs/context-policy.md)</p><p>Готовая политика: приоритеты слоёв, правила компакции, бюджеты под задачу. Итог Фазы 4 — слой персистентности (skills + memory + projects + context), на котором стоят агенты (Фаза 7).</p><h2>Материалы</h2><ul><li><a href=\"https://www.anthropic.com/engineering/effective-context-engineering-for-ai-agents\" target=\"_blank\" rel=\"noopener\">Anthropic — Effective context engineering for AI agents</a> — context rot, бюджет, стратегии.</li><li><a href=\"https://docs.anthropic.com/en/docs/build-with-claude/prompt-engineering/long-context-tips\" target=\"_blank\" rel=\"noopener\">Anthropic — Long context prompting tips</a> — порядок данных в окне.</li><li><a href=\"https://platform.claude.com/docs/en/about-claude/models/overview\" target=\"_blank\" rel=\"noopener\">Anthropic — Claude models overview</a> — размеры контекстных окон по моделям.</li></ul><hr><p><strong>Часы:</strong> ~4 · <strong>DoD:</strong> <code>pytest code -q</code> зелёный, демо запускается, ru.md заполнен. ✅ <strong>Урок готов</strong></p>"},{"id":"4.5","title":"Агентные платформы: OpenClaw / Hermes","status":"done","motto":"Скилл + память + мульти-модель = персональный агент, который живёт у тебя и доступен везде.","hours":4,"dir":"phases/04-skills-memory-projects/04-agent-platforms-openclaw-hermes","html":"<h1>Урок 4.5 · Агентные платформы: OpenClaw / Hermes</h1><p><strong>Фаза 4 — Скиллы, память и проекты (слой персистентности)</strong> · <strong>Результат фазы:</strong> Упаковывать способности в SKILL.md, хранить контекст в памяти и собирать рабочие проекты.</p><blockquote><strong>MOTTO.</strong> Скилл + память + мульти-модель = персональный агент, который живёт у тебя и доступен везде.</blockquote><h2>PROBLEM</h2><p>Скиллы (4.1) и память (4.2) по отдельности — это детали. В 2026 появились <strong>self-hosted агентные платформы</strong>, которые собирают их в постоянного персонального агента: грузят скиллы, помнят контекст между сессиями, ходят в несколько моделей и отвечают в твоих мессенджерах. Поймём механику, собрав мини-рантайм, и сравним готовые платформы.</p><h2>CONCEPT</h2><pre><code>self-hosted агент-рантайм (OpenClaw / Hermes)\n   ├─ skills (SKILL.md / agentskills.io)   ← способности (4.1)\n   ├─ memory (между сессиями)               ← контекст (4.2)\n   ├─ model router (любая модель)           ← мульти-модель (Ф9)\n   ├─ MCP / tools                           ← инструменты (Ф6)\n   └─ channels (Telegram/Slack/…)           ← доступ отовсюду</code></pre><p>Ключевая идея: агент <strong>у тебя</strong> (данные не уходят наружу — мост к 11.1), а скиллы переносимы между платформами (стандарт agentskills.io).</p><h2>BUILD IT</h2><p>Загрузка скилла в локальный рантайм + роутинг + память: [<code>code/skill_runtime.py</code>](../code/skill_runtime.py).</p><ul><li><code>parse_skill</code> — разбор SKILL.md (как в 4.1);</li><li><code>Runtime.load/route/handle</code> — грузим скиллы, роутим сообщение в подходящий, держим память.</li></ul><pre><code>python code/skill_runtime.py\npytest code -q</code></pre><p>Это уменьшенная копия того, что делают OpenClaw/Hermes: реестр скиллов + маршрутизация + память между сообщениями.</p><h2>USE IT</h2><p>Готовые платформы (self-hosted, MIT, model-agnostic):</p><ul><li><strong>OpenClaw</strong> — гейтвей-оркестратор, messaging-first (Telegram/Slack/Signal/…), ClawHub-маркетплейс скиллов, любой OpenAI-совместимый бэкенд (вкл. локальные через Ollama).</li><li><strong>Hermes Agent</strong> (Nous Research) — self-improving loop: после задачи пишет переиспользуемый скилл и улучшает его; 7-слойная модель безопасности; автоимпорт конфигурации из OpenClaw.</li><li>Оба ставятся за ~$5/мес + стоимость модели; скиллы переносимы (agentskills.io).</li></ul><p>⚠️ Self-host решает приватность/суверенитет (11.1), но это исполнение кода у тебя — нужен sandbox и защита от prompt injection (11.5).</p><h2>SHIP IT</h2><p><strong>Артефакт:</strong> Свой агент на self-hosted платформе → [<code>outputs/self-hosted-agent.md</code>](../outputs/self-hosted-agent.md)</p><p>Гайд: упаковать свой скилл (4.1) → положить в рантайм → подключить модель и канал. Связь со скиллами (4.1), памятью (4.2), MCP (Ф6), FinOps (Ф9), безопасностью (11.5).</p><h2>Материалы</h2><ul><li><a href=\"https://github.com/openclaw/openclaw\" target=\"_blank\" rel=\"noopener\">openclaw/openclaw</a> — self-hosted агент-гейтвей (MIT).</li><li><a href=\"https://github.com/NousResearch/hermes-agent\" target=\"_blank\" rel=\"noopener\">NousResearch/hermes-agent</a> — агент с self-improving loop (MIT).</li><li><a href=\"https://docs.claude.com/en/docs/agents-and-tools/agent-skills/overview\" target=\"_blank\" rel=\"noopener\">Claude — Agent Skills</a> — формат скиллов, переносимых между платформами.</li></ul><hr><p><strong>Часы:</strong> ~4 · <strong>DoD:</strong> <code>pytest code -q</code> зелёный, демо запускается, ru.md заполнен. ✅ <strong>Урок готов</strong></p>"}],"hours":20,"outcome":"Упаковывать способности в SKILL.md, хранить контекст в памяти и собирать рабочие проекты."},{"id":5,"name":"Данные и аналитика","lessons":[{"id":"5.1","title":"ИИ как аналитик","status":"done","motto":"Аналитика — это план: что сгруппировать, что посчитать, как агрегировать; ИИ строит план, код его исполняет.","hours":4,"dir":"phases/05-data-and-analytics/05-ai-as-analyst","html":"<h1>Урок 5.1 · ИИ как аналитик</h1><p><strong>Фаза 5 — Данные и аналитика</strong> · <strong>Результат фазы:</strong> Анализировать данные с ИИ, делать NL→SQL и автоотчёты с валидацией результатов.</p><blockquote><strong>MOTTO.</strong> Аналитика — это план: что сгруппировать, что посчитать, как агрегировать; ИИ строит план, код его исполняет.</blockquote><h2>PROBLEM</h2><p>«Посмотри данные и скажи, что интересного» — звучит как магия, пока не видишь механику. ИИ-аналитик не «понимает» таблицу мистически: он превращает вопрос в <strong>план</strong> (группировка + метрика + агрегация) и исполняет его кодом. Поймём этот механизм, собрав мини-аналитику на stdlib.</p><h2>CONCEPT</h2><pre><code>вопрос → план анализа → исполнение → результат\n          group_by      aggregate     числа/топ\n          metric        (sum/mean/count)</code></pre><p>Это split-apply-combine: сгруппировать строки, применить агрегат, собрать ответ. Ровно это делает pandas (и ChatGPT/Claude под капотом) — мы делаем то же руками.</p><h2>BUILD IT</h2><p>Мини-аналитика на stdlib (csv, statistics): [<code>code/analyze.py</code>](../code/analyze.py).</p><ul><li><code>load_csv(text)</code> — таблица как список словарей;</li><li><code>aggregate(rows, group_by, value, agg)</code> — sum/mean/count по группам;</li><li><code>top_n(d, n)</code> — топ по значению.</li></ul><pre><code>python code/analyze.py\npytest code -q</code></pre><p>План задаётся параметрами — это и есть «промпт → план → исполнение», только без LLM, чтобы видеть суть.</p><h2>USE IT</h2><p>Тот же анализ — через pandas и загрузку файла в чат (мульти-провайдер):</p><pre><code>import pandas as pd\ndf = pd.read_csv(\"sales.csv\")\ndf.groupby(\"region\")[\"amount\"].sum().sort_values(ascending=False)   # то же, что aggregate+top_n</code></pre><ul><li><strong>ChatGPT</strong> — загрузи CSV/Excel, спроси «выручка по регионам, топ-5» → выполнит pandas-код и построит график.</li><li><strong>Claude</strong> — загрузка файла в чат/Project, тот же запрос; через API — code execution tool.</li></ul><p>⚠️ ИИ не знает происхождения данных и допущений — результат <strong>проверяй</strong> (урок 5.5).</p><h2>SHIP IT</h2><p><strong>Артефакт:</strong> Шаблон аналитического запроса → [<code>outputs/analysis-request-template.md</code>](../outputs/analysis-request-template.md)</p><p>Структура запроса к ИИ-аналитику (данные · вопрос · метрика · разрез · формат · проверка) — чтобы ответы были воспроизводимы и проверяемы. Дальше: исполнение кода (5.2), NL→SQL (5.3).</p><h2>Материалы</h2><ul><li><a href=\"https://pandas.pydata.org/docs/getting_started/index.html\" target=\"_blank\" rel=\"noopener\">pandas — Getting started</a> — стандартный инструмент анализа данных.</li><li><a href=\"https://help.openai.com/en/articles/8437071-code-interpreter\" target=\"_blank\" rel=\"noopener\">OpenAI — Data analysis with ChatGPT</a> — анализ загруженных файлов.</li><li><a href=\"https://docs.anthropic.com/en/docs/agents-and-tools/tool-use/code-execution-tool\" target=\"_blank\" rel=\"noopener\">Anthropic — Code execution tool</a> — анализ данных кодом через API.</li></ul><hr><p><strong>Часы:</strong> ~4 · <strong>DoD:</strong> <code>pytest code -q</code> зелёный, демо запускается, ru.md заполнен. ✅ <strong>Урок готов</strong></p>"},{"id":"5.2","title":"Code Interpreter","status":"done","motto":"Code Interpreter = модель пишет код, песочница его исполняет и возвращает результат.","hours":4,"dir":"phases/05-data-and-analytics/05-code-interpreter","html":"<h1>Урок 5.2 · Code Interpreter</h1><p><strong>Фаза 5 — Данные и аналитика</strong> · <strong>Результат фазы:</strong> Анализировать данные с ИИ, делать NL→SQL и автоотчёты с валидацией результатов.</p><blockquote><strong>MOTTO.</strong> Code Interpreter = модель пишет код, песочница его исполняет и возвращает результат.</blockquote><h2>PROBLEM</h2><p>LLM плохо считает «в уме» (арифметика, статистика), но отлично <strong>пишет код</strong>. Code Interpreter замыкает цикл: модель генерирует Python, изолированная среда его исполняет, результат и ошибки возвращаются модели. Чтобы это не было магией — соберём упрощённую песочницу и поймём, где границы безопасности.</p><h2>CONCEPT</h2><pre><code>запрос → модель пишет код → [ sandbox: исполнить ] → результат/ошибка → модель\n                                  │ ограниченные builtins\n                                  │ нет сети, изоляция, лимиты\n                                  ▼ self-correction по ошибке</code></pre><p>Ключ — <strong>изоляция</strong>: код модели нельзя пускать в обычный процесс. Настоящий Code Interpreter крутится в контейнере без сети; наша версия — учебная, показывает идею и проверки.</p><h2>BUILD IT</h2><p>Упрощённый sandbox-исполнитель: [<code>code/sandbox.py</code>](../code/sandbox.py).</p><ul><li><code>unsafe_tokens(code)</code> — статическая проверка на запрещённое (<code>import</code>, <code>__</code>, <code>open(</code>, <code>os.</code>…);</li><li><code>run_code(code, data)</code> — исполнить в окружении с ограниченными builtins; результат — переменная <code>result</code>.</li></ul><pre><code>python code/sandbox.py\npytest code -q</code></pre><p>⚠️ Честная оговорка: это <strong>не настоящая изоляция</strong> — ограниченные builtins и проверки лишь демонстрируют принцип. Для прода нужен контейнер/seccomp/отдельный процесс с лимитами (безопасность — урок 6.5).</p><h2>USE IT</h2><p>Готовый Code Interpreter — расчёты и графики (мульти-провайдер):</p><ul><li><strong>ChatGPT</strong> — Advanced Data Analysis: загрузка файла → «построй регрессию и график» → выполнит pandas/matplotlib в изолированной среде (без сети, инстанс уничтожается).</li><li><strong>OpenAI API</strong> — Code Interpreter tool в Responses/Assistants.</li><li><strong>Claude</strong> — <code>code_execution</code> server-tool: Python в песочнице Anthropic, в т.ч. над файлами из Files API.</li></ul><p>Везде один паттерн: модель пишет код → песочница исполняет → результат назад, с авто-исправлением ошибок.</p><h2>SHIP IT</h2><p><strong>Артефакт:</strong> Набор аналитических промптов → [<code>outputs/analysis-prompts.md</code>](../outputs/analysis-prompts.md)</p><p>Промпты для Code Interpreter под типовые задачи (профилирование, агрегации, графики, проверка гипотез) с требованием показать код. Дальше: NL→SQL для запросов к БД (5.3).</p><h2>Материалы</h2><ul><li><a href=\"https://platform.openai.com/docs/guides/tools-code-interpreter\" target=\"_blank\" rel=\"noopener\">OpenAI — Code Interpreter (API)</a> — инструмент исполнения кода.</li><li><a href=\"https://help.openai.com/en/articles/8437071-code-interpreter\" target=\"_blank\" rel=\"noopener\">OpenAI — Data analysis with ChatGPT</a> — как устроена изоляция и работа с файлами.</li><li><a href=\"https://docs.anthropic.com/en/docs/agents-and-tools/tool-use/code-execution-tool\" target=\"_blank\" rel=\"noopener\">Anthropic — Code execution tool</a> — Python в песочнице Claude.</li></ul><hr><p><strong>Часы:</strong> ~4 · <strong>DoD:</strong> <code>pytest code -q</code> зелёный, демо запускается, ru.md заполнен. ✅ <strong>Урок готов</strong></p>"},{"id":"5.3","title":"NL→SQL и семантический слой","status":"done","motto":"NL→SQL без семантического слоя — лотерея; метрики надо определить один раз и для всех.","hours":4,"dir":"phases/05-data-and-analytics/05-nl-to-sql-semantic-layer","html":"<h1>Урок 5.3 · NL→SQL и семантический слой</h1><p><strong>Фаза 5 — Данные и аналитика</strong> · <strong>Результат фазы:</strong> Анализировать данные с ИИ, делать NL→SQL и автоотчёты с валидацией результатов.</p><blockquote><strong>MOTTO.</strong> NL→SQL без семантического слоя — лотерея; метрики надо определить один раз и для всех.</blockquote><h2>PROBLEM</h2><p>«Спроси базу на человеческом языке» ломается о неоднозначность: что такое «выручка» — сумма <code>amount</code> или с вычетом возвратов? Без <strong>семантического слоя</strong> (единых определений метрик и измерений) LLM каждый раз гадает, и числа расходятся. Соберём NL→SQL поверх семантического слоя, чтобы смысл был один.</p><h2>CONCEPT</h2><pre><code>вопрос → [семантический слой: метрики + измерения] → SQL → результат\n            выручка = SUM(amount)        регион = region\n            заказы  = COUNT(*)           месяц  = month</code></pre><p>Семантический слой — контракт смысла: «выручка» всегда <code>SUM(amount)</code>. LLM лишь сопоставляет вопрос с метрикой/разрезом, а SQL собирается из проверенных определений — отсюда консистентность.</p><h2>BUILD IT</h2><p>NL→SQL поверх семантического слоя + исполнение на sqlite: [<code>code/nl_to_sql.py</code>](../code/nl_to_sql.py).</p><ul><li><code>SEMANTIC</code> — метрики (стем-триггеры → SQL) и измерения;</li><li><code>nl_to_sql(question)</code> — собрать SQL (метрика + опц. GROUP BY);</li><li><code>run_sql(sql, rows)</code> — выполнить на in-memory sqlite (stdlib).</li></ul><pre><code>python code/nl_to_sql.py\npytest code -q</code></pre><p>Тест прогоняет «выручка по регионам» → <code>SELECT region, SUM(amount) ... GROUP BY region</code> и реально считает на sqlite. Матч наивный (по словам) — настоящий парсинг делает LLM, но <strong>семантический слой остаётся</strong>.</p><h2>USE IT</h2><p>Запросы к БД на естественном языке (мульти-провайдер): LLM генерирует SQL по схеме + семантике, ты исполняешь и проверяешь.</p><pre><code>schema = \"Таблица sales(region, month, amount). Метрики: выручка=SUM(amount).\"\nsql = Anthropic().messages.create(model=\"claude-sonnet-4-6\", max_tokens=200,\n    messages=[{\"role\": \"user\", \"content\": f\"{schema}\\nSQL для: выручка по месяцам\"}]).content[0].text\n# OpenAI / Gemini — аналогично; затем выполнить sql в своей БД и сверить</code></pre><p>Лучше — готовые слои: <strong>dbt Semantic Layer</strong> (метрики раз и навсегда), а инструменты (Genie/Spotter) дают NL→SQL поверх них. ⚠️ Всегда показывай и проверяй сгенерированный SQL.</p><h2>SHIP IT</h2><p><strong>Артефакт:</strong> Семантический слой проекта → [<code>outputs/semantic-layer.md</code>](../outputs/semantic-layer.md)</p><p>Шаблон описания метрик и измерений (имя · определение · SQL · синонимы-триггеры) под свой проект — основа для надёжного NL→SQL и BI-агентов (5.4).</p><h2>Материалы</h2><ul><li><a href=\"https://docs.getdbt.com/docs/use-dbt-semantic-layer/dbt-sl\" target=\"_blank\" rel=\"noopener\">dbt — Semantic Layer</a> — определять метрики централизованно.</li><li><a href=\"https://docs.getdbt.com/docs/build/metrics-overview\" target=\"_blank\" rel=\"noopener\">dbt — Creating metrics</a> — типы метрик и их описание.</li><li><a href=\"https://arxiv.org/abs/1809.08887\" target=\"_blank\" rel=\"noopener\">Yu et al., 2018 — Spider (text-to-SQL)</a> — бенчмарк и сложность NL→SQL.</li></ul><hr><p><strong>Часы:</strong> ~4 · <strong>DoD:</strong> <code>pytest code -q</code> зелёный, демо запускается, ru.md заполнен. ✅ <strong>Урок готов</strong></p>"},{"id":"5.4","title":"BI-агенты","status":"done","motto":"BI-агент не отвечает одним запросом, а ведёт расследование: обзор → разбивка → причина.","hours":4,"dir":"phases/05-data-and-analytics/05-bi-agents","html":"<h1>Урок 5.4 · BI-агенты</h1><p><strong>Фаза 5 — Данные и аналитика</strong> · <strong>Результат фазы:</strong> Анализировать данные с ИИ, делать NL→SQL и автоотчёты с валидацией результатов.</p><blockquote><strong>MOTTO.</strong> BI-агент не отвечает одним запросом, а ведёт расследование: обзор → разбивка → причина.</blockquote><h2>PROBLEM</h2><p>На вопрос «почему упала выручка?» один SQL не ответит — нужно <strong>расследование</strong>: посмотреть общий тренд, разбить по измерениям, найти сегмент-виновник, копнуть глубже. BI-агент планирует и исполняет такую цепочку шагов сам. Соберём планировщик, чтобы понять, как агент выбирает следующий шаг.</p><h2>CONCEPT</h2><pre><code>вопрос → policy выбирает шаг → исполнить → наблюдение → следующий шаг\n   overview → breakdown(по измерению) → top_segment(доля) → [drill-down]</code></pre><p>Это агентный цикл (reason→act→observe из Фазы 7), но над аналитикой: каждый шаг сужает поиск к причине. Policy здесь детерминированная; в проде шаги выбирает LLM.</p><h2>BUILD IT</h2><p>BI-агент-планировщик: [<code>code/bi_agent.py</code>](../code/bi_agent.py).</p><ul><li><code>investigate(rows, dimension, value)</code> — план: <code>overview</code> (итог) → <code>breakdown</code> (по измерению) → <code>top_segment</code> (сегмент + доля).</li></ul><pre><code>python code/bi_agent.py\npytest code -q</code></pre><p>Демо на продажах: итог 270 → разбивка по регионам → топ-сегмент «Москва» (доля 0.556). Видно, как агент от общего идёт к конкретному — это и есть расследование.</p><h2>USE IT</h2><p>Готовые BI-агенты (обзор и практика):</p><ul><li><strong>Databricks AI/BI Genie</strong> — NL-вопросы к данным в Unity Catalog: возвращает SQL, таблицу и график, объясняет ход; есть Managed MCP.</li><li><strong>ThoughtSpot Spotter</strong> — агент-аналитик поверх семантической модели (токены вместо прямой генерации SQL), human-in-the-loop коучинг.</li><li><strong>Hex</strong> — Notebook Agent: NL→SQL/Python в ноутбуке с диффом и ревью ячеек.</li></ul><p>Общее: семантический слой (5.3) + планирование шагов + объяснимость и governance. ⚠️ Агент ускоряет, но выводы проверяет человек (5.5).</p><h2>SHIP IT</h2><p><strong>Артефакт:</strong> Сценарий BI-расследования → [<code>outputs/bi-investigation-playbook.md</code>](../outputs/bi-investigation-playbook.md)</p><p>Плейбук расследования (обзор → разрезы → гипотезы → проверка → вывод) под типовой бизнес-вопрос. Дальше: валидация результатов и автоотчёты (5.5).</p><h2>Материалы</h2><ul><li><a href=\"https://docs.databricks.com/aws/en/genie/\" target=\"_blank\" rel=\"noopener\">Databricks — AI/BI Genie</a> — разговорная аналитика над данными.</li><li><a href=\"https://www.thoughtspot.com/product/agents/spotter\" target=\"_blank\" rel=\"noopener\">ThoughtSpot — Spotter</a> — агент-аналитик с governance.</li><li><a href=\"https://learn.hex.tech/docs/getting-started/ai-overview\" target=\"_blank\" rel=\"noopener\">Hex — AI overview</a> — AI-агенты в ноутбуке данных.</li></ul><hr><p><strong>Часы:</strong> ~4 · <strong>DoD:</strong> <code>pytest code -q</code> зелёный, демо запускается, ru.md заполнен. ✅ <strong>Урок готов</strong></p>"},{"id":"5.5","title":"Валидация, аномалии, автоотчёты","status":"done","motto":"Цифру от ИИ нельзя показывать непроверенной: сначала валидация и поиск аномалий, потом отчёт.","hours":4,"dir":"phases/05-data-and-analytics/05-validation-anomalies-reports","html":"<h1>Урок 5.5 · Валидация, аномалии, автоотчёты</h1><p><strong>Фаза 5 — Данные и аналитика</strong> · <strong>Результат фазы:</strong> Анализировать данные с ИИ, делать NL→SQL и автоотчёты с валидацией результатов.</p><blockquote><strong>MOTTO.</strong> Цифру от ИИ нельзя показывать непроверенной: сначала валидация и поиск аномалий, потом отчёт.</blockquote><h2>PROBLEM</h2><p>ИИ-аналитик уверенно выдаёт числа — но мог взять не ту метрику, потерять строки или не заметить выброс. Если такой результат уйдёт в отчёт, цена ошибки высокая. Нужен <strong>контроль качества</strong>: проверки на вменяемость, детекция аномалий и сборка отчёта — автоматически, перед показом.</p><h2>CONCEPT</h2><pre><code>результат анализа\n   │ validate_result (правила: неотрицательно, суммы сходятся, нет пропусков)\n   │ find_anomalies  (z-score: что выбивается из ряда)\n   ▼\nbuild_report → отчёт с метриками и флагами → (по расписанию)</code></pre><p>Это «фактчекинг для данных» (мост к Фазе 11). Автоотчёт = валидация + аномалии + сборка, запускаемые регулярно.</p><h2>BUILD IT</h2><p>Валидация + детекция аномалий + сборка отчёта: [<code>code/validation.py</code>](../code/validation.py).</p><ul><li><code>validate_result(metrics, rules)</code> — проверка по правилам-предикатам;</li><li><code>find_anomalies(series, z)</code> — выбросы по z-score (stdlib statistics);</li><li><code>build_report(title, metrics, anomalies)</code> — markdown-отчёт.</li></ul><pre><code>python code/validation.py\npytest code -q</code></pre><p>Демо ловит выброс 500 в ряду (z=2.23) и собирает отчёт. Правила валидации — твои бизнес-инварианты (выручка ≥ 0, заказы &gt; 0 и т.п.).</p><h2>USE IT</h2><p>Автоотчёт по расписанию (мульти-провайдер): пайплайн «данные → анализ → валидация → аномалии → отчёт», запускаемый по cron / в оркестраторе, с уведомлением при флагах.</p><pre><code>metrics = run_daily_analysis()                 # NL→SQL (5.3) / агент (5.4)\nproblems = validate_result(metrics, RULES)\nanomalies = find_anomalies(series)\nreport = build_report(\"Дневной отчёт\", metrics, anomalies)\nif problems or anomalies:\n    notify(report)                             # алерт вместо «тихого» отчёта</code></pre><p>В проде источник данных подключают через <strong>MCP-коннектор к БД</strong> (Фаза 6), а расписание — через планировщик (Genie/dbt jobs/cron).</p><h2>SHIP IT</h2><p><strong>Артефакт:</strong> Аналитический агент + (спека) MCP-коннектора к БД → [<code>outputs/analytics_agent.py</code>](../outputs/analytics_agent.py)</p><p>Готовый агент: SQL-выгрузка (sqlite) → валидация → аномалии → отчёт, плюс заметка о подключении БД через MCP (соберём в Фазе 6). Это итог фазы: сквозной пайплайн данные → анализ → проверка → отчёт.</p><h2>Материалы</h2><ul><li><a href=\"https://scikit-learn.org/stable/modules/outlier_detection.html\" target=\"_blank\" rel=\"noopener\">scikit-learn — Outlier detection</a> — методы детекции аномалий за пределами z-score.</li><li><a href=\"https://docs.claude.com/en/docs/test-and-evaluate/strengthen-guardrails/reduce-hallucinations\" target=\"_blank\" rel=\"noopener\">Anthropic — Reduce hallucinations</a> — проверяемость и «не доверяй вслепую».</li><li><a href=\"https://docs.databricks.com/aws/en/genie/\" target=\"_blank\" rel=\"noopener\">Databricks — AI/BI Genie</a> — автоматизация и MCP-доступ к данным.</li></ul><hr><p><strong>Часы:</strong> ~4 · <strong>DoD:</strong> <code>pytest code -q</code> зелёный, демо запускается, ru.md заполнен. ✅ <strong>Урок готов</strong></p>"}],"hours":18,"outcome":"Анализировать данные с ИИ, делать NL→SQL и автоотчёты с валидацией результатов."},{"id":6,"name":"Инструменты и протоколы (MCP)","lessons":[{"id":"6.1","title":"Tool use / function calling изнутри","status":"done","motto":"Модель не вызывает функции сама — она просит вызвать; протокол — это схемы, toolcall и toolresult.","hours":4,"dir":"phases/06-tools-and-protocols-mcp/06-tool-use-function-calling","html":"<h1>Урок 6.1 · Tool use / function calling изнутри</h1><p><strong>Фаза 6 — Инструменты и протоколы (MCP)</strong> · <strong>Результат фазы:</strong> Объяснить tool use изнутри и поднять собственный MCP-сервер с контролем доступа.</p><blockquote><strong>MOTTO.</strong> Модель не вызывает функции сама — она просит вызвать; протокол — это схемы, tool_call и tool_result.</blockquote><h2>PROBLEM</h2><p>«Модель сходила в базу и посчитала» — звучит как магия, но модель не исполняет код. Она лишь возвращает <strong>tool_call</strong> (имя инструмента + аргументы по JSON-схеме), а исполняет <strong>твой</strong> код, и результат (<strong>tool_result</strong>) уходит обратно в модель. Не понимая этот протокол, нельзя ни отладить tool use, ни безопасно подключить инструменты.</p><h2>CONCEPT</h2><pre><code>схемы инструментов → модель → tool_call(name, args)\n                                   │ твой диспетчер: валидирует по схеме, исполняет\n                                   ▼\n                              tool_result ──► модель ──► финал</code></pre><p>Три кита: <strong>схема</strong> (что за инструмент и аргументы), <strong>tool_call</strong> (запрос модели), <strong>tool_result</strong> (ответ твоего кода). Это фундамент, на котором стоит MCP (6.2+).</p><h2>BUILD IT</h2><p>Цикл function-calling с нуля: [<code>code/function_calling.py</code>](../code/function_calling.py).</p><ul><li><code>Tool(name, description, schema, fn)</code> — инструмент с JSON-схемой;</li><li><code>validate_args(args, schema)</code> — проверка аргументов по схеме;</li><li><code>dispatch(tool_call, registry)</code> — валидировать и исполнить;</li><li><code>run(model_fn, registry)</code> — цикл tool_call → tool_result → final.</li></ul><pre><code>python code/function_calling.py\npytest code -q</code></pre><p>Модель здесь — детерминированный stub (офлайн); меняешь его на LLM — протокол тот же. Видно, что «агентность» начинается с этого цикла.</p><h2>USE IT</h2><p>Тот же протокол — через API провайдера (мульти-провайдер):</p><pre><code>tools = [{\"name\": \"add\", \"description\": \"сложить\",\n          \"input_schema\": {\"type\": \"object\", \"properties\": {\"a\": {\"type\": \"number\"},\n          \"b\": {\"type\": \"number\"}}, \"required\": [\"a\", \"b\"]}}]\n# Anthropic: messages.create(..., tools=tools) → блок tool_use → выполнить → tool_result\n# OpenAI:    responses.create(..., tools=[{\"type\":\"function\", ...}]) → tool call → role:\"tool\"\n# Google:    function_declarations в config → functionCall → functionResponse</code></pre><p>Форма различается синтаксисом, но цикл один: схема → запрос модели → твой код → результат назад.</p><h2>SHIP IT</h2><p><strong>Артефакт:</strong> Описание набора инструментов (схемы) → [<code>outputs/tool-schemas.md</code>](../outputs/tool-schemas.md)</p><p>Готовые JSON-схемы для типового тулсета (read/search/calc) + правила описания (имя, описание-триггер, строгие типы). Дальше: стандартизуем доступ к инструментам через MCP (6.2).</p><h2>Материалы</h2><ul><li><a href=\"https://docs.anthropic.com/en/docs/agents-and-tools/tool-use/overview\" target=\"_blank\" rel=\"noopener\">Anthropic — Tool use</a> — tool use в Claude изнутри.</li><li><a href=\"https://platform.openai.com/docs/guides/function-calling\" target=\"_blank\" rel=\"noopener\">OpenAI — Function calling</a> — схемы и цикл tool calling.</li><li><a href=\"https://json-schema.org/\" target=\"_blank\" rel=\"noopener\">JSON Schema</a> — как описывать параметры инструментов.</li></ul><hr><p><strong>Часы:</strong> ~4 · <strong>DoD:</strong> <code>pytest code -q</code> зелёный, демо запускается, ru.md заполнен. ✅ <strong>Урок готов</strong></p>"},{"id":"6.2","title":"Архитектура MCP","status":"done","motto":"MCP — это USB-C между ИИ и миром: один протокол, три примитива (tools/resources/prompts).","hours":4,"dir":"phases/06-tools-and-protocols-mcp/06-mcp-architecture","html":"<h1>Урок 6.2 · Архитектура MCP</h1><p><strong>Фаза 6 — Инструменты и протоколы (MCP)</strong> · <strong>Результат фазы:</strong> Объяснить tool use изнутри и поднять собственный MCP-сервер с контролем доступа.</p><blockquote><strong>MOTTO.</strong> MCP — это USB-C между ИИ и миром: один протокол, три примитива (tools/resources/prompts).</blockquote><h2>PROBLEM</h2><p>В уроке 6.1 инструменты были «вшиты» в приложение. Но у каждого приложения свои инструменты, и переиспользовать их между Claude/Cursor/агентами нельзя. <strong>MCP</strong> стандартизует подключение: один протокол, через который любой клиент находит и зовёт инструменты любого сервера — как USB для периферии.</p><h2>CONCEPT</h2><pre><code>   Host (приложение с LLM)\n     └─ Client ⇄ Server     (1 client ↔ 1 server, JSON-RPC 2.0)\n                   ├─ tools      действия (как POST)\n                   ├─ resources  данные (как GET)\n                   └─ prompts    заготовки-шаблоны\n        discovery (list) → invoke (call)</code></pre><ul><li><strong>Host</strong> — приложение (Claude Desktop, IDE, агент). <strong>Client</strong> — коннектор внутри host (1:1 к серверу). <strong>Server</strong> — отдаёт три примитива.</li><li>Поток: клиент <strong>перечисляет</strong> возможности (discovery), затем <strong>вызывает</strong> (invoke).</li></ul><h2>BUILD IT</h2><p>In-process модель host↔client↔server с тремя примитивами: [<code>code/mcp_architecture.py</code>](../code/mcp_architecture.py).</p><ul><li><code>MCPServer</code> — <code>add_tool/add_resource/add_prompt</code>, <code>list_*</code> (discovery), <code>call_tool/read_resource/get_prompt</code> (invoke);</li><li><code>MCPClient</code> — <code>discover()</code> и <code>call()</code>.</li></ul><pre><code>python code/mcp_architecture.py\npytest code -q</code></pre><p>Это «MCP без транспорта»: видно роли и три примитива. Транспорт (JSON-RPC) добавим в 6.3.</p><h2>USE IT</h2><p>Подключение готовых MCP-коннекторов (мульти-платформа): десятки серверов уже есть (файлы, БД, GitHub, поиск).</p><ul><li><strong>Claude Desktop / Claude Code</strong> — добавить сервер в конфиг MCP; клиент сам сделает discovery.</li><li><strong>Cursor / другие host'ы</strong> — тот же протокол, свои настройки.</li><li>Реестр примеров — <code>modelcontextprotocol/servers</code> (референс-реализации).</li></ul><p>Один сервер работает в любом MCP-совместимом host — в этом и смысл стандарта.</p><h2>SHIP IT</h2><p><strong>Артефакт:</strong> Карта MCP под свой стек → [<code>outputs/mcp-map.md</code>](../outputs/mcp-map.md)</p><p>Карта: какие данные/инструменты вынести в свои MCP-серверы, какие готовые коннекторы подключить, что к какому host'у. Дальше: поднимаем свой сервер (6.3).</p><h2>Материалы</h2><ul><li><a href=\"https://modelcontextprotocol.io/specification/2025-11-25\" target=\"_blank\" rel=\"noopener\">MCP — Specification</a> — протокол, примитивы, capability negotiation.</li><li><a href=\"https://www.anthropic.com/news/model-context-protocol\" target=\"_blank\" rel=\"noopener\">Anthropic — Introducing MCP</a> — зачем нужен стандарт.</li><li><a href=\"https://github.com/modelcontextprotocol/servers\" target=\"_blank\" rel=\"noopener\">modelcontextprotocol/servers</a> — готовые серверы-примеры.</li></ul><hr><p><strong>Часы:</strong> ~4 · <strong>DoD:</strong> <code>pytest code -q</code> зелёный, демо запускается, ru.md заполнен. ✅ <strong>Урок готов</strong></p>"},{"id":"6.3","title":"Свой MCP-сервер на FastMCP","status":"done","motto":"MCP-сервер — это обработчик JSON-RPC: `initialize`, `tools/list`, `tools/call`.","hours":4,"dir":"phases/06-tools-and-protocols-mcp/06-build-mcp-server-fastmcp","html":"<h1>Урок 6.3 · Свой MCP-сервер на FastMCP</h1><p><strong>Фаза 6 — Инструменты и протоколы (MCP)</strong> · <strong>Результат фазы:</strong> Объяснить tool use изнутри и поднять собственный MCP-сервер с контролем доступа.</p><blockquote><strong>MOTTO.</strong> MCP-сервер — это обработчик JSON-RPC: <code>initialize</code>, <code>tools/list</code>, <code>tools/call</code>.</blockquote><h2>PROBLEM</h2><p>Чтобы понять, что делает FastMCP «по волшебству» за пару декораторов, надо увидеть протокол под капотом. MCP — это JSON-RPC 2.0: клиент шлёт <code>initialize</code>, <code>tools/list</code>, <code>tools/call</code>, сервер отвечает <code>result</code>/<code>error</code>. Соберём минимальный сервер сами, потом запустим то же на FastMCP.</p><h2>CONCEPT</h2><pre><code>client → {\"jsonrpc\":\"2.0\",\"id\":1,\"method\":\"initialize\"}        → result: serverInfo, capabilities\nclient → {\"method\":\"tools/list\"}                                → result: [{name, inputSchema}]\nclient → {\"method\":\"tools/call\",\"params\":{name, arguments}}     → result: {content:[...]}  | error</code></pre><p>Коды ошибок JSON-RPC: <code>-32601</code> метод не найден, <code>-32602</code> неверные параметры, <code>-32603</code> внутренняя ошибка. Транспорт — STDIO (локально) или Streamable HTTP (удалённо).</p><h2>BUILD IT</h2><p>Мини MCP-сервер на JSON-RPC, без зависимостей: [<code>code/mini_mcp_server.py</code>](../code/mini_mcp_server.py).</p><ul><li><code>MiniMCP</code> + декоратор <code>@server.tool(name, description, input_schema)</code>;</li><li><code>handle(request)</code> — обрабатывает <code>initialize</code> / <code>tools/list</code> / <code>tools/call</code>, отдаёт корректный JSON-RPC.</li></ul><pre><code>python code/mini_mcp_server.py\npytest code -q</code></pre><p>Это «FastMCP вручную»: видно, какие сообщения он формирует. Теперь то же — через настоящий SDK.</p><h2>USE IT</h2><p>То же на <strong>FastMCP</strong> (официальный SDK <code>mcp</code>), транспорты STDIO и Streamable HTTP:</p><pre><code>from mcp.server.fastmcp import FastMCP\nmcp = FastMCP(\"Demo\")\n\n@mcp.tool()\ndef add(a: int, b: int) -&gt; int:\n    \"\"\"Сложить два числа\"\"\"\n    return a + b\n\nif __name__ == \"__main__\":\n    mcp.run(transport=\"streamable-http\")   # или STDIO по умолчанию</code></pre><p>FastMCP сам строит схемы из type hints и docstring и обрабатывает JSON-RPC — но это ровно то, что мы написали руками.</p><h2>SHIP IT</h2><p><strong>Артефакт:</strong> Рабочий MCP-сервер → [<code>outputs/mini_mcp_server.py</code>](../outputs/mini_mcp_server.py)</p><p>Минимальный сервер (echo + add) с корректным JSON-RPC — основа, к которой добавим тесты/подключение (6.4) и контроль доступа (6.5).</p><h2>Материалы</h2><ul><li><a href=\"https://modelcontextprotocol.io/docs/develop/build-server\" target=\"_blank\" rel=\"noopener\">MCP — Build an MCP server</a> — пошаговый гайд.</li><li><a href=\"https://github.com/modelcontextprotocol/python-sdk\" target=\"_blank\" rel=\"noopener\">modelcontextprotocol/python-sdk</a> — официальный SDK с FastMCP.</li><li><a href=\"https://gofastmcp.com\" target=\"_blank\" rel=\"noopener\">FastMCP</a> — расширенный фреймворк поверх SDK.</li></ul><hr><p><strong>Часы:</strong> ~4 · <strong>DoD:</strong> <code>pytest code -q</code> зелёный, демо запускается, ru.md заполнен. ✅ <strong>Урок готов</strong></p>"},{"id":"6.4","title":"Тест и подключение","status":"done","motto":"Сначала прогони сервер инспектором, потом подключай к клиенту — не наоборот.","hours":4,"dir":"phases/06-tools-and-protocols-mcp/06-testing-and-connecting","html":"<h1>Урок 6.4 · Тест и подключение</h1><p><strong>Фаза 6 — Инструменты и протоколы (MCP)</strong> · <strong>Результат фазы:</strong> Объяснить tool use изнутри и поднять собственный MCP-сервер с контролем доступа.</p><blockquote><strong>MOTTO.</strong> Сначала прогони сервер инспектором, потом подключай к клиенту — не наоборот.</blockquote><h2>PROBLEM</h2><p>«Подключил сервер к Claude, не работает» — и непонятно, где сломалось: в сервере, транспорте или конфиге. Поэтому сервер сначала <strong>тестируют</strong> изолированно (MCP Inspector), проверяя протокол, и только потом подключают к клиенту. Соберём мини-инспектор, чтобы понять, что именно он проверяет.</p><h2>CONCEPT</h2><pre><code>сервер handle(request)\n   │ инспектор гоняет протокол:\n   ├─ initialize      → есть serverInfo, protocolVersion?\n   ├─ tools/list      → список, у инструментов name + inputSchema?\n   ├─ неизвестный метод → корректный error -32601?\n   └─ tools/call      → result с content?\n   ▼\nотчёт: конформен / есть проблемы → потом подключение к клиенту</code></pre><h2>BUILD IT</h2><p>Мини-инспектор протокола, без зависимостей: [<code>code/mcp_inspector.py</code>](../code/mcp_inspector.py).</p><ul><li><code>inspect(handle, call=None)</code> — прогоняет initialize/tools-list/неизвестный метод/(опц.) вызов и собирает отчёт по проверкам;</li><li><code>demo_handle</code> — минимально конформный сервер для демо.</li></ul><pre><code>python code/mcp_inspector.py\npytest code -q</code></pre><p>Инспектор работает с любым сервером вида <code>handle(request)-&gt;response</code> (в т.ч. с <code>MiniMCP</code> из 6.3). Тест проверяет: конформный сервер проходит, сломанный — нет.</p><h2>USE IT</h2><p>Готовый MCP Inspector и подключение к клиенту:</p><pre><code>npx @modelcontextprotocol/inspector node build/index.js   # UI на localhost:6274\nmcp dev server.py                                          # быстрый запуск инспектора (Python SDK)\nmcp install server.py                                      # установить сервер в Claude Desktop</code></pre><ul><li><strong>MCP Inspector</strong> — вкладки Tools/Resources/Prompts/Logs; видно реальные запросы/ответы.</li><li><strong>Claude Desktop / Cursor</strong> — добавить сервер в конфиг; клиент сделает discovery и покажет инструменты.</li></ul><h2>SHIP IT</h2><p><strong>Артефакт:</strong> Документированный сервер с примерами → [<code>outputs/mcp-server-guide.md</code>](../outputs/mcp-server-guide.md)</p><p>Гайд: как протестировать сервер инспектором, как подключить к Claude Desktop/Cursor, примеры запросов и типовые ошибки. Дальше: безопасность и доступ (6.5).</p><h2>Материалы</h2><ul><li><a href=\"https://modelcontextprotocol.io/docs/tools/inspector\" target=\"_blank\" rel=\"noopener\">MCP — Inspector</a> — визуальный тест-инструмент.</li><li><a href=\"https://github.com/modelcontextprotocol/inspector\" target=\"_blank\" rel=\"noopener\">modelcontextprotocol/inspector</a> — исходники и CLI-режим.</li><li><a href=\"https://modelcontextprotocol.io/docs/develop/build-server\" target=\"_blank\" rel=\"noopener\">MCP — Build an MCP server</a> — установка сервера в Claude Desktop.</li></ul><hr><p><strong>Часы:</strong> ~4 · <strong>DoD:</strong> <code>pytest code -q</code> зелёный, демо запускается, ru.md заполнен. ✅ <strong>Урок готов</strong></p>"},{"id":"6.5","title":"Безопасность и доступ","status":"done","motto":"Между моделью и действием всегда стоит авторизация: модель просит — сервер решает, можно ли.","hours":4,"dir":"phases/06-tools-and-protocols-mcp/06-security-and-access","html":"<h1>Урок 6.5 · Безопасность и доступ</h1><p><strong>Фаза 6 — Инструменты и протоколы (MCP)</strong> · <strong>Результат фазы:</strong> Объяснить tool use изнутри и поднять собственный MCP-сервер с контролем доступа.</p><blockquote><strong>MOTTO.</strong> Между моделью и действием всегда стоит авторизация: модель просит — сервер решает, можно ли.</blockquote><h2>PROBLEM</h2><p>MCP-сервер даёт модели руки: файлы, БД, отправка писем. Если сервер слепо исполняет всё, что просит модель, одна prompt-инъекция — и <code>delete_file('/')</code> или утечка секретов. Нужен <strong>слой доступа</strong>: кто (principal) что (tool) над чем (args) может, плюс изоляция и аудит.</p><h2>CONCEPT</h2><pre><code>модель → tool_call → [ авторизация ] → исполнение\n                         │ principal: какие инструменты разрешены\n                         │ право на запись (read-only по умолчанию)\n                         │ валидация аргументов (path traversal, scope)\n                         ▼ deny + audit, если нельзя</code></pre><p>Принципы: <strong>least privilege</strong> (минимум прав), <strong>read-only по умолчанию</strong>, <strong>валидация входов</strong>, <strong>аудит</strong>. MCP в проде использует OAuth 2.1 для HTTP-транспорта; для STDIO — секреты из окружения.</p><h2>BUILD IT</h2><p>Слой контроля доступа, без зависимостей: [<code>code/mcp_security.py</code>](../code/mcp_security.py).</p><ul><li><code>Principal(name, allowed_tools, can_write)</code> — кто и что может;</li><li><code>check_access(principal, tool, args)</code> — разрешить/запретить + причина;</li><li><code>is_safe_path(path)</code> — защита от path traversal;</li><li><code>guarded_call(...)</code> — проверка + вызов + запись в аудит.</li></ul><pre><code>python code/mcp_security.py\npytest code -q</code></pre><p>Тесты ловят реальные угрозы: вызов неразрешённого инструмента, запись без прав, <code>../../etc/passwd</code>.</p><h2>USE IT</h2><p>Что нельзя отдавать модели — практика (мульти-платформа):</p><ul><li><strong>Не давай write/delete по умолчанию</strong> — read-only, запись только явным principal'ам.</li><li><strong>Не клади секреты в ответы инструментов</strong> и в схемы — модель их «запомнит» и может разгласить.</li><li><strong>Минимизируй scope токенов</strong> — не <code>db:*</code>/<code>admin:*</code>, а точечные права (Rich Authorization Requests).</li><li><strong>HTTP-транспорт → OAuth 2.1</strong>: сервер как Resource Server, проверка audience токена; STDIO → секреты из env.</li><li><strong>Санитизируй входы</strong> и опасайся «confused deputy» у прокси-серверов.</li></ul><p>⚠️ Связка с prompt-инъекциями (Фаза 11): данные из инструментов могут содержать вредоносные инструкции.</p><h2>SHIP IT</h2><p><strong>Артефакт:</strong> Чек-лист безопасности MCP → [<code>outputs/mcp-security-checklist.md</code>](../outputs/mcp-security-checklist.md)</p><p>Чек-лист перед выкладкой сервера: права, изоляция, секреты, токены, валидация, аудит. Завершает Фазу 6 — сервер с контролем доступа.</p><h2>Материалы</h2><ul><li><a href=\"https://modelcontextprotocol.io/docs/tutorials/security/security_best_practices\" target=\"_blank\" rel=\"noopener\">MCP — Security best practices</a> — риски и меры (в т.ч. confused deputy).</li><li><a href=\"https://modelcontextprotocol.io/specification/2025-11-25\" target=\"_blank\" rel=\"noopener\">MCP — Authorization (spec)</a> — OAuth 2.1, Resource Indicators.</li><li><a href=\"https://www.redhat.com/en/blog/model-context-protocol-mcp-understanding-security-risks-and-controls\" target=\"_blank\" rel=\"noopener\">Red Hat — MCP security risks and controls</a> — практический разбор рисков.</li></ul><hr><p><strong>Часы:</strong> ~4 · <strong>DoD:</strong> <code>pytest code -q</code> зелёный, демо запускается, ru.md заполнен. ✅ <strong>Урок готов</strong></p>"}],"hours":18,"outcome":"Объяснить tool use изнутри и поднять собственный MCP-сервер с контролем доступа."},{"id":7,"name":"Agent Engineering","lessons":[{"id":"7.1","title":"Цикл reason → act → observe","status":"done","motto":"Агент — это не модель, а цикл: подумал → сделал → посмотрел на результат → повторил.","hours":5,"dir":"phases/07-agent-engineering/07-reason-act-observe-loop","html":"<h1>Урок 7.1 · Цикл reason → act → observe</h1><p><strong>Фаза 7 — Agent Engineering</strong> · <strong>Результат фазы:</strong> Собрать надёжного агента с памятью, планированием, human-in-the-loop и guardrails.</p><blockquote><strong>MOTTO.</strong> Агент — это не модель, а цикл: подумал → сделал → посмотрел на результат → повторил.</blockquote><h2>PROBLEM</h2><p>«Агент» звучит как магия, пока не видишь цикл изнутри. Без этого нельзя ни отладить зависание, ни понять, почему агент крутится в петле или зовёт несуществующий инструмент. А ещё кажется, что для агента обязательно нужен дорогой LLM и API-ключ — на самом деле сам механизм от модели не зависит.</p><h2>CONCEPT</h2><p>Любой агент — это петля из трёх шагов вокруг <strong>policy</strong> (того, кто принимает решение) и набора <strong>инструментов</strong> (того, что делает руками):</p><pre><code>        ┌──────────────────────────────────────┐\n        │                                       │\n   goal ▼            (reason)                    │\n        policy ── Action(tool, args) ──► tool ──┘\n        ▲                                  │\n        │            (observe)             │ (act)\n        └────────── observation ◄──────────┘\n                         │\n              Action(final) ──► ответ</code></pre><p>Policy на каждом витке смотрит на цель и историю и выбирает: вызвать инструмент или дать финальный ответ. Результат инструмента (<code>observation</code>) возвращается в историю — и это <strong>память</strong> агента в простейшем виде. Петля повторяется до финала или до лимита шагов.</p><h2>BUILD IT</h2><p>Цикл реализован с нуля, без зависимостей: [<code>code/agent_loop.py</code>](../code/agent_loop.py).</p><ul><li><code>run_agent(goal, tools, policy, max_steps)</code> — сам цикл reason→act→observe;</li><li><code>Action</code> — единая форма решения (<code>tool</code> или <code>final</code>);</li><li><code>RuleBasedPolicy</code> — детерминированная «модель» (чтобы урок шёл офлайн);</li><li><strong>guardrails</strong> уже встроены: лимит шагов (петля → <code>AgentError</code>) и проверка неизвестного инструмента.</li></ul><p>Запуск демо и тестов:</p><pre><code>python code/agent_loop.py\npytest code -q</code></pre><p>Демо решает <code>(2 + 3) * 4 - 1</code> за три вызова инструментов — видно, как результат каждого шага становится входом следующего.</p><h2>USE IT</h2><p>То же самое — с настоящей моделью. Меняется <strong>только policy</strong>: вместо правил её роль играет LLM через function calling, а форма <code>Action</code> остаётся прежней. &lt;!-- <strong>Requires:</strong> платный API-ключ провайдера --&gt;</p><pre><code># набросок LLM-policy под тот же run_agent(...)\ndef llm_policy(goal, history):\n    resp = client.messages.create(\n        model=\"claude-haiku-4-5\",          # дешёвая модель на роль «рассуждателя»\n        tools=TOOL_SCHEMAS,                 # схемы add/sub/mul\n        messages=build_messages(goal, history),\n    )\n    block = resp.content[0]\n    if block.type == \"tool_use\":\n        return Action(\"tool\", block.name, tuple(block.input.values()))\n    return Action(\"final\", answer=block.text)</code></pre><p>Вывод урока: «агентность» — это инженерия цикла и инструментов, а не сама модель. Модель — заменяемая деталь (мост к Фазе 9: на роль policy берут модель подешевле).</p><h2>SHIP IT</h2><p><strong>Артефакт:</strong> <code>agent-loop</code> (skill) → [<code>outputs/skill-agent-loop.md</code>](../outputs/skill-agent-loop.md)</p><p>Переиспользуемый каркас цикла с guardrails: подставляешь свои инструменты и policy. Дальше в фазе наращивается память и планирование (7.2), human-in-the-loop (7.3), разбор поломок (7.4).</p><h2>Материалы</h2><ul><li><a href=\"https://arxiv.org/abs/2210.03629\" target=\"_blank\" rel=\"noopener\">ReAct: Reasoning + Acting (arXiv 2210.03629)</a> — первоисточник цикла reason→act→observe.</li><li><a href=\"https://www.anthropic.com/research/building-effective-agents\" target=\"_blank\" rel=\"noopener\">Anthropic — Building Effective Agents</a> — агент как LLM с инструментами в цикле; когда он вообще нужен.</li><li><a href=\"https://www.anthropic.com/engineering/writing-tools-for-agents\" target=\"_blank\" rel=\"noopener\">Anthropic — Writing effective tools for agents</a> — как проектировать инструменты и интерфейс агент↔среда.</li></ul><hr><p><strong>Часы:</strong> ~5 · <strong>DoD:</strong> <code>pytest code -q</code> зелёный (5 тестов), демо запускается, ru.md заполнен. ✅ <strong>Урок готов</strong> (эталон уровня «агенты»).</p>"},{"id":"7.2","title":"Память и планирование агента","status":"done","motto":"Надёжный агент не импровизирует на каждом шаге: он строит план и несёт состояние в памяти.","hours":5,"dir":"phases/07-agent-engineering/07-agent-memory-and-planning","html":"<h1>Урок 7.2 · Память и планирование агента</h1><p><strong>Фаза 7 — Agent Engineering</strong> · <strong>Результат фазы:</strong> Собрать надёжного агента с памятью, планированием, human-in-the-loop и guardrails.</p><blockquote><strong>MOTTO.</strong> Надёжный агент не импровизирует на каждом шаге: он строит план и несёт состояние в памяти.</blockquote><h2>PROBLEM</h2><p>Голый цикл reason→act→observe (7.1) решает простое, но на многошаговой задаче спотыкается: забывает промежуточные результаты и теряет нить. Двух вещей не хватает — <strong>плана</strong> (разбить цель на шаги, как в 2.3) и <strong>памяти</strong> (носить состояние между шагами, как в 4.2). Соединим их в агенте.</p><h2>CONCEPT</h2><pre><code>цель → plan() → [шаг1, шаг2, шаг3]\n                   │ каждый шаг:\n                   │   recall(value)  ← читаем из памяти\n                   │   tool(...)      ← действуем\n                   │   remember(value) → пишем в память\n                   ▼\n              итог + трейс шагов</code></pre><p>Память превращает цепочку шагов в связный процесс: шаг N видит результат шага N−1. Это мост от «промпт-цепочки» (2.3) к автономному агенту.</p><h2>BUILD IT</h2><p>Планировщик + рабочая память поверх цикла из 7.1: [<code>code/planner_agent.py</code>](../code/planner_agent.py).</p><ul><li><code>Memory</code> — <code>remember/recall</code> (рабочая память агента);</li><li><code>plan(goal)</code> — декомпозиция цели в шаги;</li><li><code>run(goal)</code> — исполнить план, неся состояние через память; возвращает <code>(итог, память, трейс)</code>.</li></ul><pre><code>python code/planner_agent.py\npytest code -q</code></pre><p>Демо считает <code>(2+3)*4-1 = 19</code>: видно, как каждый шаг читает прошлое значение из памяти и пишет новое; в памяти остаются все промежуточные шаги.</p><h2>USE IT</h2><p>Многошаговая задача в реальном инструменте (мульти-провайдер): план строит LLM, память — платформенная или своя.</p><ul><li><strong>Claude Agent SDK</strong> — agent loop + memory tool (<code>/memories</code>) / managed memory stores; план — через промптинг или субагентов.</li><li><strong>OpenAI Agents SDK</strong> — sessions хранят историю; планирование через инструкции.</li><li>Свой вариант: <code>MemoryStore</code> из урока 4.2 как долговременная память + планировщик-LLM.</li></ul><p>⚠️ Память, в которую пишет агент, — поверхность для prompt-инъекций: reference-данные держи read-only (урок 6.5).</p><h2>SHIP IT</h2><p><strong>Артефакт:</strong> Агент с памятью и планом → [<code>outputs/planner_agent.py</code>](../outputs/planner_agent.py)</p><p>Переиспользуемый каркас «план + память» — подставляешь свои инструменты и цель. Дальше: заслоны и подтверждения на опасных шагах (7.3).</p><h2>Материалы</h2><ul><li><a href=\"https://platform.claude.com/docs/en/agent-sdk/overview\" target=\"_blank\" rel=\"noopener\">Anthropic — Agent SDK overview</a> — agent loop, контекст, инструменты.</li><li><a href=\"https://platform.claude.com/docs/en/agents-and-tools/tool-use/memory-tool\" target=\"_blank\" rel=\"noopener\">Anthropic — Memory tool</a> — память агента между сессиями.</li><li><a href=\"https://arxiv.org/abs/2210.03629\" target=\"_blank\" rel=\"noopener\">ReAct (arXiv 2210.03629)</a> — рассуждение и действие как основа агента.</li></ul><hr><p><strong>Часы:</strong> ~5 · <strong>DoD:</strong> <code>pytest code -q</code> зелёный, демо запускается, ru.md заполнен. ✅ <strong>Урок готов</strong></p>"},{"id":"7.3","title":"Human-in-the-loop и guardrails","status":"done","motto":"Автономность без заслонов опасна: критичные действия — через подтверждение и валидацию.","hours":5,"dir":"phases/07-agent-engineering/07-human-in-the-loop-guardrails","html":"<h1>Урок 7.3 · Human-in-the-loop и guardrails</h1><p><strong>Фаза 7 — Agent Engineering</strong> · <strong>Результат фазы:</strong> Собрать надёжного агента с памятью, планированием, human-in-the-loop и guardrails.</p><blockquote><strong>MOTTO.</strong> Автономность без заслонов опасна: критичные действия — через подтверждение и валидацию.</blockquote><h2>PROBLEM</h2><p>Агент, который сам жмёт «удалить», «перевести деньги», «задеплоить», однажды сделает это по галлюцинации или prompt-инъекции. Нужны <strong>guardrails</strong>: валидация аргументов до исполнения и <strong>human-in-the-loop</strong> — пауза на подтверждение для необратимых действий. Это разница между «помощником» и «миной».</p><h2>CONCEPT</h2><pre><code>действие агента\n   │ 1) валидаторы (типы, лимиты, разрешённые операции)\n   │ 2) опасное? → пауза → подтверждение человека (approve/reject)\n   │ 3) известный инструмент?\n   ▼ только тогда — исполнение</code></pre><p>Безопасные действия идут без трения; опасные (delete/send/deploy) останавливаются и ждут человека. Валидаторы ловят «плохие» аргументы ещё до подтверждения.</p><h2>BUILD IT</h2><p>Точки подтверждения + валидация действий: [<code>code/guardrails.py</code>](../code/guardrails.py).</p><ul><li><code>requires_confirmation(tool)</code> — опасные инструменты (delete/send_money/deploy…);</li><li><code>run_action(action, tools, confirm, validators)</code> — валидация → подтверждение → исполнение;</li><li><code>amount_limit(max)</code> — пример валидатора (лимит суммы).</li></ul><pre><code>python code/guardrails.py\npytest code -q</code></pre><p>Тесты: безопасное действие исполняется без трения; опасное без подтверждения — блок; <code>confirm=False</code> — блок; перевод сверх лимита — блок валидатором.</p><h2>USE IT</h2><p>Контроль агента на критичных шагах — готовыми механизмами (мульти-провайдер):</p><ul><li><strong>OpenAI Agents SDK</strong> — <code>guardrails</code> (input/output/tool tripwire) + <strong>human-in-the-loop</strong>: <code>needs_approval</code> на инструменте, пауза с <code>RunState</code>, <code>approve()/reject()</code>, resume.</li><li><strong>Anthropic</strong> — tool use с подтверждением на стороне приложения; разрешения в Claude Code/Agent SDK.</li><li>Паттерн: дешёвый guardrail-модель отсекает плохое до дорогого вызова; необратимое — только после approve.</li></ul><h2>SHIP IT</h2><p><strong>Артефакт:</strong> Шаблон guardrails → [<code>outputs/guardrails-template.md</code>](../outputs/guardrails-template.md)</p><p>Готовый шаблон: список опасных действий, валидаторы, точки подтверждения, аудит. Дальше: что делать, когда агент всё равно ломается (7.4).</p><h2>Материалы</h2><ul><li><a href=\"https://openai.github.io/openai-agents-python/guardrails/\" target=\"_blank\" rel=\"noopener\">OpenAI — Guardrails (Agents SDK)</a> — input/output/tool проверки.</li><li><a href=\"https://openai.github.io/openai-agents-python/human_in_the_loop/\" target=\"_blank\" rel=\"noopener\">OpenAI — Human-in-the-loop</a> — пауза и approve/reject.</li><li><a href=\"https://www.anthropic.com/research/building-effective-agents\" target=\"_blank\" rel=\"noopener\">Anthropic — Building Effective Agents</a> — guardrails и тестирование в песочнице.</li></ul><hr><p><strong>Часы:</strong> ~5 · <strong>DoD:</strong> <code>pytest code -q</code> зелёный, демо запускается, ru.md заполнен. ✅ <strong>Урок готов</strong></p>"},{"id":"7.4","title":"Где агенты ломаются","status":"done","motto":"Агенты ломаются предсказуемо — петли, дрейф цели, бесконечная работа; это ловится инженерно.","hours":5,"dir":"phases/07-agent-engineering/07-where-agents-break","html":"<h1>Урок 7.4 · Где агенты ломаются</h1><p><strong>Фаза 7 — Agent Engineering</strong> · <strong>Результат фазы:</strong> Собрать надёжного агента с памятью, планированием, human-in-the-loop и guardrails.</p><blockquote><strong>MOTTO.</strong> Агенты ломаются предсказуемо — петли, дрейф цели, бесконечная работа; это ловится инженерно.</blockquote><h2>PROBLEM</h2><p>Автономный агент рано или поздно зависает: крутится в петле (повторяет одно действие), дрейфует от цели (увлёкся не тем) или просто не останавливается. Если не ловить эти режимы, агент жжёт токены и время впустую. Воспроизведём поломки и встроим детекторы.</p><h2>CONCEPT</h2><pre><code>типовые поломки:               детектор:\n- петля (повтор действия)   →  detect_loop (N одинаковых подряд)\n- дрейф цели                →  goal_relatedness &lt; порога\n- бесконечная работа        →  лимит шагов</code></pre><p>Это «предохранители» вокруг agent loop из 7.1. Глубже — self-reflection (Reflexion): агент анализирует неудачу и корректирует подход, а не повторяет её.</p><h2>BUILD IT</h2><p>Воспроизведение петель/дрейфа + детекторы: [<code>code/agent_failures.py</code>](../code/agent_failures.py).</p><ul><li><code>detect_loop(actions, repeat)</code> — N одинаковых действий подряд;</li><li><code>goal_relatedness(goal, action)</code> — связь действия с целью (грубая оценка дрейфа);</li><li><code>run_guarded(policy, goal, max_steps, repeat, drift_threshold)</code> — гоняет policy и возвращает статус: <code>done</code> / <code>loop_detected</code> / <code>goal_drift</code> / <code>step_limit</code>.</li></ul><pre><code>python code/agent_failures.py\npytest code -q</code></pre><p>Демо: зацикленная policy → <code>loop_detected</code>; нормальная → <code>done</code>; бесконечная без повторов → <code>step_limit</code>. Поломки ловятся <strong>до</strong> того, как агент сожжёт бюджет.</p><h2>USE IT</h2><p>Инженерные приёмы контроля (мульти-провайдер):</p><ul><li><strong>Лимит шагов/времени/бюджета</strong> — жёсткий потолок на цикл (как в agent loop 7.1).</li><li><strong>Детекция повторов</strong> — одинаковые tool_call подряд → стоп или смена стратегии.</li><li><strong>Привязка к цели</strong> — периодически сверять действия с исходной целью (анти-дрейф).</li><li><strong>Self-reflection (Reflexion)</strong> — после неудачи попросить агента отрефлексировать и перепланировать.</li><li><strong>Наблюдаемость</strong> — логировать трейс (Фаза 10), чтобы видеть, где сломалось.</li></ul><h2>SHIP IT</h2><p><strong>Артефакт:</strong> prompt-debug-agent → [<code>outputs/prompt-debug-agent/</code>](../outputs/prompt-debug-agent/README.md)</p><p>Набор детекторов (петля/дрейф/лимит) + плейбук отладки агента: симптом → причина → приём. Завершает Фазу 7: агент с памятью, планом, guardrails и предохранителями от поломок.</p><h2>Материалы</h2><ul><li><a href=\"https://www.anthropic.com/research/building-effective-agents\" target=\"_blank\" rel=\"noopener\">Anthropic — Building Effective Agents</a> — когда агент уместен, ограничения автономии.</li><li><a href=\"https://arxiv.org/abs/2303.11366\" target=\"_blank\" rel=\"noopener\">Shinn et al., 2023 — Reflexion</a> — self-reflection и обучение на неудачах без дообучения.</li><li><a href=\"https://www.anthropic.com/engineering/effective-context-engineering-for-ai-agents\" target=\"_blank\" rel=\"noopener\">Anthropic — Effective context engineering</a> — context rot и длинные горизонты.</li></ul><hr><p><strong>Часы:</strong> ~5 · <strong>DoD:</strong> <code>pytest code -q</code> зелёный, демо запускается, ru.md заполнен. ✅ <strong>Урок готов</strong></p>"},{"id":"7.5","title":"Evals агентов","status":"done","motto":"Агента нельзя улучшать на глаз: нужен тест-сет, метрики и защита от регрессий.","hours":4,"dir":"phases/07-agent-engineering/07-agent-evals","html":"<h1>Урок 7.5 · Evals агентов</h1><p><strong>Фаза 7 — Agent Engineering</strong> · <strong>Результат фазы:</strong> Собрать надёжного агента с памятью, планированием, human-in-the-loop и guardrails.</p><blockquote><strong>MOTTO.</strong> Агента нельзя улучшать на глаз: нужен тест-сет, метрики и защита от регрессий.</blockquote><h2>PROBLEM</h2><p>Поправил промпт/инструмент агента — на одном примере стало лучше, а на десяти других хуже, и ты этого не видишь. Без <strong>eval-набора</strong> нельзя ни сравнить версии агента, ни поймать регрессию перед выкладкой. Это eval-harness из 2.5, но для агента: оцениваем итог и траекторию (какие инструменты звал, сколько шагов).</p><h2>CONCEPT</h2><pre><code>тест-сет {input, expected}\n        │ agent_fn (версия агента)\n   scorer: success / tool-correctness / шаги\n        │ усреднили → success_rate\n        ▼\n   compare(versии) · has_regression(baseline, candidate)</code></pre><p>Offline-evals ловят регрессии до прода (как юнит-тесты); online-evals меряют дрейф качества на реальном трафике.</p><h2>BUILD IT</h2><p>Offline eval-harness агента + сравнение версий + детект регрессий: [<code>code/agent_eval.py</code>](../code/agent_eval.py).</p><ul><li><code>evaluate_agent(agent_fn, cases, scorer)</code> → <code>{success_rate, per_case}</code>;</li><li><code>compare_versions(versions, cases)</code> — рейтинг версий;</li><li><code>has_regression(baseline, candidate, cases)</code> — стало ли хуже.</li></ul><pre><code>python code/agent_eval.py\npytest code -q</code></pre><p>Тест: рабочий агент → 1.0, сломанный → 0.0, регрессия <code>good→broken</code> детектится. Это инструмент выбора версии.</p><h2>USE IT</h2><p>Online-evals и регрессии в проде (мульти-провайдер):</p><ul><li><strong>LangSmith Evaluation</strong> — offline (на датасетах, как unit-тесты) + online (скоринг прод-трафика); агентные траектории, RAG-метрики; интеграция с pytest/CI.</li><li><strong>RAGAS</strong> — метрики для RAG/agentic без ручной разметки.</li><li><strong>Anthropic — empirical evaluations</strong> — как строить надёжные eval-наборы и грейдинг (LLM-as-judge, урок 2.5).</li><li>Включи eval в CI: порог на success_rate, падение — блок мерджа.</li></ul><h2>SHIP IT</h2><p><strong>Артефакт:</strong> <code>agent-eval</code> (skill) → [<code>outputs/agent-eval/</code>](../outputs/agent-eval/SKILL.md)</p><p>Скилл оценки агента: подкладываешь тест-сет и версии — получаешь рейтинг и флаг регрессии. Зарегистрирован в [<code>.claude/skills/agent-eval</code>](../../../.claude/skills/agent-eval/SKILL.md). Связь с 2.5 (eval промптов), 7.4 (поломки), 10.4 (online-трейсинг).</p><h2>Материалы</h2><ul><li><a href=\"https://docs.langchain.com/langsmith/evaluation\" target=\"_blank\" rel=\"noopener\">LangSmith — Evaluation</a> — offline/online evals агентов.</li><li><a href=\"https://docs.ragas.io/en/stable/concepts/metrics/available_metrics/\" target=\"_blank\" rel=\"noopener\">Ragas — метрики</a> — метрики для RAG и agentic.</li><li><a href=\"https://docs.anthropic.com/en/docs/build-with-claude/develop-tests\" target=\"_blank\" rel=\"noopener\">Anthropic — Create strong empirical evaluations</a> — построение eval-наборов.</li></ul><hr><p><strong>Часы:</strong> ~4 · <strong>DoD:</strong> <code>pytest code -q</code> зелёный, демо запускается, ru.md заполнен. ✅ <strong>Урок готов</strong></p>"},{"id":"7.6","title":"Realtime и голосовые агенты","status":"done","motto":"Голосовой агент — это поток и право перебить: ответ идёт по чанкам, пользователь может вмешаться.","hours":4,"dir":"phases/07-agent-engineering/07-realtime-voice-agents","html":"<h1>Урок 7.6 · Realtime и голосовые агенты</h1><p><strong>Фаза 7 — Agent Engineering</strong> · <strong>Результат фазы:</strong> Собрать надёжного агента с памятью, планированием, human-in-the-loop и guardrails.</p><blockquote><strong>MOTTO.</strong> Голосовой агент — это поток и право перебить: ответ идёт по чанкам, пользователь может вмешаться.</blockquote><h2>PROBLEM</h2><p>Чат-агент отвечает «целиком», голосовой — иначе: критична <strong>низкая задержка</strong> (TTFT), ответ идёт <strong>потоком</strong> (по аудио-чанкам), и пользователь может <strong>перебить</strong> (barge-in) — тогда текущий ответ надо оборвать. Без модели этого цикла realtime-агент ощущается «тормозным» и «глухим».</p><h2>CONCEPT</h2><pre><code>аудио пользователя ──► VAD (детект речи) ──► агент\n                                              │ стримит ответ по токенам\n       barge-in (пользователь заговорил) ──► отменить текущий ответ\n                                              │\n                              TTFT — ключевая метрика (время до 1-го токена)</code></pre><p>Два отличия от чат-цикла (7.1): <strong>стриминг вывода</strong> и <strong>прерываемость</strong>. Управление турами (кто говорит) — сердце realtime.</p><h2>BUILD IT</h2><p>Турновый streaming-цикл с barge-in, без зависимостей: [<code>code/streaming_loop.py</code>](../code/streaming_loop.py).</p><ul><li><code>StreamingAgent.respond(tokens, interrupt_after)</code> — стрим ответа; перебивание обрывает турн и ставит state <code>interrupted</code>;</li><li><code>first_token_latency(timeline)</code> — TTFT.</li></ul><pre><code>python code/streaming_loop.py\npytest code -q</code></pre><p>Тест: без перебивания — полный ответ (<code>idle</code>); barge-in после N токенов — частичный ответ и состояние <code>interrupted</code>. Это и есть управление турами.</p><h2>USE IT</h2><p>Готовые realtime/voice API (мульти-провайдер):</p><ul><li><strong>OpenAI Realtime API</strong> — speech-to-speech одной моделью (низкая задержка), WebRTC/WebSocket/SIP, VAD + автоматическое прерывание, tool use, телефония.</li><li><strong>Gemini Live API</strong> — двунаправленный WebSocket, аудио/видео/текст, barge-in, 70+ языков, live-перевод, function calling.</li><li>Паттерн: VAD ловит речь → стрим ответа → barge-in отменяет текущий ответ (ровно как наш <code>interrupt_after</code>); для клиента — ephemeral tokens (безопасность).</li></ul><h2>SHIP IT</h2><p><strong>Артефакт:</strong> Голосовой/realtime агент-демо → [<code>outputs/streaming_loop.py</code>](../outputs/streaming_loop.py)</p><p>Каркас турнового стрима с barge-in — подставляешь реальный VAD и Realtime/Live API. Связь с 7.1 (agent loop), 7.3 (guardrails на голосовых действиях), 10.2 (латентность как метрика).</p><h2>Материалы</h2><ul><li><a href=\"https://developers.openai.com/api/docs/guides/realtime\" target=\"_blank\" rel=\"noopener\">OpenAI — Realtime API</a> — speech-to-speech, прерывания, транспорты.</li><li><a href=\"https://ai.google.dev/gemini-api/docs/live-api\" target=\"_blank\" rel=\"noopener\">Gemini — Live API</a> — realtime аудио/видео, barge-in, перевод.</li><li><a href=\"https://developers.openai.com/api/docs/guides/voice-agents\" target=\"_blank\" rel=\"noopener\">OpenAI — Voice agents</a> — сборка голосового агента.</li></ul><hr><p><strong>Часы:</strong> ~4 · <strong>DoD:</strong> <code>pytest code -q</code> зелёный, демо запускается, ru.md заполнен. ✅ <strong>Урок готов</strong></p>"}],"hours":26,"outcome":"Собрать надёжного агента с памятью, планированием, human-in-the-loop и guardrails."},{"id":8,"name":"Мульти-агенты и оркестрация","lessons":[{"id":"8.1","title":"Оркестратор и субагенты","status":"done","motto":"Координация важнее силы одиночки: оркестратор раздаёт роли, субагенты работают параллельно.","hours":5,"dir":"phases/08-multi-agent-orchestration/08-orchestrator-and-subagents","html":"<h1>Урок 8.1 · Оркестратор и субагенты</h1><p><strong>Фаза 8 — Мульти-агенты и оркестрация</strong> · <strong>Результат фазы:</strong> Построить мульти-агентный процесс с оркестратором и передачей контекста.</p><blockquote><strong>MOTTO.</strong> Координация важнее силы одиночки: оркестратор раздаёт роли, субагенты работают параллельно.</blockquote><h2>PROBLEM</h2><p>Один агент на сложной открытой задаче упирается в контекст и делает всё последовательно. Сильнее — <strong>команда</strong>: оркестратор (lead) декомпозирует задачу и раздаёт части специализированным субагентам, у каждого свой контекст и инструменты. У Anthropic такой подход дал +90% к качеству ресёрча (ценой ~15× токенов).</p><h2>CONCEPT</h2><pre><code>        задача\n          │ оркестратор (lead): план + декомпозиция\n   ┌──────┼──────┐\nresearcher  writer  reviewer     ← субагенты (свой контекст, свои инструменты)\n   └──────┼──────┘\n          ▼ оркестратор сводит результаты → ответ</code></pre><p>Это паттерн <strong>orchestrator-workers</strong>: lead делегирует, субагенты выполняют, lead синтезирует. Каждый субагент изолирован — «шум» его шагов не засоряет общий контекст.</p><h2>BUILD IT</h2><p>Оркестратор, раздающий роли субагентам: [<code>code/orchestrator.py</code>](../code/orchestrator.py).</p><ul><li><code>Orchestrator.register(role, fn)</code> — зарегистрировать субагента-роль;</li><li><code>delegate(role, task)</code> — отдать подзадачу субагенту;</li><li><code>run(plan)</code> — выполнить план <code>[(role, task)]</code>, собрать результаты по ролям.</li></ul><pre><code>python code/orchestrator.py\npytest code -q</code></pre><p>Демо: researcher собирает факты → writer пишет черновик по ним → reviewer проверяет. Видно делегирование и сборку; поздние шаги используют результаты ранних.</p><h2>USE IT</h2><p>Agent teams на практике (мульти-провайдер):</p><ul><li><strong>Claude Code subagents</strong> — изолированные субагенты (<code>.claude/agents/*.md</code>), каждый со своим system prompt, контекстом, набором инструментов и моделью (Haiku для дешёвой разведки, Opus для сложного).</li><li><strong>Claude Agent SDK</strong> — субагенты программно, параллельный запуск.</li><li>Принцип Anthropic: «think like your agents», «scale effort to complexity», «teach the orchestrator how to delegate».</li></ul><p>⚠️ Мульти-агент дороже в 4–15× по токенам — применяй там, где это оправдано (Фаза 9).</p><h2>SHIP IT</h2><p><strong>Артефакт:</strong> Мульти-агентный сценарий → [<code>outputs/multi-agent-scenario.md</code>](../outputs/multi-agent-scenario.md)</p><p>Сценарий: какие роли субагентов, что делегируем, как сводим. Дальше: как агенты находят друг друга и передают задачи (8.2), как делят общий контекст (8.3).</p><h2>Материалы</h2><ul><li><a href=\"https://www.anthropic.com/engineering/multi-agent-research-system\" target=\"_blank\" rel=\"noopener\">Anthropic — How we built our multi-agent research system</a> — orchestrator-worker на практике.</li><li><a href=\"https://www.anthropic.com/research/building-effective-agents\" target=\"_blank\" rel=\"noopener\">Anthropic — Building Effective Agents</a> — паттерн orchestrator-workers.</li><li><a href=\"https://code.claude.com/docs/en/sub-agents\" target=\"_blank\" rel=\"noopener\">Claude Code — Subagents</a> — изолированные субагенты с ролями.</li></ul><hr><p><strong>Часы:</strong> ~5 · <strong>DoD:</strong> <code>pytest code -q</code> зелёный, демо запускается, ru.md заполнен. ✅ <strong>Урок готов</strong></p>"},{"id":"8.2","title":"A2A и MCP Server Cards","status":"done","motto":"Чтобы агенты сотрудничали, им нужен общий язык: карточки возможностей + конверт задачи.","hours":5,"dir":"phases/08-multi-agent-orchestration/08-a2a-and-server-cards","html":"<h1>Урок 8.2 · A2A и MCP Server Cards</h1><p><strong>Фаза 8 — Мульти-агенты и оркестрация</strong> · <strong>Результат фазы:</strong> Построить мульти-агентный процесс с оркестратором и передачей контекста.</p><blockquote><strong>MOTTO.</strong> Чтобы агенты сотрудничали, им нужен общий язык: карточки возможностей + конверт задачи.</blockquote><h2>PROBLEM</h2><p>В 8.1 субагенты были «вшиты» в оркестратор. Но агенты от разных команд/фреймворков должны находить друг друга и передавать задачи без ручной интеграции. Для этого есть <strong>A2A</strong> (agent-to-agent): агент публикует <strong>карточку</strong> (что умеет), другой агент её находит и шлёт <strong>задачу</strong> в стандартном конверте.</p><h2>CONCEPT</h2><pre><code>Agent Card (JSON): {name, skills, endpoint, auth}   ← discovery\n        │ find_agent: подобрать по навыкам\n        ▼\nTask (id, message) ──► agent ──► Artifact (результат)   ← A2A-конверт</code></pre><p>A2A дополняет MCP: <strong>MCP</strong> соединяет агента с инструментами, <strong>A2A</strong> — агента с агентом. Оба используют JSON-RPC/HTTP и discovery через карточки.</p><h2>BUILD IT</h2><p>Карточки агентов + передача задач: [<code>code/a2a.py</code>](../code/a2a.py).</p><ul><li><code>AgentCard(name, skills, handler)</code> — карточка возможностей;</li><li><code>find_agent(task, cards)</code> — discovery: подобрать агента по навыкам;</li><li><code>send_task(task, card)</code> / <code>route_and_run(task, cards)</code> — A2A-конверт результата.</li></ul><pre><code>python code/a2a.py\npytest code -q</code></pre><p>Демо: «sql запрос» → sql-agent, «поиск по документам» → doc-agent, «нарисуй картинку» → нет агента. Это discovery + handoff в миниатюре.</p><h2>USE IT</h2><p>Автообнаружение инструментов и агентов (мульти-платформа):</p><ul><li><strong>A2A Protocol</strong> (Google → Linux Foundation) — Agent Card по well-known URL, Task/Message/Artifact, стриминг (SSE), enterprise-auth; 50+ партнёров.</li><li><strong>MCP discovery</strong> — <code>tools/list</code> и server cards: клиент сам узнаёт, что умеет сервер (урок 6.3).</li><li>Комбинация: MCP даёт агенту инструменты, A2A — оркеструет агентов между собой.</li></ul><h2>SHIP IT</h2><p><strong>Артефакт:</strong> Схема A2A-взаимодействия → [<code>outputs/a2a-schema.md</code>](../outputs/a2a-schema.md)</p><p>Схема: карточки агентов твоего процесса, какие навыки, как находят друг друга, формат задачи. Дальше: общий контекст и конфликты (8.3).</p><h2>Материалы</h2><ul><li><a href=\"https://a2a-protocol.org/latest/\" target=\"_blank\" rel=\"noopener\">A2A Protocol</a> — стандарт agent-to-agent (карточки, Task/Artifact).</li><li><a href=\"https://github.com/a2aproject/A2A\" target=\"_blank\" rel=\"noopener\">a2aproject/A2A</a> — спецификация и реализации.</li><li><a href=\"https://modelcontextprotocol.io/specification/2025-11-25\" target=\"_blank\" rel=\"noopener\">MCP — Specification</a> — discovery и server cards.</li></ul><hr><p><strong>Часы:</strong> ~5 · <strong>DoD:</strong> <code>pytest code -q</code> зелёный, демо запускается, ru.md заполнен. ✅ <strong>Урок готов</strong></p>"},{"id":"8.3","title":"Координация и контекст","status":"done","motto":"Несколько агентов над одним состоянием — это про передачу контекста и предотвращение конфликтов.","hours":5,"dir":"phases/08-multi-agent-orchestration/08-coordination-and-context","html":"<h1>Урок 8.3 · Координация и контекст</h1><p><strong>Фаза 8 — Мульти-агенты и оркестрация</strong> · <strong>Результат фазы:</strong> Построить мульти-агентный процесс с оркестратором и передачей контекста.</p><blockquote><strong>MOTTO.</strong> Несколько агентов над одним состоянием — это про передачу контекста и предотвращение конфликтов.</blockquote><h2>PROBLEM</h2><p>Когда субагенты (8.1) пишут в общий результат, возникает то, чего нет у одиночки: <strong>конфликты</strong> (двое переписали один ключ по-разному) и <strong>потеря контекста</strong> (агент не видит, что сделал другой). Без координации мульти-агентный процесс выдаёт несогласованный результат. Соберём общий контекст с детекцией конфликтов.</p><h2>CONCEPT</h2><pre><code>        SharedContext (blackboard)\n   agent1 ─ write(facts, ...) ┐\n   agent2 ─ read(facts) ──────┤  состояние видно всем\n   agent3 ─ write(facts, ...) ┘  ← конфликт, если другой агент и другое значение</code></pre><p>Координация = общее состояние + правило записи. Anthropic сводит результаты субагентов через <strong>shared memory store</strong> (а не длинные чат-ответы) и реконсиляцию у lead-агента.</p><h2>BUILD IT</h2><p>Общий контекст с передачей состояния и детекцией конфликтов: [<code>code/coordination.py</code>](../code/coordination.py).</p><ul><li><code>SharedContext.write(key, value, agent)</code> / <code>read(key)</code> — общее состояние с автором;</li><li>детекция конфликта: другой агент перезаписал ключ другим значением → запись в <code>conflicts</code>;</li><li><code>run_process(steps)</code> — прогон агентов над общим контекстом.</li></ul><pre><code>python code/coordination.py\npytest code -q</code></pre><p>Тесты: consumer видит контекст producer'а; конфликт фиксируется при кросс-агентной перезаписи; тот же агент или то же значение — не конфликт.</p><h2>USE IT</h2><p>Прогон мульти-агентного процесса (мульти-провайдер): оркестратор (8.1) + discovery (8.2) + общий контекст.</p><ul><li><strong>Shared memory store</strong> — субагенты пишут краткие выжимки в общее хранилище, lead сводит (как у Anthropic), а не пересылают длинные ответы.</li><li><strong>Изолированные контексты</strong> субагентов (Claude Code) + явная передача нужного через контекст — меньше «context rot» (урок 4.4).</li><li><strong>Реконсиляция у lead</strong> — единая точка сборки и разрешения конфликтов.</li></ul><h2>SHIP IT</h2><p><strong>Артефакт:</strong> Рабочий мульти-агентный процесс → [<code>outputs/multi_agent_process.py</code>](../outputs/multi_agent_process.py)</p><p>Сквозной процесс: оркестратор раздаёт роли, субагенты пишут в общий контекст, конфликты детектируются, lead сводит результат. Итог Фазы 8 — мульти-агентный процесс с передачей контекста.</p><h2>Материалы</h2><ul><li><a href=\"https://www.anthropic.com/engineering/multi-agent-research-system\" target=\"_blank\" rel=\"noopener\">Anthropic — Multi-agent research system</a> — shared memory и реконсиляция результатов.</li><li><a href=\"https://www.anthropic.com/engineering/effective-context-engineering-for-ai-agents\" target=\"_blank\" rel=\"noopener\">Anthropic — Effective context engineering</a> — управление контекстом на длинных горизонтах.</li><li><a href=\"https://code.claude.com/docs/en/sub-agents\" target=\"_blank\" rel=\"noopener\">Claude Code — Subagents</a> — изоляция контекста субагентов.</li></ul><hr><p><strong>Часы:</strong> ~5 · <strong>DoD:</strong> <code>pytest code -q</code> зелёный, демо запускается, ru.md заполнен. ✅ <strong>Урок готов</strong></p>"},{"id":"8.4","title":"Оркестрация: фреймворки vs no-code","status":"done","motto":"Под капотом фреймворков и no-code — один и тот же граф: узлы, рёбра, состояние.","hours":4,"dir":"phases/08-multi-agent-orchestration/08-frameworks-vs-nocode","html":"<h1>Урок 8.4 · Оркестрация: фреймворки vs no-code</h1><p><strong>Фаза 8 — Мульти-агенты и оркестрация</strong> · <strong>Результат фазы:</strong> Построить мульти-агентный процесс с оркестратором и передачей контекста.</p><blockquote><strong>MOTTO.</strong> Под капотом фреймворков и no-code — один и тот же граф: узлы, рёбра, состояние.</blockquote><h2>PROBLEM</h2><p>Свой оркестратор (8.1) понятен, но в проде выбирают между <strong>код-фреймворками</strong> (LangGraph, CrewAI, AutoGen), <strong>gateway-платформами</strong> (OpenClaw/Hermes, урок 4.5) и <strong>no-code/workflow</strong> (n8n). Легко взять не тот класс инструмента. Чтобы выбирать осознанно, нужно понять, что у всех внутри — граф процесса — и где каждый класс уместен.</p><h2>CONCEPT</h2><pre><code>граф процесса = узлы (шаги) + рёбра (переходы, в т.ч. условные/циклы) + state\n   └─ это и есть «движок» LangGraph, n8n-флоу, своего оркестратора\n\nвыбор класса инструмента:\n  from-scratch / SDK   контроль, нет лок-ина, надо писать всё\n  код-фреймворк        LangGraph (граф/циклы), CrewAI (роли), AutoGen (диалоги)\n  no-code / workflow    n8n (визуально, 400+ интеграций, AI-agent-ноды)\n  gateway-платформа     OpenClaw / Hermes (персональный агент, каналы) — 4.5</code></pre><h2>BUILD IT</h2><p>Мини-граф процесса (как LangGraph) своими руками: [<code>code/mini_graph.py</code>](../code/mini_graph.py).</p><ul><li><code>Graph.add_node/add_edge/add_conditional/run</code> — узлы, рёбра, условные ветвления и циклы поверх общего <code>state</code>;</li><li>лимит шагов как guard от бесконечных циклов (7.4).</li></ul><pre><code>python code/mini_graph.py\npytest code -q</code></pre><p>Тест: линейный граф, условное ветвление, цикл с выходом по условию. Это ядро любого оркестратора — у LangGraph то же, плюс персистентность и инструменты.</p><h2>USE IT</h2><p>Тот же процесс — в готовых инструментах (мульти-инструмент):</p><ul><li><strong>LangGraph</strong> — стейтовый граф с циклами, persistence, human-in-the-loop; «низкоуровневый» контроль (Uber, Klarna, J.P. Morgan).</li><li><strong>CrewAI</strong> — оркестрация по ролям (researcher/analyst/writer), быстрый старт, но более «opinionated».</li><li><strong>n8n</strong> — визуальный workflow с AI-Agent-нодами (на базе LangChain), 400+ интеграций, MCP, human-in-the-loop; смешивает детерминированные шаги и ИИ.</li><li>Правило: <strong>детерминированная автоматизация с парой ИИ-шагов → n8n</strong>; <strong>сложная агентная логика с циклами/состоянием → LangGraph/код</strong>; <strong>роли-команда → CrewAI</strong>; <strong>персональный агент с каналами → OpenClaw/Hermes</strong>.</li></ul><h2>SHIP IT</h2><p><strong>Артефакт:</strong> Матрица «когда фреймворк, когда no-code» → [<code>outputs/orchestration-matrix.md</code>](../outputs/orchestration-matrix.md)</p><p>Карта выбора (from-scratch ↔ SDK ↔ LangGraph/CrewAI/AutoGen ↔ OpenClaw/Hermes ↔ n8n): критерии, плюсы/минусы, когда что. Связь с 8.1 (оркестратор), 4.5 (платформы), 7.4 (лимиты/циклы).</p><h2>Материалы</h2><ul><li><a href=\"https://docs.langchain.com/oss/python/langgraph/overview\" target=\"_blank\" rel=\"noopener\">LangGraph — overview</a> — стейтовый граф агентов.</li><li><a href=\"https://github.com/crewAIInc/crewAI\" target=\"_blank\" rel=\"noopener\">crewAIInc/crewAI</a> — оркестрация по ролям.</li><li><a href=\"https://docs.n8n.io/advanced-ai/\" target=\"_blank\" rel=\"noopener\">n8n — Advanced AI</a> — no-code workflow с AI-агентами.</li></ul><hr><p><strong>Часы:</strong> ~4 · <strong>DoD:</strong> <code>pytest code -q</code> зелёный, демо запускается, ru.md заполнен. ✅ <strong>Урок готов</strong></p>"}],"hours":18,"outcome":"Построить мульти-агентный процесс с оркестратором и передачей контекста."},{"id":9,"name":"Экономика и оркестрация моделей (FinOps)","lessons":[{"id":"9.1","title":"Цены 2026 и за что платишь","status":"done","motto":"Платишь за токены, а не за запросы; стоимость надо уметь считать до отправки.","hours":4,"dir":"phases/09-model-economics-finops/09-pricing-2026","html":"<h1>Урок 9.1 · Цены 2026 и за что платишь</h1><p><strong>Фаза 9 — Экономика и оркестрация моделей (FinOps)</strong> · <strong>Результат фазы:</strong> Спроектировать маршрутизацию моделей и посчитать бюджет «до/после».</p><blockquote><strong>MOTTO.</strong> Платишь за токены, а не за запросы; стоимость надо уметь считать до отправки.</blockquote><h2>PROBLEM</h2><p>«Сколько это стоит в проде на 100k запросов?» — без калькулятора это гадание. Цена зависит от модели, числа входных/выходных токенов и режима (кэш, batch). Не считая стоимость заранее, легко получить счёт в 10× из-за одной неоптимальной модели или невключённого кэша.</p><h2>CONCEPT</h2><pre><code>стоимость = (вход × цена_вход + выход × цена_выход) / 1M\n   модификаторы:\n   - prompt caching: кэшированный вход ≈ 10% цены (−90%)\n   - batch API: −50% на всё\n   (скидки стэкаются → эффективно &lt;5% от базовой цены)</code></pre><p>Выход обычно в разы дороже входа. Разброс между моделями — на порядки (Flash vs Opus), поэтому выбор модели — главный рычаг стоимости.</p><h2>BUILD IT</h2><p>Калькулятор стоимости по токенам с кэшем и batch: [<code>code/cost_calculator.py</code>](../code/cost_calculator.py).</p><ul><li><code>PRICES</code> — карта цен 2026 (вход/выход на 1M);</li><li><code>cost(model, n_in, n_out, cached_in, batch)</code> — стоимость вызова;</li><li><code>compare(models, n_in, n_out)</code> — сравнить модели по цене.</li></ul><pre><code>python code/cost_calculator.py\npytest code -q</code></pre><p>Демо: тот же запрос на Opus в ~11× дороже, чем на Flash; кэш и batch заметно режут счёт. Это вход в маршрутизацию (9.2–9.3).</p><h2>USE IT</h2><p>Сверка с реальными тарифами (мульти-провайдер): подставь актуальные цены и режимы.</p><ul><li><strong>Anthropic</strong> — pricing + prompt caching (cache read 0.1×, write 1.25×/2×) + batch −50%.</li><li><strong>OpenAI / Google</strong> — свои тарифы и кэширование/батчи; цены меняются — держи карту цен в одном месте.</li></ul><p>⚠️ На июнь 2026: Opus 4.8 — $5/$25, Sonnet 4.6 — $3/$15, Haiku 4.5 — $1/$5 за 1M (вход/выход); <strong>DeepSeek V4-Flash — ~$0.14/$0.28</strong> (самый дешёвый frontier-class, open-weight). Сверяйся с актуальными страницами тарифов.</p><h2>SHIP IT</h2><p><strong>Артефакт:</strong> Бюджет-калькулятор → [<code>outputs/cost_calculator.py</code>](../outputs/cost_calculator.py)</p><p>Переиспользуемый калькулятор (токены → $, с кэшем/batch) — основа для маршрутизации (9.2–9.3) и FinOps-учёта (9.4). Связан с <code>prompt-token-budgeter</code> из урока 1.1.</p><h2>Материалы</h2><ul><li><a href=\"https://platform.claude.com/docs/en/about-claude/pricing\" target=\"_blank\" rel=\"noopener\">Anthropic — Pricing</a> — тарифы и компоненты стоимости.</li><li><a href=\"https://openai.com/api/pricing/\" target=\"_blank\" rel=\"noopener\">OpenAI — API pricing</a> — цены моделей OpenAI.</li><li><a href=\"https://platform.claude.com/docs/en/build-with-claude/prompt-caching\" target=\"_blank\" rel=\"noopener\">Anthropic — Prompt caching</a> — как кэш режет стоимость входа.</li><li><a href=\"https://api-docs.deepseek.com/quick_start/pricing\" target=\"_blank\" rel=\"noopener\">DeepSeek — Models &amp; Pricing</a> — open-weight модель с ценой в разы ниже закрытых.</li></ul><hr><p><strong>Часы:</strong> ~4 · <strong>DoD:</strong> <code>pytest code -q</code> зелёный, демо запускается, ru.md заполнен. ✅ <strong>Урок готов</strong></p>"},{"id":"9.2","title":"Какую модель под какую задачу","status":"done","motto":"Не одна модель на всё, а маршрут по сложности: простое — дёшево, сложное — топ.","hours":4,"dir":"phases/09-model-economics-finops/09-model-per-task","html":"<h1>Урок 9.2 · Какую модель под какую задачу</h1><p><strong>Фаза 9 — Экономика и оркестрация моделей (FinOps)</strong> · <strong>Результат фазы:</strong> Спроектировать маршрутизацию моделей и посчитать бюджет «до/после».</p><blockquote><strong>MOTTO.</strong> Не одна модель на всё, а маршрут по сложности: простое — дёшево, сложное — топ.</blockquote><h2>PROBLEM</h2><p>Гонять всё через Opus — дорого; всё через Flash — качество проседает на сложном. Нужен <strong>роутер</strong>: оценить сложность запроса и направить на подходящую модель. Это главный рычаг экономии (40–70%) без потери качества.</p><h2>CONCEPT</h2><pre><code>запрос → classify(сложность) → route(модель)\n   low   (короткое, классификация/перевод)   → Gemini Flash\n   medium(обычная задача)                     → Sonnet 4.6\n   high  (код, анализ, рассуждение, длинное)  → Opus 4.8</code></pre><p>Сложность оцениваем по признакам: длина, ключевые слова (код/анализ/докажи), нужно ли рассуждение. Это дешёвый «привратник» перед дорогим вызовом.</p><h2>BUILD IT</h2><p>Классификатор сложности + маршрутизация: [<code>code/complexity_router.py</code>](../code/complexity_router.py).</p><ul><li><code>classify(request)</code> — <code>low</code> / <code>medium</code> / <code>high</code> по признакам;</li><li><code>route(request)</code> — модель под сложность.</li></ul><pre><code>python code/complexity_router.py\npytest code -q</code></pre><p>Демо: «переведи фразу» → Flash, «саммари» → Sonnet, «проанализируй архитектуру и оптимизируй код» → Opus. Матч по стемам (учитывает морфологию, см. грабли 5.3).</p><h2>USE IT</h2><p>Маршрут «дешёвые → средние → топ» в проде (мульти-провайдер):</p><ul><li><strong>Anthropic</strong> — Haiku для классификации/роутинга/извлечения, Sonnet как дефолт, Opus только на сложное (см. «Choosing the right model»).</li><li><strong>Кросс-провайдер</strong> — Gemini Flash на массовое, GPT/Sonnet на универсальное, Opus на топ.</li><li><strong>Open-weight тир</strong> — DeepSeek V4 (дёшево + reasoning через thinking-mode); подходит, когда нужен self-host ради приватности/суверенитета данных (Фаза 11).</li><li>Роутер можно делать дешёвой моделью или эвристикой (как наш <code>classify</code>) — главное, чтобы он сам был дешёвым.</li></ul><h2>SHIP IT</h2><p><strong>Артефакт:</strong> Матрица маршрутизации → [<code>outputs/routing-matrix.md</code>](../outputs/routing-matrix.md)</p><p>Таблица «класс задачи → модель → почему» под твои задачи (развитие матрицы из урока 0.1). Дальше: каскад с quality-gate (9.3).</p><h2>Материалы</h2><ul><li><a href=\"https://docs.anthropic.com/en/docs/about-claude/models/choosing-a-model\" target=\"_blank\" rel=\"noopener\">Anthropic — Choosing the right model</a> — как выбирать модель.</li><li><a href=\"https://docs.anthropic.com/en/docs/reducing-latency\" target=\"_blank\" rel=\"noopener\">Anthropic — Reducing latency</a> — модель как рычаг скорости/цены.</li><li><a href=\"https://platform.claude.com/docs/en/about-claude/models/overview\" target=\"_blank\" rel=\"noopener\">Anthropic — Models overview</a> — тиры и их сильные стороны.</li></ul><hr><p><strong>Часы:</strong> ~4 · <strong>DoD:</strong> <code>pytest code -q</code> зелёный, демо запускается, ru.md заполнен. ✅ <strong>Урок готов</strong></p>"},{"id":"9.3","title":"Паттерны экономии","status":"done","motto":"Сначала дёшево, эскалация по необходимости: каскад + кэш + batch дают 70–90% экономии.","hours":4,"dir":"phases/09-model-economics-finops/09-cost-saving-patterns","html":"<h1>Урок 9.3 · Паттерны экономии</h1><p><strong>Фаза 9 — Экономика и оркестрация моделей (FinOps)</strong> · <strong>Результат фазы:</strong> Спроектировать маршрутизацию моделей и посчитать бюджет «до/после».</p><blockquote><strong>MOTTO.</strong> Сначала дёшево, эскалация по необходимости: каскад + кэш + batch дают 70–90% экономии.</blockquote><h2>PROBLEM</h2><p>Маршрут по сложности (9.2) хорош, но классификатор может ошибиться. Надёжнее — <strong>каскад</strong>: пробуем дешёвую модель, проверяем качество, и только если не дотянула — эскалируем на сильную. Плюс инфраструктурные скидки: prompt caching и batch API. Вместе — кратная экономия без потери качества.</p><h2>CONCEPT</h2><pre><code>запрос → cheap → quality &gt;= gate? ──да──► ответ (заплатили мало)\n                     │ нет\n                     ▼\n                  mid → quality &gt;= gate? ──да──► ответ\n                     │ нет\n                     ▼\n                  top → ответ (платим за топ только когда нужно)\n   + prompt caching (−90% на повторяемый вход)  + batch (−50%)</code></pre><p>Идея каскада из <strong>FrugalGPT</strong>: дешёвая модель часто справляется; дорогую зовём только на сложном. Скидки стэкаются.</p><h2>BUILD IT</h2><p>Роутер + каскад с quality-gate: [<code>code/cost_router.py</code>](../code/cost_router.py).</p><ul><li><code>cascade(request, answer_fn, quality_fn, gate)</code> — идёт по лестнице моделей, пока качество ≥ gate; считает суммарную стоимость;</li><li><code>always_top_cost()</code> — сколько стоило бы «всё через топ» (для сравнения экономии).</li></ul><pre><code>python code/cost_router.py\npytest code -q</code></pre><p>Тест на смешанной нагрузке: каскад дешевле, чем «всегда топ» — простое решается дёшево, на сложное эскалируем. Качество измеряет <code>quality_fn</code> (стаб; в проде — LLM-judge из урока 2.5).</p><h2>USE IT</h2><p>Инфраструктурные паттерны экономии (мульти-провайдер):</p><ul><li><strong>Prompt caching</strong> — повторяемый префикс (system, документы) кэшируется: cache read ≈ 10% цены входа. Окупается с 1–2 попаданий.</li><li><strong>Batch API</strong> — асинхронные задачи (классификация, eval, генерация) −50%; стэкается с кэшем → эффективно &lt;5% базовой цены.</li><li><strong>Каскад + роутер</strong> — quality-gate решает, нужна ли эскалация (как наш <code>cascade</code>).</li><li><strong>Quality-gate</strong> — дешёвой моделью-судьёй (урок 2.5), чтобы не эскалировать зря.</li></ul><h2>SHIP IT</h2><p><strong>Артефакт:</strong> <code>cost-aware-router</code> (skill) → [<code>outputs/cost-aware-router/</code>](../outputs/cost-aware-router/SKILL.md)</p><p>Скилл каскадного роутера: подставляешь модели, quality_fn и gate — получаешь дешёвую оркестрацию с эскалацией. Зарегистрирован в [<code>.claude/skills/cost-aware-router</code>](../../../.claude/skills/cost-aware-router/SKILL.md). Дальше: учёт и ROI (9.4).</p><h2>Материалы</h2><ul><li><a href=\"https://arxiv.org/abs/2305.05176\" target=\"_blank\" rel=\"noopener\">Chen et al., 2023 — FrugalGPT</a> — каскад LLM, экономия 50–98%.</li><li><a href=\"https://platform.claude.com/docs/en/build-with-claude/prompt-caching\" target=\"_blank\" rel=\"noopener\">Anthropic — Prompt caching</a> — −90% на кэшированный вход.</li><li><a href=\"https://platform.claude.com/docs/en/build-with-claude/batch-processing\" target=\"_blank\" rel=\"noopener\">Anthropic — Batch processing</a> — −50% на асинхронные задачи.</li></ul><hr><p><strong>Часы:</strong> ~4 · <strong>DoD:</strong> <code>pytest code -q</code> зелёный, демо запускается, ru.md заполнен. ✅ <strong>Урок готов</strong></p>"},{"id":"9.4","title":"FinOps для ИИ","status":"done","motto":"Нельзя оптимизировать то, что не измеряешь: тегируй запросы, считай по разрезам, меряй ROI.","hours":4,"dir":"phases/09-model-economics-finops/09-finops-for-ai","html":"<h1>Урок 9.4 · FinOps для ИИ</h1><p><strong>Фаза 9 — Экономика и оркестрация моделей (FinOps)</strong> · <strong>Результат фазы:</strong> Спроектировать маршрутизацию моделей и посчитать бюджет «до/после».</p><blockquote><strong>MOTTO.</strong> Нельзя оптимизировать то, что не измеряешь: тегируй запросы, считай по разрезам, меряй ROI.</blockquote><h2>PROBLEM</h2><p>Маршрутизация и каскад (9.2–9.3) экономят, но без учёта непонятно, сработало ли и где деньги. Нужен <strong>FinOps-контур</strong>: каждый запрос с тегами (команда, модель, cache-hit, стоимость), агрегация по разрезам, бюджеты с алертами и расчёт ROI автоматизации.</p><h2>CONCEPT</h2><pre><code>запрос → лог с тегами {team, model, cache_hit, cost}\n              │ агрегация\n   по команде / по модели / cache-hit rate\n              │\n   бюджет + алерт при превышении\n   ROI = (экономия + выгода) / затраты   ← «до/после»</code></pre><p>FinOps для ИИ — это видимость (куда уходят деньги), ответственность (кому отнести расход) и оптимизация (что улучшить). Метрика зрелости — не «cost per inference», а <strong>cost per outcome</strong> (стоимость результата).</p><h2>BUILD IT</h2><p>Тегирование, агрегация, бюджет и ROI: [<code>code/finops.py</code>](../code/finops.py).</p><ul><li><code>Ledger.record(cost, **tags)</code> / <code>by_tag(key)</code> — лог и агрегация по разрезам;</li><li><code>cache_hit_rate()</code>, <code>over_budget(limit)</code> — мониторинг;</li><li><code>roi(before, after, value)</code> — окупаемость автоматизации.</li></ul><pre><code>python code/finops.py\npytest code -q</code></pre><p>Демо: расходы по командам и моделям, cache-hit rate, флаг превышения бюджета и ROI = 6.5 (было 100 → стало 20 + выгода 50). Это «бюджет-дашборд» в миниатюре.</p><h2>USE IT</h2><p>Лимиты, алерты, мониторинг токенов (мульти-провайдер):</p><ul><li><strong>Тегируй каждый запрос</strong> — команда/фича/модель/cache-hit; без тегов нет attribution.</li><li><strong>Лимиты и алерты</strong> — потолок бюджета на команду/фичу, уведомление при приближении.</li><li><strong>Дашборд</strong> — расходы по разрезам + cache-hit rate + cost per outcome; недельный/месячный форкаст (расходы ИИ нелинейны).</li><li><strong>ROI «до/после»</strong> — стоимость процесса до автоматизации vs после + полученная выгода (capstone, Фаза 12).</li></ul><h2>SHIP IT</h2><p><strong>Артефакт:</strong> Бюджет-дашборд + расчёт ROI → [<code>outputs/finops-dashboard.md</code>](../outputs/finops-dashboard.md)</p><p>Шаблон дашборда (разрезы, метрики, алерты) + формула ROI. Завершает Фазу 9: спроектированная маршрутизация (9.2–9.3) и измеренный бюджет «до/после».</p><h2>Материалы</h2><ul><li><a href=\"https://www.finops.org/wg/finops-for-ai-overview/\" target=\"_blank\" rel=\"noopener\">FinOps Foundation — FinOps for AI</a> — фреймворк управления стоимостью ИИ.</li><li><a href=\"https://www.cloudzero.com/blog/finops-for-ai/\" target=\"_blank\" rel=\"noopener\">CloudZero — FinOps for AI</a> — почему ИИ меняет cost management.</li><li><a href=\"https://platform.claude.com/docs/en/about-claude/pricing\" target=\"_blank\" rel=\"noopener\">Anthropic — Pricing</a> — компоненты стоимости для учёта.</li></ul><hr><p><strong>Часы:</strong> ~4 · <strong>DoD:</strong> <code>pytest code -q</code> зелёный, демо запускается, ru.md заполнен. ✅ <strong>Урок готов</strong></p>"}],"hours":14,"outcome":"Спроектировать маршрутизацию моделей и посчитать бюджет «до/после»."},{"id":10,"name":"Production и интеграция","lessons":[{"id":"10.1","title":"Интеграция в рабочие пакеты","status":"done","motto":"ИИ полезен, когда встроен в работу: фича-флаг + fallback делают встраивание безопасным.","hours":4,"dir":"phases/10-production-and-integration/10-integration-into-workspaces","html":"<h1>Урок 10.1 · Интеграция в рабочие пакеты</h1><p><strong>Фаза 10 — Production и интеграция</strong> · <strong>Результат фазы:</strong> Встроить ИИ в рабочий инструмент с мониторингом токенов и стоимости.</p><blockquote><strong>MOTTO.</strong> ИИ полезен, когда встроен в работу: фича-флаг + fallback делают встраивание безопасным.</blockquote><h2>PROBLEM</h2><p>Демо в чате ≠ прод. Встраивая ИИ в реальный процесс (CRM, поддержка, документы), нельзя допустить, чтобы сбой модели или плохой ответ положил рабочий инструмент. Нужны <strong>фича-флаг</strong> (мгновенно выключить) и <strong>graceful fallback</strong> (упала модель → процесс деградирует, а не падает). ИИ должен быть заменяемой деталью.</p><h2>CONCEPT</h2><pre><code>существующий процесс\n   └─ [ AI-шаг ]\n        enabled? ──нет──► fallback (старый детерминированный путь)\n        try AI ──ошибка──► fallback\n        ok ──► AI-результат</code></pre><p>Точки встраивания ищем там, где ИИ даёт ценность (черновик, классификация, саммари), но всегда с запасным путём. Это делает откат безрисковым.</p><h2>BUILD IT</h2><p>Безопасная обёртка ИИ-шага: [<code>code/integration.py</code>](../code/integration.py).</p><ul><li><code>AIFeature(fn, fallback, enabled)</code> — фича-флаг + try/except → fallback;</li><li><code>run_pipeline(steps, x)</code> — ИИ как один из шагов существующего пайплайна.</li></ul><pre><code>python code/integration.py\npytest code -q</code></pre><p>Демо показывает три исхода: ИИ работает; ИИ упал → fallback; фича выключена → fallback. Прод не падает из-за ИИ — ключевое свойство интеграции.</p><h2>USE IT</h2><p>Встраивание через рабочие пакеты (мульти-провайдер):</p><ul><li><strong>Microsoft 365 Copilot</strong> — ИИ в Word/Excel/Outlook/Teams поверх Microsoft Graph (доступ только к тому, что разрешено пользователю).</li><li><strong>Gemini for Google Workspace</strong> — ИИ в Gmail/Docs/Sheets; данные не покидают организацию.</li><li><strong>Свой инструмент через API</strong> — AI-шаг за фича-флагом и fallback (как в Build It), с мониторингом (10.2).</li></ul><p>Везде принцип один: ИИ рядом с данными процесса + контроль доступа и откат.</p><blockquote><strong>Врезка (2026): self-hosted агент-рантайм и многоканальность.</strong> Агента можно деплоить не только за API, но и на локальную платформу — <strong>OpenClaw / Hermes</strong> (MIT): скиллы, память, мульти-модельная оркестрация и подключение к 50+ каналам (Telegram, Slack, WhatsApp, Signal). Удобно для персонального агента с многоканальным доступом (подробно — урок 4.5). Те же гарантии — фича-флаг, fallback, контроль доступа — остаются.</blockquote><h2>SHIP IT</h2><p><strong>Артефакт:</strong> План интеграции для команды → [<code>outputs/integration-plan.md</code>](../outputs/integration-plan.md)</p><p>Шаблон: где встроить ИИ, какой fallback, как включать (rollout/флаги), что мониторить. Дальше: деплой и мониторинг (10.2), телеметрия (10.3).</p><h2>Материалы</h2><ul><li><a href=\"https://learn.microsoft.com/en-us/copilot/microsoft-365/microsoft-365-copilot-overview\" target=\"_blank\" rel=\"noopener\">Microsoft 365 Copilot — Overview</a> — ИИ в рабочих приложениях.</li><li><a href=\"https://support.google.com/docs/answer/13952129\" target=\"_blank\" rel=\"noopener\">Gemini for Google Workspace</a> — ИИ в Gmail/Docs/Sheets.</li><li><a href=\"https://learn.microsoft.com/en-us/microsoft-365/copilot/microsoft-365-copilot-architecture\" target=\"_blank\" rel=\"noopener\">Microsoft 365 Copilot — архитектура</a> — данные, доступ, границы безопасности.</li></ul><hr><p><strong>Часы:</strong> ~4 · <strong>DoD:</strong> <code>pytest code -q</code> зелёный, демо запускается, ru.md заполнен. ✅ <strong>Урок готов</strong></p>"},{"id":"10.2","title":"Деплой и мониторинг","status":"done","motto":"Задеплоил — значит измеряешь: без логов prompt/response и метрик здоровья прод вслепую.","hours":4,"dir":"phases/10-production-and-integration/10-deploy-and-monitoring","html":"<h1>Урок 10.2 · Деплой и мониторинг</h1><p><strong>Фаза 10 — Production и интеграция</strong> · <strong>Результат фазы:</strong> Встроить ИИ в рабочий инструмент с мониторингом токенов и стоимости.</p><blockquote><strong>MOTTO.</strong> Задеплоил — значит измеряешь: без логов prompt/response и метрик здоровья прод вслепую.</blockquote><h2>PROBLEM</h2><p>После выкладки ИИ-процесса нужно знать: не растёт ли доля ошибок, не деградирует ли латентность, что именно сломалось. Без структурного логирования вызовов и метрик мониторинга «у пользователя не работает» превращается в гадание. ИИ ещё и недетерминирован — наблюдаемость критична вдвойне.</p><h2>CONCEPT</h2><pre><code>каждый вызов → лог {model, latency, status, error}\n                     │\n   метрики:  error_rate · p50/p95 latency · recent_errors\n                     │\n              алерты при превышении порогов</code></pre><p>Логируй то, что позволит отладить и оценить здоровье: модель, латентность, статус, ошибку (и при согласии — содержимое, с редактированием PII).</p><h2>BUILD IT</h2><p>Структурный логгер вызовов + метрики: [<code>code/monitoring.py</code>](../code/monitoring.py).</p><ul><li><code>CallLogger.log(model, latency_ms, ok, error)</code> — запись вызова;</li><li><code>error_rate()</code>, <code>latency_percentile(p)</code>, <code>recent_errors()</code> — метрики мониторинга.</li></ul><pre><code>python code/monitoring.py\npytest code -q</code></pre><p>Демо: 1 ошибка из 5 → error_rate 0.2, p95 latency ловит «хвост» 1500ms. Это сырьё для алертов и дашборда.</p><h2>USE IT</h2><p>Запуск процесса в проде с observability (мульти-провайдер):</p><ul><li><strong>OpenTelemetry GenAI</strong> — стандарт семантических конвенций (<code>gen_ai.*</code>): модель, токены, латентность, tool-calls — единый формат для любого вендора.</li><li><strong>Langfuse / аналоги</strong> — трейсинг вызовов, ошибок и токенов; LLM-as-judge для качества.</li><li><strong>Алерты</strong> — на error_rate, p95 latency, всплеск стоимости; редактируй PII перед логированием контента.</li></ul><h2>SHIP IT</h2><p><strong>Артефакт:</strong> Мониторинг-конфиг → [<code>outputs/monitoring-config.md</code>](../outputs/monitoring-config.md)</p><p>Конфиг: что логировать, какие метрики, пороги алертов, политика по PII. Дальше: телеметрия токенов/стоимости и воспроизводимость (10.3).</p><h2>Материалы</h2><ul><li><a href=\"https://opentelemetry.io/blog/2026/genai-observability/\" target=\"_blank\" rel=\"noopener\">OpenTelemetry — GenAI observability</a> — трейсинг вызовов LLM.</li><li><a href=\"https://opentelemetry.io/blog/2024/otel-generative-ai/\" target=\"_blank\" rel=\"noopener\">OpenTelemetry — for Generative AI</a> — семантические конвенции <code>gen_ai.*</code>.</li><li><a href=\"https://langfuse.com/docs/observability/overview\" target=\"_blank\" rel=\"noopener\">Langfuse — Observability</a> — трейсинг, ошибки, токены, оценка.</li></ul><hr><p><strong>Часы:</strong> ~4 · <strong>DoD:</strong> <code>pytest code -q</code> зелёный, демо запускается, ru.md заполнен. ✅ <strong>Урок готов</strong></p>"},{"id":"10.3","title":"Телеметрия и воспроизводимость","status":"done","motto":"Считай токены и стоимость на процесс и фиксируй конфиг прогона — чтобы повторить и сравнить.","hours":4,"dir":"phases/10-production-and-integration/10-telemetry-and-reproducibility","html":"<h1>Урок 10.3 · Телеметрия и воспроизводимость</h1><p><strong>Фаза 10 — Production и интеграция</strong> · <strong>Результат фазы:</strong> Встроить ИИ в рабочий инструмент с мониторингом токенов и стоимости.</p><blockquote><strong>MOTTO.</strong> Считай токены и стоимость на процесс и фиксируй конфиг прогона — чтобы повторить и сравнить.</blockquote><h2>PROBLEM</h2><p>Мониторинг (10.2) говорит «здорово ли», телеметрия — «сколько стоит и воспроизводимо ли». Без учёта токенов/стоимости в разрезе процессов не понять, что дорого; без фиксации конфига (модель, параметры, версия промпта) нельзя воспроизвести результат и честно сравнить «до/после» (мост к FinOps 9.4 и Capstone 12).</p><h2>CONCEPT</h2><pre><code>телеметрия:   record(process, tokens_in, tokens_out, cost) → by_process()\nвоспроизводимость: RunConfig{model, temperature, prompt_version, seed}\n                       └─ fingerprint() : одинаковый конфиг → одинаковый хэш</code></pre><p>Телеметрия привязывает расходы к процессам; fingerprint конфига делает прогон воспроизводимым и сравнимым.</p><h2>BUILD IT</h2><p>Учёт токенов/стоимости + отпечаток конфига: [<code>code/telemetry.py</code>](../code/telemetry.py).</p><ul><li><code>Telemetry.record(...)</code> / <code>by_process()</code> / <code>total_cost()</code> — расходы по процессам;</li><li><code>RunConfig.fingerprint()</code> — стабильный хэш конфига (воспроизводимость).</li></ul><pre><code>python code/telemetry.py\npytest code -q</code></pre><p>Тесты: агрегация токенов/стоимости по процессам; одинаковый конфиг → одинаковый отпечаток, смена параметра/версии промпта → другой. Это основа честного «до/после».</p><h2>USE IT</h2><p>Дашборд расходов и качества (мульти-провайдер):</p><ul><li><strong>Токены/стоимость на процесс</strong> — из usage в ответе API (<code>input_tokens</code>/<code>output_tokens</code>) × карта цен (9.1).</li><li><strong>Качество</strong> — LLM-as-judge / eval-наборы (урок 2.5) рядом со стоимостью на дашборде.</li><li><strong>Воспроизводимость</strong> — логируй <code>RunConfig</code> (модель, temperature, версия промпта, seed) с каждым прогоном; OTel GenAI стандартизует поля токенов.</li></ul><h2>SHIP IT</h2><p><strong>Артефакт:</strong> Встроенный в рабочий процесс ассистент → [<code>outputs/embedded_assistant.py</code>](../outputs/embedded_assistant.py)</p><p>Сквозной ассистент: фича-флаг + fallback (10.1) + логирование (10.2) + телеметрия токенов/стоимости и fingerprint конфига (10.3). Итог Фазы 10 — ИИ, встроенный в процесс с мониторингом и воспроизводимостью.</p><h2>Материалы</h2><ul><li><a href=\"https://langfuse.com/docs/observability/get-started\" target=\"_blank\" rel=\"noopener\">Langfuse — Get started with tracing</a> — токены, сессии, трейсы.</li><li><a href=\"https://opentelemetry.io/blog/2026/genai-observability/\" target=\"_blank\" rel=\"noopener\">OpenTelemetry — GenAI observability</a> — стандартные поля токенов/латентности.</li><li><a href=\"https://docs.anthropic.com/en/docs/build-with-claude/develop-tests\" target=\"_blank\" rel=\"noopener\">Anthropic — Create strong empirical evaluations</a> — измеримость и воспроизводимость качества.</li></ul><hr><p><strong>Часы:</strong> ~4 · <strong>DoD:</strong> <code>pytest code -q</code> зелёный, демо запускается, ru.md заполнен. ✅ <strong>Урок готов</strong></p>"},{"id":"10.4","title":"Observability и трейсинг агентов","status":"done","motto":"Агент недетерминирован — без трейса шагов отладка в проде превращается в гадание.","hours":4,"dir":"phases/10-production-and-integration/10-observability-tracing","html":"<h1>Урок 10.4 · Observability и трейсинг агентов</h1><p><strong>Фаза 10 — Production и интеграция</strong> · <strong>Результат фазы:</strong> Встроить ИИ в рабочий инструмент с мониторингом токенов и стоимости.</p><blockquote><strong>MOTTO.</strong> Агент недетерминирован — без трейса шагов отладка в проде превращается в гадание.</blockquote><h2>PROBLEM</h2><p>Мониторинг (10.2) даёт агрегаты «здорово/нет», но когда агент выдал странный результат, нужно увидеть <strong>весь путь</strong>: какие шаги, какие инструменты, какой retrieval, где время и токены. Это <strong>трейсинг</strong>: вложенные spans на каждый шаг агента — как в OpenTelemetry GenAI.</p><h2>CONCEPT</h2><pre><code>trace (один запрос)\n └─ span: agent_run\n     ├─ span: retrieve (k=3)        ← attrs, длительность\n     ├─ span: llm_call (model, токены)\n     └─ span: tool (args, результат)\n   → дерево spans: где узкое место, где упало, сколько стоило</code></pre><p>Spans вложены (шаг → подшаг), несут атрибуты (модель, токены) и длительность. Из трейса видно bottleneck и точку отказа.</p><h2>BUILD IT</h2><p>Трейсер шагов агента (вложенные spans) с нуля: [<code>code/tracer.py</code>](../code/tracer.py).</p><ul><li><code>Tracer.span(name, **attrs)</code> — контекст-менеджер: вложенность, родитель, длительность;</li><li><code>total_duration()</code>, <code>slowest()</code> — сводка и узкое место (clock подменяем для тестов).</li></ul><pre><code>python code/tracer.py\npytest code -q</code></pre><p>Тест с фейковым clock: вложенные spans с правильным родителем и длительностью, <code>slowest</code> находит узкое место. Это мини-OpenTelemetry для агента.</p><h2>USE IT</h2><p>Готовые платформы трейсинга/observability (мульти-инструмент):</p><ul><li><strong>Arize Phoenix</strong> — open-source, на OpenTelemetry/OpenInference; трейсинг + evals локально, без отправки в SaaS; поддержка OpenAI/Claude Agent SDK, LangGraph, CrewAI.</li><li><strong>LangSmith</strong> — трейсинг + evals (offline/online), framework-agnostic, CI-интеграция.</li><li><strong>Langfuse</strong> — open-source трейсинг, токены/стоимость, LLM-as-judge.</li><li>Все на стандарте <strong>OpenTelemetry GenAI</strong> (<code>gen_ai.*</code>) — единый формат spans без вендор-лока.</li></ul><h2>SHIP IT</h2><p><strong>Артефакт:</strong> <code>observability</code> (skill) → [<code>outputs/observability/</code>](../outputs/observability/SKILL.md)</p><p>Скилл трейсинга: оборачиваешь шаги агента в spans, получаешь дерево с длительностями и узким местом. Зарегистрирован в [<code>.claude/skills/observability</code>](../../../.claude/skills/observability/SKILL.md). Связь с 10.2 (мониторинг), 10.3 (телеметрия), 7.5 (online-evals), 7.4 (отладка поломок).</p><h2>Материалы</h2><ul><li><a href=\"https://arize.com/docs/phoenix\" target=\"_blank\" rel=\"noopener\">Arize Phoenix</a> — open-source трейсинг/eval на OpenTelemetry.</li><li><a href=\"https://docs.langchain.com/langsmith/\" target=\"_blank\" rel=\"noopener\">LangSmith</a> — трейсинг и оценка агентов.</li><li><a href=\"https://opentelemetry.io/blog/2026/genai-observability/\" target=\"_blank\" rel=\"noopener\">OpenTelemetry — GenAI observability</a> — стандартные spans/метрики.</li></ul><hr><p><strong>Часы:</strong> ~4 · <strong>DoD:</strong> <code>pytest code -q</code> зелёный, демо запускается, ru.md заполнен. ✅ <strong>Урок готов</strong></p>"}],"hours":16,"outcome":"Встроить ИИ в рабочий инструмент с мониторингом токенов и стоимости."},{"id":11,"name":"Этика, безопасность, governance","lessons":[{"id":"11.1","title":"Конфиденциальность и корпоративные данные","status":"done","motto":"Прежде чем отдать текст модели — пойми, что в нём: PII, секреты, тайну отдавать нельзя.","hours":3,"dir":"phases/11-ethics-safety-governance/11-privacy-and-corporate-data","html":"<h1>Урок 11.1 · Конфиденциальность и корпоративные данные</h1><p><strong>Фаза 11 — Этика, безопасность, governance</strong> · <strong>Результат фазы:</strong> Оценить риски, настроить приватность и написать политику использования ИИ.</p><blockquote><strong>MOTTO.</strong> Прежде чем отдать текст модели — пойми, что в нём: PII, секреты, тайну отдавать нельзя.</blockquote><h2>PROBLEM</h2><p>Самый частый инцидент с ИИ в компании — утечка данных: сотрудник вставил в чат клиентские PII, ключи или коммерческую тайну. Раз отправил во внешнюю модель — контроль потерян. Нужен автоматический привратник: классифицировать чувствительность, найти и замаскировать PII, решить, можно ли отправлять.</p><h2>CONCEPT</h2><pre><code>текст → detect_pii (email/phone/card/secret)\n            │\n   classify_sensitivity: public &lt; internal &lt; confidential &lt; restricted\n            │\n   redact (маскировка)  +  can_send(level, порог) → да/нет</code></pre><p>Правило: чем чувствительнее данные, тем строже канал. Секреты/карты — <code>restricted</code>, во внешнюю модель не уходят; PII — маскируем; публичное — можно.</p><h2>BUILD IT</h2><p>Классификатор чувствительности + детектор/маскировка PII: [<code>code/data_classifier.py</code>](../code/data_classifier.py).</p><ul><li><code>detect_pii(text)</code> — email/phone/card/secret по regex;</li><li><code>classify_sensitivity(text)</code> — уровень public…restricted;</li><li><code>redact(text)</code> — маскировка; <code>can_send(level, max_allowed)</code> — решение об отправке.</li></ul><pre><code>python code/data_classifier.py\npytest code -q</code></pre><p>Демо: текст с почтой, телефоном и ключом → <code>restricted</code>, маскируется, во внешнюю модель не отправляется.</p><h2>USE IT</h2><p>Настройки приватности в инструментах (мульти-провайдер):</p><ul><li><strong>Anthropic</strong> — commercial-данные (Claude for Work/Enterprise/API) <strong>не идут на обучение</strong>; consumer — по выбору; есть ZDR для enterprise, retention 30 дней для API.</li><li><strong>OpenAI / Google</strong> — enterprise-режимы с изоляцией данных и отключением обучения.</li><li><strong>Data residency / суверенитет</strong> — важно, *где* физически лежат данные. Пример: hosted DeepSeek API хранит данные в Китае (отсюда госбаны и GDPR-вопросы в ряде юрисдикций) — для регулируемых данных это стоп-фактор.</li><li><strong>Open-weight как митигация</strong> — DeepSeek/Llama/Qwen под открытой лицензией можно <strong>запустить у себя</strong> (self-host), и данные не покидают периметр. Решает приватность ценой инфраструктуры, не цены.</li><li>Для своего пайплайна: классификация + redaction <strong>до</strong> отправки (как в Build It); чувствительное — только в одобренные каналы или в self-host.</li></ul><h2>SHIP IT</h2><p><strong>Артефакт:</strong> Политика обработки данных → [<code>outputs/data-policy.md</code>](../outputs/data-policy.md)</p><p>Политика: уровни данных, что в какие модели можно, обязательная redaction, retention. Дальше: фактчекинг (11.2), реестр рисков (11.3), общая политика ИИ (11.4).</p><h2>Материалы</h2><ul><li><a href=\"https://privacy.claude.com/en/articles/10023548-how-long-do-you-store-my-data\" target=\"_blank\" rel=\"noopener\">Anthropic — How long do you store my data</a> — retention и приватность.</li><li><a href=\"https://privacy.claude.com/en/collections/10663361-commercial-customers\" target=\"_blank\" rel=\"noopener\">Anthropic — Commercial Customers (privacy)</a> — данные бизнеса не идут на обучение.</li><li><a href=\"https://www.anthropic.com/news/updates-to-our-consumer-terms\" target=\"_blank\" rel=\"noopener\">Anthropic — Updates to Consumer Terms</a> — выбор по использованию данных.</li><li><a href=\"https://cdn.deepseek.com/policies/en-US/deepseek-privacy-policy.html\" target=\"_blank\" rel=\"noopener\">DeepSeek — Privacy Policy</a> — кейс data residency (хранение в КНР) и зачем open-weight self-host.</li></ul><hr><p><strong>Часы:</strong> ~3 · <strong>DoD:</strong> <code>pytest code -q</code> зелёный, демо запускается, ru.md заполнен. ✅ <strong>Урок готов</strong></p>"},{"id":"11.2","title":"Фактчекинг и борьба с галлюцинациями","status":"done","motto":"Доверяй, но заземляй: каждое фактическое утверждение должно опираться на источник.","hours":3,"dir":"phases/11-ethics-safety-governance/11-fact-checking-hallucinations","html":"<h1>Урок 11.2 · Фактчекинг и борьба с галлюцинациями</h1><p><strong>Фаза 11 — Этика, безопасность, governance</strong> · <strong>Результат фазы:</strong> Оценить риски, настроить приватность и написать политику использования ИИ.</p><blockquote><strong>MOTTO.</strong> Доверяй, но заземляй: каждое фактическое утверждение должно опираться на источник.</blockquote><h2>PROBLEM</h2><p>Модель уверенно генерирует факты, которых нет в источниках (урок 1.4). В governance-контексте это риск: неверная цифра в отчёте, выдуманная ссылка в письме клиенту. Нужен <strong>пайплайн фактчекинга</strong>: разложить ответ на утверждения и проверить каждое на заземлённость (grounding) в доверенных источниках.</p><h2>CONCEPT</h2><pre><code>ответ → extract_claims (утверждения)\n           │ для каждого:\n   verify_claim(claim, sources): есть ли заземление в источниках?\n           │\n   supported / unsupported → отчёт (что проверять руками)</code></pre><p>Фактчекинг — это не «верю/не верю», а <strong>проверка опоры</strong>: незаземлённое утверждение — кандидат в галлюцинации и в ручную проверку.</p><h2>BUILD IT</h2><p>Пайплайн проверки утверждений на заземление: [<code>code/fact_check.py</code>](../code/fact_check.py).</p><ul><li><code>extract_claims(text)</code> — разбить на утверждения;</li><li><code>verify_claim(claim, sources)</code> — <code>supported</code> / <code>unsupported</code> по совпадению с источниками;</li><li><code>fact_check(text, sources)</code> — отчёт по всем утверждениям.</li></ul><pre><code>python code/fact_check.py\npytest code -q</code></pre><p>Демо: «возврат в течение 14 дней» — supported (есть в источниках), «гарантия 100 лет» — unsupported. Грубое заземление по совпадению; в проде — эмбеддинги/NLI и реальные источники.</p><h2>USE IT</h2><p>Перекрёстная проверка моделями и источниками (мульти-провайдер):</p><ul><li><strong>Grounding/цитаты</strong> — проси модель цитировать источник для каждого факта (Anthropic «reduce hallucinations»).</li><li><strong>Перекрёстная проверка</strong> — тот же вопрос на другой модели; расхождения = красный флаг.</li><li><strong>RAG как источник истины</strong> — отвечать только по найденным документам (Фаза 3) и помечать незаземлённое.</li><li><strong>Право на «не знаю»</strong> — снижает выдумывание (урок 1.4).</li></ul><h2>SHIP IT</h2><p><strong>Артефакт:</strong> Чек-лист фактчекинга → [<code>outputs/fact-check-checklist.md</code>](../outputs/fact-check-checklist.md)</p><p>Чек-лист: что заземлять, как просить цитаты, когда перекрёстно проверять, что эскалировать человеку. Связь с фазой 5 (валидация данных) и фазой 3 (RAG).</p><h2>Материалы</h2><ul><li><a href=\"https://docs.claude.com/en/docs/test-and-evaluate/strengthen-guardrails/reduce-hallucinations\" target=\"_blank\" rel=\"noopener\">Anthropic — Reduce hallucinations</a> — цитаты, «не знаю», низкая температура.</li><li><a href=\"https://github.com/anthropics/courses/blob/master/prompt_engineering_interactive_tutorial/Anthropic%201P/08_Avoiding_Hallucinations.ipynb\" target=\"_blank\" rel=\"noopener\">anthropics/courses — Avoiding Hallucinations</a> — практика.</li><li><a href=\"https://arxiv.org/abs/2202.03629\" target=\"_blank\" rel=\"noopener\">Ji et al., 2022 — Survey of Hallucination in NLG</a> — типы и причины галлюцинаций.</li></ul><hr><p><strong>Часы:</strong> ~3 · <strong>DoD:</strong> <code>pytest code -q</code> зелёный, демо запускается, ru.md заполнен. ✅ <strong>Урок готов</strong></p>"},{"id":"11.3","title":"Предвзятость и юридические риски","status":"done","motto":"Вывод ИИ — это риск: предвзятость, переобещания, чужой текст; их надо аудировать.","hours":3,"dir":"phases/11-ethics-safety-governance/11-bias-and-legal-risks","html":"<h1>Урок 11.3 · Предвзятость и юридические риски</h1><p><strong>Фаза 11 — Этика, безопасность, governance</strong> · <strong>Результат фазы:</strong> Оценить риски, настроить приватность и написать политику использования ИИ.</p><blockquote><strong>MOTTO.</strong> Вывод ИИ — это риск: предвзятость, переобещания, чужой текст; их надо аудировать.</blockquote><h2>PROBLEM</h2><p>ИИ может выдать дискриминирующее обобщение, переобещание («гарантирую 100%») или дословно воспроизвести чужой текст (авторские права). В проде это репутационные и юридические риски. Нужен <strong>аудит вывода</strong>: автоматически отмечать рискованные паттерны и вести реестр рисков.</p><h2>CONCEPT</h2><pre><code>вывод ИИ → audit(rules)\n   bias       — предвзятые обобщения              → high\n   overclaim  — «гарантирую», «100%», «всегда»    → medium\n   copyright  — длинная дословная цитата          → medium\n            ▼\n   реестр рисков {category, severity, snippet}</code></pre><p>Эвристики — нижняя граница (не заменяют юриста и человека), но задают, ЧТО проверять, и масштабируются на поток.</p><h2>BUILD IT</h2><p>Аудит вывода на риски: [<code>code/risk_audit.py</code>](../code/risk_audit.py).</p><ul><li><code>audit(text, max_quote_words)</code> — находит bias / overclaim / copyright;</li><li><code>severity_counts(findings)</code> — сводка по серьёзности.</li></ul><pre><code>python code/risk_audit.py\npytest code -q</code></pre><p>Демо ловит предвзятое обобщение (high), переобещание (medium) и слишком длинную цитату (copyright). Чистый текст — пустой реестр.</p><h2>USE IT</h2><p>Аудит вывода на риски в проде (мульти-провайдер):</p><ul><li><strong>Риск-классификация по EU AI Act</strong> — определи уровень риска сценария (запрещённый/высокий/ограниченный/минимальный) → требования.</li><li><strong>NIST AI RMF</strong> — функции Govern/Map/Measure/Manage: систематически выявлять и снижать риски (включая bias).</li><li><strong>LLM-судья + правила</strong> — комбинируй эвристики (как <code>audit</code>) с моделью-ревьюером; критичное — на человека и юриста.</li></ul><h2>SHIP IT</h2><p><strong>Артефакт:</strong> Реестр рисков → [<code>outputs/risk-register.md</code>](../outputs/risk-register.md)</p><p>Шаблон реестра: риск, категория, серьёзность, вероятность, владелец, меры снижения. Дальше: общая политика ИИ, сводящая приватность, фактчекинг и риски (11.4).</p><h2>Материалы</h2><ul><li><a href=\"https://www.nist.gov/itl/ai-risk-management-framework\" target=\"_blank\" rel=\"noopener\">NIST — AI Risk Management Framework</a> — Govern/Map/Measure/Manage.</li><li><a href=\"https://digital-strategy.ec.europa.eu/en/policies/regulatory-framework-ai\" target=\"_blank\" rel=\"noopener\">EU AI Act — regulatory framework</a> — риск-ориентированное регулирование.</li><li><a href=\"https://artificialintelligenceact.eu/high-level-summary/\" target=\"_blank\" rel=\"noopener\">EU AI Act — high-level summary</a> — уровни риска и обязанности.</li></ul><hr><p><strong>Часы:</strong> ~3 · <strong>DoD:</strong> <code>pytest code -q</code> зелёный, демо запускается, ru.md заполнен. ✅ <strong>Урок готов</strong></p>"},{"id":"11.4","title":"Корпоративные политики ИИ","status":"done","motto":"Политика ИИ — это не бюрократия, а правила, которые делают использование ИИ безопасным и проверяемым.","hours":3,"dir":"phases/11-ethics-safety-governance/11-ai-usage-policy","html":"<h1>Урок 11.4 · Корпоративные политики ИИ</h1><p><strong>Фаза 11 — Этика, безопасность, governance</strong> · <strong>Результат фазы:</strong> Оценить риски, настроить приватность и написать политику использования ИИ.</p><blockquote><strong>MOTTO.</strong> Политика ИИ — это не бюрократия, а правила, которые делают использование ИИ безопасным и проверяемым.</blockquote><h2>PROBLEM</h2><p>Без политики каждый сотрудник решает сам: что отдавать модели, какие инструменты использовать, кто отвечает за ошибку. Это хаос и риски (11.1–11.3). Нужна <strong>политика использования ИИ</strong>: какие данные допустимы, какие инструменты одобрены, где обязателен человек, кто отвечает — и проверка конкретных кейсов против неё.</p><h2>CONCEPT</h2><pre><code>Политика ИИ (обязательные разделы):\n  Допустимые данные · Запрещённые данные · Одобренные инструменты ·\n  Проверка человеком · Ответственность\n        │ validate_policy → чего не хватает\n        │ check_usecase → можно ли этот кейс\n   (PII во внешнюю модель? авто-решение о людях? → блок)</code></pre><p>Политика опирается на рамки: <strong>NIST AI RMF</strong> (Govern), <strong>ISO/IEC 42001</strong> (сертифицируемая система управления ИИ), <strong>EU AI Act</strong> (обязанности по уровню риска).</p><h2>BUILD IT</h2><p>Валидатор полноты политики + проверка кейса: [<code>code/policy.py</code>](../code/policy.py).</p><ul><li><code>validate_policy(text)</code> — какие обязательные разделы отсутствуют;</li><li><code>check_usecase(sends_pii, external, automated_decision)</code> — разрешён ли кейс.</li></ul><pre><code>python code/policy.py\npytest code -q</code></pre><p>Демо: PII во внешнюю модель — заблокировано; автоскоринг людей без человека — заблокировано; саммари публичных данных — разрешено. Это политика как исполняемые правила.</p><h2>USE IT</h2><p>Согласование политики с реальными процессами (мульти-фреймворк):</p><ul><li><strong>NIST AI RMF</strong> — функция Govern: роли, ответственность, процедуры на весь жизненный цикл.</li><li><strong>ISO/IEC 42001</strong> — Plan-Do-Check-Act для AIMS; единственный сертифицируемый стандарт.</li><li><strong>EU AI Act</strong> — обязанности по уровню риска (прозрачность, оценка, логирование); сроки вступления.</li><li>Практика: политику пишут с участием security/legal, проверяют на реальных кейсах, ревизуют регулярно.</li></ul><h2>SHIP IT</h2><p><strong>Артефакт:</strong> Чек-лист политики ИИ → [<code>outputs/ai-policy-checklist.md</code>](../outputs/ai-policy-checklist.md)</p><p>Чек-лист политики: разделы, правила по данным/инструментам, точки human-in-the-loop, ответственные, ревизия. Итог Фазы 11 — оценка рисков, приватность и политика; вход в Capstone (Фаза 12).</p><h2>Материалы</h2><ul><li><a href=\"https://www.nist.gov/itl/ai-risk-management-framework\" target=\"_blank\" rel=\"noopener\">NIST — AI Risk Management Framework</a> — функция Govern и управление рисками.</li><li><a href=\"https://www.iso.org/standard/42001\" target=\"_blank\" rel=\"noopener\">ISO/IEC 42001 — AI management systems</a> — сертифицируемая система управления ИИ.</li><li><a href=\"https://digital-strategy.ec.europa.eu/en/policies/regulatory-framework-ai\" target=\"_blank\" rel=\"noopener\">EU AI Act — regulatory framework</a> — обязанности и сроки.</li></ul><hr><p><strong>Часы:</strong> ~3 · <strong>DoD:</strong> <code>pytest code -q</code> зелёный, демо запускается, ru.md заполнен. ✅ <strong>Урок готов</strong></p>"},{"id":"11.5","title":"Prompt injection и безопасность агентов","status":"done","motto":"Данные из недоверенных источников — это код для модели; их инструкции нельзя выполнять вслепую.","hours":4,"dir":"phases/11-ethics-safety-governance/11-prompt-injection-agent-security","html":"<h1>Урок 11.5 · Prompt injection и безопасность агентов</h1><p><strong>Фаза 11 — Этика, безопасность, governance</strong> · <strong>Результат фазы:</strong> Оценить риски, настроить приватность и написать политику использования ИИ.</p><blockquote><strong>MOTTO.</strong> Данные из недоверенных источников — это код для модели; их инструкции нельзя выполнять вслепую.</blockquote><h2>PROBLEM</h2><p>LLM не отличает «инструкции» от «данных» — обе идут одним каналом. Поэтому веб-страница, письмо или документ из RAG могут содержать <strong>скрытую команду</strong>, которая перехватывает агента (<strong>prompt injection</strong>, <strong>RAG-poisoning</strong>). Для агента с инструментами это критично: injection ведёт к «excessive agency» — выполнению опасных действий. Это <strong>№1 риск OWASP LLM Top 10 (LLM01)</strong>.</p><h2>CONCEPT</h2><pre><code>недоверенные данные (веб/письмо/RAG-чанк)\n   │ могут содержать: \"ignore previous… exfiltrate…\"\n   ▼\ndetect_injection → sanitize → tool allowlist → human на критичном\n   (defense-in-depth: ни один слой не достаточен сам по себе)</code></pre><p>Полного решения нет (природа LLM), поэтому — <strong>эшелонированная защита</strong>: детект + санитизация + least-privilege инструменты + человек на критичных действиях (7.3).</p><h2>BUILD IT</h2><p>Демо prompt injection / RAG-poisoning + базовая защита: [<code>code/injection_defense.py</code>](../code/injection_defense.py).</p><ul><li><code>detect_injection(text)</code> — маркеры инъекций (RU+EN);</li><li><code>sanitize(text)</code> — нейтрализация в недоверенных данных;</li><li><code>tool_allowed</code> / <code>safe_to_act(action, allowlist, context)</code> — блок при инъекции в контексте или инструменте вне allowlist.</li></ul><pre><code>python code/injection_defense.py\npytest code -q</code></pre><p>Тест: детект EN/RU, sanitize маскирует, <code>safe_to_act</code> блокирует RAG-poisoning и инструмент вне allowlist. ⚠️ Детект по сигнатурам — нижняя граница (обходится перефразировкой); это слой, а не серебряная пуля.</p><h2>USE IT</h2><p>Защита в проде (мульти-инструмент):</p><ul><li><strong>OWASP LLM01</strong> — чек-лист: разделять данные/инструкции, ограничивать привилегии, фильтровать вход/выход, human-in-the-loop на чувствительном, adversarial-тесты.</li><li><strong>MCP-гейты</strong> — на сервере инструментов (Ф6): allowlist, аутентификация, изоляция; не отдавать модели опасные операции.</li><li><strong>Runtime-guardrails</strong> — input/output tripwire (OpenAI Agents SDK guardrails, урок 7.3), least-privilege инструментов, sandbox для self-host агентов (4.5).</li><li>Связь: приватность (11.1), guardrails/HITL (7.3), поломки агентов (7.4).</li></ul><h2>SHIP IT</h2><p><strong>Артефакт:</strong> Чек-лист защиты от prompt injection → [<code>outputs/prompt-injection-checklist.md</code>](../outputs/prompt-injection-checklist.md)</p><p>Практический чек-лист (по OWASP LLM01): что фильтровать, какие allowlist, где человек, как тестировать. Завершает Фазу 11 на стороне agent security.</p><h2>Материалы</h2><ul><li><a href=\"https://owasp.org/www-project-top-10-for-large-language-model-applications/\" target=\"_blank\" rel=\"noopener\">OWASP — Top 10 for LLM Applications</a> — LLM01 Prompt Injection и др.</li><li><a href=\"https://owasp.org/www-project-top-10-for-large-language-model-applications/assets/PDF/OWASP-Top-10-for-LLMs-v2025.pdf\" target=\"_blank\" rel=\"noopener\">OWASP — Top 10 for LLMs 2025 (PDF)</a> — описание и митигации.</li><li><a href=\"https://www.anthropic.com/research/building-effective-agents\" target=\"_blank\" rel=\"noopener\">Anthropic — Building Effective Agents</a> — guardrails и ограничение автономии.</li></ul><hr><p><strong>Часы:</strong> ~4 · <strong>DoD:</strong> <code>pytest code -q</code> зелёный, демо запускается, ru.md заполнен. ✅ <strong>Урок готов</strong></p>"}],"hours":14,"outcome":"Оценить риски, настроить приватность и написать политику использования ИИ."},{"id":12,"name":"Capstone — финальный проект","lessons":[{"id":"12.1","title":"Выбор процесса и спецификация","status":"done","motto":"Capstone начинается не с кода, а со спецификации: цель, измеримые метрики, ограничения, бюджет.","hours":10,"dir":"phases/12-capstone/12-process-selection-and-spec","html":"<h1>Урок 12.1 · Выбор процесса и спецификация</h1><p><strong>Фаза 12 — Capstone</strong> · <strong>Результат фазы:</strong> Автоматизировать реальный процесс end-to-end и защитить решение с метриками эффекта.</p><blockquote><strong>MOTTO.</strong> Capstone начинается не с кода, а со спецификации: цель, измеримые метрики, ограничения, бюджет.</blockquote><h2>PROBLEM</h2><p>«Автоматизирую что-нибудь с ИИ» — путь в никуда: без чёткой цели и <strong>измеримых</strong> метрик нечего защищать и не докажешь эффект «до/после». Нужно выбрать реальный процесс и зафиксировать спецификацию так, чтобы успех был проверяемым.</p><h2>CONCEPT</h2><pre><code>CapstoneSpec\n  goal        — что автоматизируем (один конкретный процесс)\n  metrics[]   — измеримые: baseline («до») + target (цель)  ← без этого нет защиты\n  constraints — приватность, human-in-the-loop, безопасность\n  budget_usd  — потолок расходов (FinOps, Фаза 9)\n        │ validate_spec → готова ли к сборке</code></pre><p>Хороший выбор процесса: частый, болезненный, с понятной метрикой эффекта (время/стоимость/качество), не критичный для жизни на старте.</p><h2>BUILD IT</h2><p>Спецификация + валидатор измеримости: [<code>code/spec.py</code>](../code/spec.py).</p><ul><li><code>CapstoneSpec</code> / <code>Metric</code> — структура спеки;</li><li><code>is_measurable(metric)</code> — есть ли baseline и target;</li><li><code>validate_spec(spec)</code> — чего не хватает (цель, измеримые метрики, бюджет).</li></ul><pre><code>python code/spec.py\npytest code -q</code></pre><p>Валидатор не пропустит спеку без измеримых метрик или бюджета — иначе защиту (12.3) не на чем строить.</p><h2>USE IT</h2><p>Согласование с реальной задачей: возьми <strong>свой</strong> процесс (разбор тикетов, подготовка отчётов, ответы клиентам), зафиксируй baseline руками (сколько времени/денег сейчас), поставь target. Опирайся на:</p><ul><li><strong>Define success criteria</strong> (Anthropic) — критерии успеха до разработки.</li><li><strong>Empirical evaluations</strong> — как измерять качество.</li><li>Согласуй ограничения (приватность 11.1, human-in-the-loop 7.3) с реальными процессами.</li></ul><h2>SHIP IT</h2><p><strong>Артефакт:</strong> Проектная спецификация → [<code>outputs/capstone-spec.md</code>](../outputs/capstone-spec.md)</p><p>Заполненная спека твоего процесса (цель · метрики baseline/target · ограничения · бюджет). Это вход в сборку решения (12.2) и основа защиты (12.3).</p><h2>Материалы</h2><ul><li><a href=\"https://docs.anthropic.com/en/docs/build-with-claude/define-success\" target=\"_blank\" rel=\"noopener\">Anthropic — Define your success criteria</a> — критерии успеха.</li><li><a href=\"https://docs.anthropic.com/en/docs/build-with-claude/develop-tests\" target=\"_blank\" rel=\"noopener\">Anthropic — Create strong empirical evaluations</a> — измеримость качества.</li><li><a href=\"https://github.com/stabuev/ai-engineering-from-scratch\" target=\"_blank\" rel=\"noopener\">ai-engineering-from-scratch</a> — образец каркаса «фазы → артефакты».</li></ul><hr><p><strong>Часы:</strong> ~10 · <strong>DoD:</strong> <code>pytest code -q</code> зелёный, демо запускается, ru.md заполнен. ✅ <strong>Урок готов</strong></p>"},{"id":"12.2","title":"Сборка решения","status":"done","motto":"Capstone — это не новый кубик, а сборка изученного в один работающий процесс.","hours":10,"dir":"phases/12-capstone/12-building-the-solution","html":"<h1>Урок 12.2 · Сборка решения</h1><p><strong>Фаза 12 — Capstone</strong> · <strong>Результат фазы:</strong> Автоматизировать реальный процесс end-to-end и защитить решение с метриками эффекта.</p><blockquote><strong>MOTTO.</strong> Capstone — это не новый кубик, а сборка изученного в один работающий процесс.</blockquote><h2>PROBLEM</h2><p>Все кирпичи курса собраны (промпты, RAG, агенты, MCP, роутинг, мониторинг). Задача Capstone — соединить их в <strong>один end-to-end процесс</strong>, решающий реальную задачу из спеки (12.1), а не очередное демо. Главное — рабочая интеграция, а не отдельные фрагменты.</p><h2>CONCEPT</h2><pre><code>вход → [роутинг по сложности (Ф9)] → выбор модели\n     → [guardrail (Ф7)] → пропустить опасное?\n     → [инструмент через MCP (Ф6)] → действие\n     → [память (Ф4/7)] → состояние процесса\n     → [телеметрия токенов/стоимости (Ф10)] → учёт\n     → результат</code></pre><p>Capstone = граф из кирпичей: агентный цикл + cost-aware маршрутизация + память + инструменты + наблюдаемость. Каждый блок уже собран в своей фазе — здесь они работают вместе.</p><h2>BUILD IT</h2><p>End-to-end процесс из кирпичей курса: [<code>code/solution.py</code>](../code/solution.py).</p><ul><li><code>classify(task)</code> — роутинг по сложности (Фаза 9);</li><li><code>Capstone.run(task)</code> — guardrail → инструмент → память → телеметрия; возвращает результат + модель + стоимость;</li><li><code>total_cost()</code> — суммарные расходы процесса.</li></ul><pre><code>python code/solution.py\npytest code -q</code></pre><p>Демо: простая задача → Haiku (дёшево), сложная → Opus; обе записаны в память и телеметрию. Это скелет реального процесса.</p><h2>USE IT</h2><p>Интеграция в рабочий инструмент (мульти-провайдер): замени стабы на реальные блоки курса.</p><ul><li><strong>Агент</strong> — цикл reason→act→observe (Фаза 7) на Claude Agent SDK / OpenAI Agents SDK.</li><li><strong>Инструменты/данные</strong> — через MCP-сервер (Фаза 6), доступ к БД/файлам с контролем (6.5, 11.1).</li><li><strong>Cost-aware</strong> — роутер/каскад (Фаза 9), кэш и batch.</li><li><strong>Память</strong> — модуль из 4.2; <strong>наблюдаемость</strong> — логи/телеметрия (Фаза 10).</li><li>Встрой за фича-флагом + fallback (10.1) в реальный инструмент команды.</li></ul><h2>SHIP IT</h2><p><strong>Артефакт:</strong> Работающий end-to-end процесс → [<code>outputs/solution.py</code>](../outputs/solution.py)</p><p>Сборка процесса под твою спеку (12.1). Дальше — замерить эффект и защитить (12.3).</p><h2>Материалы</h2><ul><li><a href=\"https://www.anthropic.com/research/building-effective-agents\" target=\"_blank\" rel=\"noopener\">Anthropic — Building Effective Agents</a> — паттерны сборки агентных систем.</li><li><a href=\"https://platform.claude.com/docs/en/agent-sdk/overview\" target=\"_blank\" rel=\"noopener\">Anthropic — Agent SDK overview</a> — agent loop, инструменты, контекст.</li><li><a href=\"https://modelcontextprotocol.io/specification/2025-11-25\" target=\"_blank\" rel=\"noopener\">MCP — Specification</a> — подключение инструментов и данных.</li></ul><hr><p><strong>Часы:</strong> ~10 · <strong>DoD:</strong> <code>pytest code -q</code> зелёный, демо запускается, ru.md заполнен. ✅ <strong>Урок готов</strong></p>"},{"id":"12.3","title":"Презентация и защита","status":"done","motto":"Защита — это цифры: насколько упало время и стоимость, выросло качество, какой ROI.","hours":10,"dir":"phases/12-capstone/12-presentation-and-defense","html":"<h1>Урок 12.3 · Презентация и защита</h1><p><strong>Фаза 12 — Capstone</strong> · <strong>Результат фазы:</strong> Автоматизировать реальный процесс end-to-end и защитить решение с метриками эффекта.</p><blockquote><strong>MOTTO.</strong> Защита — это цифры: насколько упало время и стоимость, выросло качество, какой ROI.</blockquote><h2>PROBLEM</h2><p>Работающее решение (12.2) ничего не доказывает без измерений. Защита Capstone — не рассказ «я сделал агента», а <strong>эффект</strong>: сравнение «до/после» по времени, качеству и стоимости и расчёт ROI. Без baseline из спеки (12.1) защищать нечего.</p><h2>CONCEPT</h2><pre><code>метрики «до» (baseline) vs «после» (решение):\n   время   ↓  improvement%   (меньше = лучше)\n   стоимость ↓ improvement%  (FinOps, Ф9–10)\n   качество ↑ improvement%   (eval, Ф2.5/5.5)\n        │\n   ROI = (экономия + выгода) / затраты\n        │\n   презентация + чек-лист компетенций</code></pre><p>Честная защита: реальные замеры, а не «кажется лучше»; учтённая стоимость решения; ограничения и риски (Фаза 11) проговорены.</p><h2>BUILD IT</h2><p>Замеры «до/после» + ROI + чек-лист компетенций: [<code>code/metrics.py</code>](../code/metrics.py).</p><ul><li><code>improvement(before, after, lower_is_better)</code> — % улучшения (время/стоимость vs качество);</li><li><code>roi(before_cost, after_cost, value_gained)</code> — окупаемость;</li><li><code>before_after(...)</code> — сводка по метрикам; <code>competency_report(...)</code> — готовность компетенций.</li></ul><pre><code>python code/metrics.py\npytest code -q</code></pre><p>Демо: время −70%, стоимость −80%, качество +25.7%, ROI 5.5. Это и есть содержание защиты.</p><h2>USE IT</h2><p>Демо и обратная связь:</p><ul><li><strong>Замерь до/после</strong> на реальных кейсах: время (хронометраж), качество (eval-harness 2.5 / валидация 5.5), стоимость (телеметрия 10.3 / FinOps 9.4).</li><li><strong>Посчитай ROI</strong> «до/после» (FinOps for AI): экономия + выгода / затраты на решение.</li><li><strong>Покажи демо</strong> процесса и собери обратную связь; проговори ограничения и риски (Фаза 11).</li></ul><h2>SHIP IT</h2><p><strong>Артефакт:</strong> Презентация + чек-лист компетенций → [<code>outputs/presentation-and-competencies.md</code>](../outputs/presentation-and-competencies.md)</p><p>Структура защиты (проблема → решение → метрики до/после → ROI → риски → демо) + чек-лист освоенных компетенций курса. Это финал курса: реальный процесс с доказанным эффектом.</p><h2>Материалы</h2><ul><li><a href=\"https://www.finops.org/wg/finops-for-ai-overview/\" target=\"_blank\" rel=\"noopener\">FinOps Foundation — FinOps for AI</a> — стоимость и cost per outcome.</li><li><a href=\"https://docs.anthropic.com/en/docs/build-with-claude/develop-tests\" target=\"_blank\" rel=\"noopener\">Anthropic — Create strong empirical evaluations</a> — измерение качества.</li><li><a href=\"https://www.cloudzero.com/blog/finops-for-ai/\" target=\"_blank\" rel=\"noopener\">CloudZero — FinOps for AI</a> — ROI и метрики эффекта.</li></ul><hr><p><strong>Часы:</strong> ~10 · <strong>DoD:</strong> <code>pytest code -q</code> зелёный, демо запускается, ru.md заполнен. ✅ <strong>Урок готов</strong></p>"}],"hours":30,"outcome":"Автоматизировать реальный процесс end-to-end и защитить решение с метриками эффекта."}];
const GLOSSARY = [{"term":"Токен","says":"Кусок текста, которым оперирует модель","means":"Не слово и не символ: текст бьётся на токены по статистике (BPE). Платишь и считаешь бюджет в токенах (урок 1.1)."},{"term":"Контекстное окно","says":"Сколько токенов модель видит за раз","means":"Конечный бюджет: system + история + RAG + ответ. Вышло за окно — модель не видит (уроки 1.2, 4.4)."},{"term":"Inference","says":"Генерация ответа по одному токену","means":"Авторегрессивный цикл: предсказать токен → дописать → повторить. Отсюда стриминг (урок 1.2)."},{"term":"Температура","says":"Степень случайности генерации","means":"T=0 — детерминированный argmax; выше — разнообразнее. На новых Claude не настраивается (урок 1.3)."},{"term":"Галлюцинация","says":"Уверенно неверный ответ","means":"Модель предсказывает правдоподобное, а не истину. Лечится заземлением, цитатами, правом «не знаю» (уроки 1.4, 11.2)."},{"term":"Промпт","says":"Интерфейс к модели","means":"Анатомия: роль, контекст, задача, формат, примеры. Воспроизводимость измеряется eval-harness (Фаза 2)."},{"term":"Few-shot","says":"Примеры в промпте","means":"Показать 2–5 примеров вход→выход, чтобы задать формат/класс без дообучения (урок 2.2)."},{"term":"Chain-of-Thought","says":"Пошаговое рассуждение","means":"«Думай по шагам» поднимает качество на многошаговых задачах (урок 2.2)."},{"term":"RAG","says":"Найди нужное, потом ответь","means":"Retrieval + augmentation: подкладываем релевантные куски в промпт. Прод-версия — гибрид+rerank (уроки 3.3, 3.5)."},{"term":"Эмбеддинг","says":"Представление текста числами","means":"Близкое по смыслу близко в пространстве; поиск по косинусу. Учебно — TF-IDF (урок 3.3)."},{"term":"Векторное хранилище","says":"База для поиска по смыслу","means":"add/query/persist по эмбеддингам (Chroma/FAISS). Память RAG (урок 3.4)."},{"term":"MCP","says":"Разъём ИИ ↔ инструменты","means":"Model Context Protocol: host ↔ client ↔ server, tools/resources/prompts, discovery (Фаза 6)."},{"term":"Tool use","says":"Вызов инструментов моделью","means":"Function calling: модель просит инструмент по схеме, получает результат в цикле (уроки 6.1, 7.1)."},{"term":"Агент","says":"Цикл reason → act → observe","means":"Не модель, а петля: policy выбирает действие, инструмент исполняет, результат в историю (урок 7.1)."},{"term":"Guardrail","says":"Заслон перед действием","means":"Валидация + подтверждение опасного (human-in-the-loop) до исполнения (урок 7.3)."},{"term":"Оркестратор","says":"Раздаёт роли субагентам","means":"Lead декомпозирует задачу, субагенты выполняют, lead сводит (Фаза 8)."},{"term":"A2A","says":"Протокол агент ↔ агент","means":"Карточки возможностей (discovery) + конверт задачи. Дополняет MCP (урок 8.2)."},{"term":"FinOps","says":"Экономика моделей","means":"Маршрутизация по сложности, каскад+кэш+batch, учёт и ROI «до/после» (Фаза 9)."},{"term":"Observability","says":"Видимость работы агента","means":"Вложенные spans на шаги (как OpenTelemetry): где узкое место и где упало (урок 10.4)."},{"term":"Prompt injection","says":"Перехват через данные","means":"Скрытая инструкция в данных/RAG (OWASP LLM01). Защита: allowlist, sanitize, человек (урок 11.5)."}];
